window.COURSE_CHUNKS=window.COURSE_CHUNKS||{};window.COURSE_CHUNKS["数据结构"]=[{"page":121,"body":"**本节完整补充**\n\n**可变数组（Resizable Array）**\n\nC 语言的数组是固定大小的，尽管 C99 可以用变量来定义一个数组的大小，但是一旦我们定义了这个数组，在运行过程中是无法改变它的大小的。如果我们在运行程序前没法确定具体大小，传统做法只能是尽可能让数组大一点，但是仍然有可能面临用完的风险，或者空间浪费的问题。\n\nThink about a set of functions that provide a mechanism of resizable array of int.\n\n• Growable\n\n• Get the current size\n\n• Access to the elements\n\n**The Interface(函数接口)**\n\n```c\nArray array_create(int init_size);\nvoid array_free(Array *a);\nint array_size(const Array *a);\nint* array_at(Array *a, int index);\nvoid array_inflate(Array *a, int more_size);\n```\n\n* 有些地方会把结构定义为 *Array，因为后面的函数接口很多地方都要用到结构的指针，而不是结构本身，这样看起来似乎更加简洁美观一点。\n\n* 但是现在越来越多人习惯不在定义新类型时把`*`带进去，因为如果这样定义，Array 一定是个从别处制造出来的指针（比如动态申请的内存），如果有个函数，要想在内部定义一个 Array 类型的本地变量 a 就做不到了。如果仅仅定义为 Array，那可以直接在函数内部用`Array a;`定义一个类型为 Array 的变量。\n\n* 简而言之，不要定义指针类型，除了上面的理由外，还有别的人阅读代码时，很难想到 Array 被定义成指针类型了，不利于程序可读性。\n\nArray.h 文件：\n\n```c\n#ifndef _ARRAY_H_\n#define _ARRAY_H_\n\ntypedef struct {\n    int *array;\n    int size;\n} Array;\n\nconst int BLOCK_SIZE = 5;\n\nArray array_create(int init_size);\nvoid array_free(Array *a);\nint array_size(const Array *a);\nint* array_at(Array *a, int index);\nvoid array_inflate(Array *a, int more_size);\n\n#endif\n```\n\nArray.c 文件：\n\n```c\n#include \"array.h\"\n\nArray array_create(int init_size)\n{\n    Array a;\n    a.size = init_size;\n    a.array = (int *) malloc(sizeof(int) * a.size);\n    return a;\n}\n\nvoid array_free(Array *a)\n{\n    free(a->array);\n    a->array = NULL;\n    a->size = 0;\n}\n\nint array_size(const Array *a)\n{\n    return a->size;\n}\n\nint* array_at(Array *a, int index)\n{\n    if (index >= a->size)\n        array_inflate(a, (index / BLOCK_SIZE + 1) * BLOCK_SIZE - a->size);\n    return &(a->array[index]);\n}\n\nvoid array_inflate(Array *a, int more_size)\n{\n    int *p = (int*)malloc(sizeof(int) * (a->size + more_size));\n    int i;\n    for (i = 0; i < a->size; i++)  //可用库函数 memcpy()实现\n        p[i] = a->array[i];\n    free(a->array);\n    a->array = p;\n    a->size += more_size;\n}\n```\n\nmain.c 文件：\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include \"Array.h\"\n#include \"Array.c\"\n\nint main(int argc, char const *argv[])\n{\n    Array a = array_create(10);\n    printf(\"length(a) = %d\\n\", array_size(&a));\n    *array_at(&a, 0) = 10;\n    printf(\"a[0] = %d\\n\", *array_at(&a, 0));\n    int number = 0;\n    int cnt = 0;\n    while (number != -1) {\n        scanf(\"%d\", &number);\n        if (number != - 1)\n            *array_at(&a, cnt++) = number;\n    }\n    for (int i = 0; i < array_size(&a); i++) {\n        printf(\"%d \", *array_at(&a, i));\n        if ((i + 1) % 5 == 0)\n            printf(\"\\n\");\n    }\n    array_free(&a);\n\n    return 0;\n}\n```\n\n**14.1-1*可变数组**\n\n**the Interface**\n\n1. Array array_create(int init_size);创建数组\n\n2. void array_free(Array*a);回收数组\n\n3. int array_size(const Array*a);告诉我们数组里面现在有几个单元可以使用\n\n4. int*array_at(Array*a,int index);访问数组某个单元\n\n5. void array_inflate(Array*a,int more_size);让数组\n\n```c\n#ifndef _ARRAY_H_\n#define _ARRAY_H_\n\ntypedef struct {\n    int *array;\n    int size;\n}Array;\n\nArray array_create(int init_size);\nvoid array_free(Array *a);\nint array_size(const Array*a);\n4. int*array_at(Array*a,int index);\n5. void array_inflate(Array*a,int more_size);\n\n#endif\n#include \"array.h\"\n\nArray array_create(int init_size)\n{\n    Array a;//首先是数组的创建，需要用到动态内存分配，结合所需要的size，来创建一个数组\n    a.size = init_size;\n    a.array = (int*)malloc(sizeof(int)*a.size);\n    return a;\n}\nvoid array_free(Array *a)//再然后就是内存的释放、得到数组的大小\n{\n    free(a->array);\n    a->array = NULL;\n    a->size = 0;\n}\n int array_size(const Array*a);\n int*array_at(Array*a,int index);\n void array_inflate(Array*a,int more_size);\n\nint main(int argc,char const *argv[])\n{\n    Array a = array_create(100);\n\n    return 0;\n}\n```"},{"page":122,"body":"**本节完整补充**\n\n**Ⅰ. Array array_create(int init_size)创建数组函数解析**\n\n在初始化过程中，array_create() 函数内部的本地变量 a，会用我们在该函数内部制作出来的结构初始化。换句话说，a 存在于 main() 本地，array_create() 和 main() 的 a 的成员都是一样的，这样 main() 函数可以更灵活的使用 array a\n\nQ1：为什么返回 Array 不返回 Array 的指针？\n\n* 因为 a 是本地变量，返回本地变量，本地变量就无效了。\n\nQ2：为什么不传入一个结构指针，在函数内部 malloc() 一个结构，处理后返回 malloc 的东西呢？\n\n* 因为这样使用 Array 更复杂。如果按上面的定义方式，main() 函数只要`Array a = array_create(100);`即可创建数组，很像正常的 c 语言代码的写法。\n\n如果按下面的方式定义函数，会有两个潜在风险：\n\n①传入的指针`a == NULL`\n\n②传入的指针 a 已经指向一个存在的数组，这时还需要先 free()\n\n所以，与其进行这么复杂的处理，不如直接像上面一样返回 Array。当然，上面的方法也仅仅是一种可能，不代表最优的方法。\n\n```c\nArray* array_create(Array* a, int init_size)\n{\n    a->size = init_size;\n    a->array = (int *) malloc(sizeof(int) * a.size);\n    return a;\n}\n```\n\n**Ⅱ. void array_free(Array *a)释放数组函数解析**\n\n* 主函数 main() 的 a 本身离开 main()，空间会被自动回收，可是这个 Array a 结构中有指针所指的东西 array*，指针所指的东西你需要有恰当的方式去释放它。\n\n* 所以 array_free() 做的事情就是对 a->array 来说的，保险起见可以继续让`a->array = NULL;a->size = 0;`因为 free(0)、free(NULL) 是无害的，防止使用者调用两次函数。\n\n**Ⅲ.int array_size(const Array *a)数组长度函数解析**\n\n* 简单返回数组大小。\n\nQ：为什么不直接在函数外部使用`a.size`访问呢？\n\n* 这种做法叫**封装**。将函数内部细节保护起来了，保护`a.size`，而且，如果将来模块变得复杂，不能简单通过`a.size`获取长度时，这时改进 array_size() 函数即可，方便代码维护。\n\n**Ⅳ.int* array_at(Array *a, int index)数组元素访问函数解析**\n\n* 访问指定下标的数组元素。\n\nQ：为什么要返回指针而不是返回一个 int\n\n* 这样可以直接获取到目标变量的地址，甚至可以直接拿这个变量做赋值。\n\n* 当然，也可以不这么写，而改成两个子函数实现上述的访问+赋值两个操作，此时访问数组得到值即可，形参也可以设为 const，修改数组元素可以交给 array_set() 函数处理。\n\n```c\nint array_get(const Array *a, int index)\n{\n    return a->array[index];\n}\nvoid array_set(Array *a, int index, int value)\n{\n    a->array[index] = value;\n}\n```\n\n**Ⅴ. void array_inflate(Array *a, int more_size)数组扩容函数解析**\n\n* 本来 malloc() 出来的内存空间不能自己扩容，我们的解决方案是重新去 malloc() 一块内存，然后再用一个循环将旧空间里面的元素逐一拷贝到新空间里面去。\n\n* 将 array_inflate() 函数和 array_at() 函数结合，就可以得到一个访问函数越界就自动扩容的函数。\n\n* 考虑到扩容过程是，新申请一块内存，然后把旧数据拷贝的策略。所以每次扩容如果仅仅申请 1 个空间（正好满足新元素的访问），未免太不经济了，会浪费大量时间在拷贝上。于是引入`BLOCK_SIZE`的概念。每次内存空间不够时，新扩容`sizeof(int) * BLOCK_SIZE`的内存，这样就不用每超出一个内存单元就扩容一次了。\n\n* 这样，通过 array_at() 函数引入`BLOCK_SIZE`概念，我们在不改变 array_inflate() 函数的情况下，实现了数组扩容过程的优化。\n\n& ██████████ \\\\\n\n& ██████████\\underset{BLOCK}{\\underbrace{█████}}\n\n扩容 BLOCK 个内存单元\n\n**14.1-2可变数组的数据访问**\n\n```c\n//从上述第16行内容延续，这里需要多加两个标准头文件\n#include<stdio.h>\n#include<stdlib.h>\n//3-6行的代码叫做封装,能够将里面的内容保护起来，这样别人就不知道你里面什么样子的了，保持神秘感哈哈\n int array_size(const Array*a)\n {\n     return a->size;\n }\n int*array_at(Array*a,int index)//在然后是进行该数组的访问和修改\n     //这里面需要注意的是array_at的返回需要是一个指针，如此便可以做到修改\n {\n     return &(a->array[index]);\n }\n\nint*array_get(const Array*a,int index);\n{\n    return a->array[index];\n}\n\nvoid array_set(Array *a,int index, int value)\n{\n    a->array[index] = value;\n}\n void array_inflate(Array*a,int more_size);\n\nint main(int argc,char const *argv[])\n{\n    Array a = array_create(100);\n    printf(\"%d\\n\",array_size(&a));\n    //printf(\"%d\\n\",a.size);十七行跟十六行一个意思\n    *array_at(&a,0) = 10;//将一个值写到数组里面\n    printf(\"%d\\n\",*array_at(&a,0));\n    array_free(&a);\n\n    return 0;\n}\n```"},{"page":123,"body":"**本节完整补充**\n\n**Ⅰ. Array array_create(int init_size)创建数组函数解析**\n\n在初始化过程中，array_create() 函数内部的本地变量 a，会用我们在该函数内部制作出来的结构初始化。换句话说，a 存在于 main() 本地，array_create() 和 main() 的 a 的成员都是一样的，这样 main() 函数可以更灵活的使用 array a\n\nQ1：为什么返回 Array 不返回 Array 的指针？\n\n* 因为 a 是本地变量，返回本地变量，本地变量就无效了。\n\nQ2：为什么不传入一个结构指针，在函数内部 malloc() 一个结构，处理后返回 malloc 的东西呢？\n\n* 因为这样使用 Array 更复杂。如果按上面的定义方式，main() 函数只要`Array a = array_create(100);`即可创建数组，很像正常的 c 语言代码的写法。\n\n如果按下面的方式定义函数，会有两个潜在风险：\n\n①传入的指针`a == NULL`\n\n②传入的指针 a 已经指向一个存在的数组，这时还需要先 free()\n\n所以，与其进行这么复杂的处理，不如直接像上面一样返回 Array。当然，上面的方法也仅仅是一种可能，不代表最优的方法。\n\n```c\nArray* array_create(Array* a, int init_size)\n{\n    a->size = init_size;\n    a->array = (int *) malloc(sizeof(int) * a.size);\n    return a;\n}\n```\n\n**Ⅱ. void array_free(Array *a)释放数组函数解析**\n\n* 主函数 main() 的 a 本身离开 main()，空间会被自动回收，可是这个 Array a 结构中有指针所指的东西 array*，指针所指的东西你需要有恰当的方式去释放它。\n\n* 所以 array_free() 做的事情就是对 a->array 来说的，保险起见可以继续让`a->array = NULL;a->size = 0;`因为 free(0)、free(NULL) 是无害的，防止使用者调用两次函数。\n\n**Ⅲ.int array_size(const Array *a)数组长度函数解析**\n\n* 简单返回数组大小。\n\nQ：为什么不直接在函数外部使用`a.size`访问呢？\n\n* 这种做法叫**封装**。将函数内部细节保护起来了，保护`a.size`，而且，如果将来模块变得复杂，不能简单通过`a.size`获取长度时，这时改进 array_size() 函数即可，方便代码维护。\n\n**Ⅳ.int* array_at(Array *a, int index)数组元素访问函数解析**\n\n* 访问指定下标的数组元素。\n\nQ：为什么要返回指针而不是返回一个 int\n\n* 这样可以直接获取到目标变量的地址，甚至可以直接拿这个变量做赋值。\n\n* 当然，也可以不这么写，而改成两个子函数实现上述的访问+赋值两个操作，此时访问数组得到值即可，形参也可以设为 const，修改数组元素可以交给 array_set() 函数处理。\n\n```c\nint array_get(const Array *a, int index)\n{\n    return a->array[index];\n}\nvoid array_set(Array *a, int index, int value)\n{\n    a->array[index] = value;\n}\n```\n\n**Ⅴ. void array_inflate(Array *a, int more_size)数组扩容函数解析**\n\n* 本来 malloc() 出来的内存空间不能自己扩容，我们的解决方案是重新去 malloc() 一块内存，然后再用一个循环将旧空间里面的元素逐一拷贝到新空间里面去。\n\n* 将 array_inflate() 函数和 array_at() 函数结合，就可以得到一个访问函数越界就自动扩容的函数。\n\n* 考虑到扩容过程是，新申请一块内存，然后把旧数据拷贝的策略。所以每次扩容如果仅仅申请 1 个空间（正好满足新元素的访问），未免太不经济了，会浪费大量时间在拷贝上。于是引入`BLOCK_SIZE`的概念。每次内存空间不够时，新扩容`sizeof(int) * BLOCK_SIZE`的内存，这样就不用每超出一个内存单元就扩容一次了。\n\n* 这样，通过 array_at() 函数引入`BLOCK_SIZE`概念，我们在不改变 array_inflate() 函数的情况下，实现了数组扩容过程的优化。\n\n& ██████████ \\\\\n\n& ██████████\\underset{BLOCK}{\\underbrace{█████}}\n\n扩容 BLOCK 个内存单元\n\n**14.1-3可变数组的自动增长**\n\n```c\n//从上述24行延续\n void array_inflate(Array*a,int more_size)\n {\n     int*p = (int*)malloc(sizeof(int)(a->size + more_size));\n     int i;\n     for( i = 0; i < a->size; i++ ) {\n         p[i] = a->size[i];\n     }//可以换成标准库的函数mencpy，效率更高\n     free(a->array);\n     a->array = p;\n     a->size += more_size;\n }//核心代码\n\nint main(int argc,char const *argv[])\n{\n    Array a = array_create(100);\n    printf(\"%d\\n\",array_size(&a));\n    //printf(\"%d\\n\",a.size);十七行跟十六行一个意思\n    *array_at(&a,0) = 10;//将一个值写到数组里面\n    printf(\"%d\\n\",*array_at(&a,0));\n    int number;\n    int cnt = 0;\n    while(number != -1 ){\n        scanf(\"%d\",&number);\n        if( number != -1 ){\n        *array_at(&a,cnt++) = number;\n        //scanf(\"%d\",array_at(&a,cnt++));\n    }//无限的读入整数，让自己不断的自己增长\n    array_free(&a);\n\n    return 0;\n}\n//第九行的内容改动\n\nint*array_at(Array*a,int index)//在然后是进行该数组的访问和修改\n     //这里面需要注意的是array_at的返回需要是一个指针，如此便可以做到修改\n {\n    if( index >= a->size ){\n        array_inflate(a,(index/BLOCK_SIZE+1)-a->size+1);//这里有点乱，后续要来修正\n    }\n     return &(a->array[index]);\n }\n```"},{"page":124,"body":"**本节完整补充**\n\n**可变数组的缺陷**\n\nAllocate new memory each time it inflates is an easy and clean way. But\n\n• It takes time to copy, and\n\n• may fail in memory restricted situation\n\n除了之前提到的，每次扩容时要花很多时间拷贝的问题之外。（特别是在内存受限场合，比如单片机等等）有时候会出现总内存空间足够，但无法再申请内存空间的情况（比如目前申请了一半的内存空间，要再申请一半+BLOCK 的内存空间就没法做到了，有一部分内存空间没法充分利用）。\n\n这时候，如果有一种方法，每申请一个 BLOCK，就将其“链”到前一个 BLOCK 之后（**Linked Blocks**），而不是重新申请一块内存空间，再拷贝，就能充分利用到内存的每一块，从而解决上述两个问题。\n\n**14.2-1可变数组的缺陷**\n\n1. 申请使用内存，当我们需要更大的内存而之前的不需要了之后，之前的就会被废弃掉，在内存受限的情况下(比如单片机)就会导致内存明明还有，但是却已经申请不了更大的内存了(浪费内存空间)\n\n2. 效率极低\n\n可变数组每次扩容都重新申请一整块更大的内存，再把旧内容复制过去，然后释放旧块。这种方法实现简单，但有两个现实缺陷：复制耗时，以及内存受限时“明明还有不少空闲块，却申请不到一个连续的大块”。\n\n例如原先有 100 个元素，要扩容到 200：如果系统里剩下 80 个空闲元素、但被其他分配占用，不能连续凑出 100 个新元素，`realloc` 或 `malloc` 就可能失败，即使空闲总空间足够。\n\n链表式方案不是一次性申请大块连续内存，而是每次申请一个固定大小的 BLOCK，用链接把它们串起来。这样单个分配请求变小，各 BLOCK 可以分散在内存中，更适合内存受限的嵌入式环境。代价是需要维护额外的链指针，遍历从顺序下标访问变成沿链表移动，删除和插入则可能更灵活。\n\n这个对比可以整理成：\n\n- 数组式：随机访问快、缓存友好，但扩容复制成本高、连续内存要求高。\n\n- 链表式：扩容局部、内存碎片容忍性好，但随机访问慢、需要指针和释放管理。\n\n选择数据结构时，先量化数据规模、访问方式和内存限制。不要把“数组简单”当成永远正确的答案，也不要把“链表动态”当成无代价的万能解。\n\n可变数组思路简单，但“扩容即整块复制”的代价不能只看元素个数。`realloc` 可能原地扩展，也可能申请新块并复制旧内容；失败时它返回 `NULL`，原内存仍然有效。正确写法是先用临时指针接收返回值，再更新数组指针，不能直接把原指针覆盖，否则失败后无法释放旧内存。\n\n一个最小扩容骨架可以这样理解：\n\n```c\nint *newData = realloc(data, newCapacity * sizeof(int));\nif (!newData) {\n    /* 保留 data，按当前容量继续或清理退出 */\n}\ndata = newData;\ncapacity = newCapacity;\n```\n\n这段代码暴露出两个真实约束：新容量必须乘以元素 sizeof，不能只传元素数量；扩容后旧数据是否自动复制由 `realloc` 负责，但在嵌入式或自定义分配器中不能假设它总能找到连续内存。即使空闲内存总量足够，碎片也可能让一个大申请失败，这就是可变数组在内存受限环境下的第二类问题。\n\n链表式方案用多个小块连接，让每次申请变小，从而降低连续内存压力，但代价是随机访问需要沿链移动，并且每个节点还要保存下一个指针。对这种“空间碎片”和“访问成本”的权衡，课程后面还会用链表、树和文件系统反复验证。选择结构前先明确：数据要频繁按下标访问，还是频繁增长和插入？允许复制旧数据，还是必须避免大块连续分配？回答后再比较数组与链接块策略。\n\n再看一个关键细节：扩容倍数不是越大越好，也不是每次只加一个元素最好。容量按倍数增长可以摊薄复制成本，但倍数过大时，一次性申请的大块内存可能更早失败，而且会留下大量暂时用不上的空间。平衡的做法通常是按约 1.5 倍或 2 倍扩容，并在容量较大时改为分块链表，而不是让单块内存无限增大。\n\n实际调试动态内存时，应该检查分配是否失败、释放是否完整、释放后是否仍访问旧指针，以及 `realloc` 返回新地址后旧的 `data` 是否还被使用。`malloc`、`realloc` 和 `free` 都可能改变堆的状态，单个变量保存好当前有效指针后，所有读写都只能通过它进行。链表方案同样需要记录头指针和尾指针，如果丢失链头，后续节点就会变成无法释放的内存。讨论内存结构时，必须把“存储布局”“访问方式”“增长方式”和“释放策略”放在一起看，不能只看其中一个优点。"},{"page":125,"body":"**本节完整补充**\n\n**2.链表**\n\n本节介绍了链表的概念和实现。链表是由多个节点组成的数据结构，每个节点包含一个数据和一个指向下一个节点的指针。与数组不同，链表的大小可以动态改变。\n\n用链表代替数组进行数据的存储和操作主要有两个优点：一是不需要事先定义存储空间大小，可以实时动态分配，内存使用效率高；二是可以很方便地插入新元素（结点），使数据保持排序状态，操作效率高。\n\n**链表（Linked List）**\n\n* 链表是一种常见而重要的动态存储分布的数据结构。它由若干个同一结构类型的“结点”依次串接而成的。链表有很多类型，本节只介绍单向链表。\n\n**链表的定义与结构**\n\n```c\ntypedef struct _node {\n    int value;\n    struct _node *next;\n} Node;\n```\n\n* 链表结点由**数据**和**指向下一个结点的指针**组成。\n\n* 通常使用结构的嵌套来定义单向链表结点的数据类型，利用 typedef 关键字将类型简化为 Node，第三行编译器还不知道 Node 关键字，所以不能将第三行的 struct _node 简写成 Node。\n\n* 结构类型 Node 中的 next 分量又是该结构类型的指针，称为**结构的递归定义**。利用这种定义方法，可构造出单向链表这一较为复杂的数据结构。\n\n**链表的构建与添加元素**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct _node {\n    int value;\n    struct _node *next;\n} Node;\n\nint main(int argc, char const *argv[])\n{\n    Node *head = NULL;\n    int number;\n    do{\n        scanf(\"%d\", &number);\n        if (number != -1)\n        {\n        //add to linked-list\n        Node *p = (Node*)malloc(sizeof(Node));\n        p->value = number;\n        p->next = NULL;\n        //find the last\n        Node *last = head;\n        if (last)\n        {\n            while (last->next)\n            {\n                last = last->next;\n            }\n            //attach\n            last->next = p;\n            }\n            else\n            {\n                head = p;\n            }\n        }\n    }while (number != -1);\n}\n```\n\n* 链表变量一般用指针`head`表示，用来存放链表首结点的地址。\n\n* 找到最后一个元素的方法就是令`last`指向头指针，然后遍历到所指元素的`next`为空。\n\n* 要申请大小为`Node`结构的动态内存空间，新申请到的空间首地址要被强制类型转换成`Node*`型指针，并保存到指针变量`p`中，经过强制类型转换，使得赋值号两边的**类型一致**。这就是下面这句语句的含义：\n\n* （如果申请成功，p 指向被分配内存空间的起始地址；若未申请到内存空间，则 p 的值为 NULL。）\n\n```c\nNode *p = (Node*)malloc(sizeof(Node));\n```\n\n* 注意，对于链表为空时，要特殊考虑，因为链表为空，`last == head`，而`head == NULL`，此时`last->next`，即空指针的`next`是无效的，所以链表为空时要特殊处理，上面用了一个`if-else`分别处理。\n\n* **小 Tips**：快速找到你程序中**链表的边界情况**，就是看你的**指针出现在指向运算符左边时**，会不会有`NULL`的特殊情况，因为`NULL`的指向运算是错误的，此时要有代码（比如 if 语句）保证这些语句是安全的。\n\n链表示意图\n\n**14.2-2链表**\n\n1. 这是为百度进来补充的图，翁恺的课程是没有静态的图，用动态进行演示的\n\n```c\n#include\"node.h\"\n#include<stdio.h>\n#include<stdlib.h>\n\n//typedef struct _node{\n//    int value;\n//    struct _node *next;\n//}Node;\n\nint main(int argc,char const argv[])\n{\n    Node*head = NULL;\n    int number;\n    do{\n        scanf(\"%d\",&number);\n        if( number != -1 ) {\n            //add to linked-list\n            Node*p = (Node*)malloc(size(Node));\n            p->next = NULL;\n            //find the last\n            Node*last = head;\n            if( last ){\n                while (last ->next){\n                    last = last->next;\n                    }\n                //attach\n                last->next = p;\n                }else{\n                    head = p;\n            }\n        }\n    }while( number != -1 );\n\n    return 0;\n}\n```"},{"page":126,"body":"**本节完整补充**\n\n**链表的函数**\n\n* 如何设计一个函数，实现上一小节插入节点的操作？\n\n* 如果仅仅是简单的把核心代码作为新函数的代码，会有什么问题？\n\n```c\nvoid add(Node *head, int number)\n{\n    //add to linked-list\n    Node *p = (Node*)malloc(sizeof(Node));\n    p->value = number;\n    p->next = NULL;\n    //find the last\n    Node *last = head;\n    if (last) {\n        while (last->next) {\n            last = last->next;\n        }\n        //attach\n        last->next = p;\n    } else {\n        head = p;\n    }\n}\n\nint main(int argc, char const *argv[])\n{\n    Node *head = NULL;\n    int number;\n    do{\n        scanf(\"%d\", &number);\n        if (number != -1) {\n            add(head, number);\n        }\n    } while (number != -1);\n\n    return 0;\n}\n```\n\n* 但是有个问题，在 add() 函数内部修改指针 head，不会影响到 main() 里面的指针 head。\n\n* 这是因为你在函数内部修改的是**指针的副本**，而不是原始的指针。即使你在函数内部改变了指针的值（例如让它指向一个新的节点），这个改变也不会反映到函数外部，因为函数外部的指针并没有被修改。\n\n方案一：Node* head 是一个全局变量\n\n* 如果只是学习算法来说，可能没什么大问题。但是，编写程序要注意全局变量是有害的。这样写，你这个 add 函数其实就是一次性的，只对这个全局变量 head 适用，如果有带有其他头结点的其他链表，还要重新编写过。\n\n* 所以我们的想法是，这个 head 尽量使用传入的参数，而不是一个全局变量。\n\n方案二：add 函数返回类型改为`Node*`\n\n* 然后在主函数调用时返回 Node *的变量到主函数\n\n```c\nhead = add(head, number);\n```\n\n* 这个方案有进步，没有使用全局变量，而且 add 函数是线程安全的，而且 add 函数可以针对很多不同的链表。\n\n* 但是有个唯一的小缺点，就是使用你这个函数的程序员要非常小心的做这个赋值操作；\n\n* 因为 C 语言没办法强迫程序员一定这么做，如果他忘了在主函数给 head 指针赋值，此时对空链表的 add 就是错误的操作。\n\n方案三： add 函数传入二级指针`Node** pHead`\n\n* 此时返不返回指针已经无所谓了，主函数也不用再像方案二一样赋值。不过后面要用到`head`的地方要改成`*phead`\n\n```c\nvoid add(Node** phead, int number)\n{\n    ···\n        Node *last = *phead;\n    ···\n        else {\n            *phead = p;\n        }\n}\n```\n\n* 这个也是个不错的方案，我们传入了一个指针的指针（二级指针），这样在函数内部，能让我们对指针的值做修改。这就与 swap() 函数类似，传两个变量进去是没有意义的，所以我们传两个变量的指针进去。\n\n方案四：（**最佳方案**）自定义一个新的类型 List 来代表整个链表\n\n```c\ntypedef struct _list {\n    Node *head;\n} List;\n\nvoid add(List *pList, int number)\n{\n    //add to linked-list\n    Node *p = (Node*)malloc(sizeof(Node));\n    p->value = number;\n    p->next = NULL;\n    //find the last\n    Node *last = pList->head;\n    if (last) {\n        while (last->next)\n            last = last->next;\n        //attach\n        last->next = p;\n    } else {\n        pList->head = p;\n    }\n}\n\nint main(int argc, char const *argv[])\n{\n    List list;\n    int number;\n    list.head = NULL;\n    do{\n        scanf(\"%d\", &number);\n        if (number != -1)\n            add(&list, number);\n    } while (number != -1);\n\n    return 0;\n}\n```\n\n* 这么做有很多好处，比如方便之后**扩充、改进我们的链表函数**，比如找为尾指针，可以定义一个 tail 指针永远指向尾部节点。\n\n* 注意一下，这么做以后，初始化以及函数 add 中的 find last 操作都**会发生变化**，此处不展开。\n\n```c\ntypedef struct _list {\n    Node *head;\n    Node *tail;\n} List;\nint main(···)\n{\n    ···\n    list.head = list.tail = NULL;\n    ···\n}\n```\n\n**14.2-3链表的函数**\n\n```c\n#include\"node.h\"\n#include<stdio.h>\n#include<stdlib.h>\n\n//typedef struct _node{\n//    int value;\n//    struct _node *next;\n//}Node;\nvoid add(Node*head,int number);\n\ntypedef struct _list{\n    Node*head;\n    Node*tail;\n}List;\n\nint main(int argc,char const argv[])\n{\n    Node*head = NULL;\n    List list;\n    int number;\n    list.head = list.tail = NULL;\n    do{\n        scanf(\"%d\",&number);\n        if( number != -1 ){\n            add(&list,number);\n        }\n    }while( number != -1);\n\n    return 0;\n}\n\nvoid add(List*pList,int number)\n{\n       //add to linked-list\n    Node*p = (Node*)malloc(size(Node));\n    p->value = number;\n    p->next = NULL;\n    //find the last\n    Node*last = pList->head;\n       if( last ){\n          while (last ->next){\n              last = last->next;\n            }\n          //attach\n           last->next = p;\n          }else{\n            head = p;\n    }\n    return head;\n}\n```"},{"page":127,"body":"**本节完整补充**\n\n**链表的搜索**\n\n**1.链表的遍历**\n\n* 下面是链表遍历的经典写法。\n\n* 注意，由于各结点在内存中不是连续存放的，所以不可以用`p++`来寻找下一个结点。\n\n```c\nNode *p;\nfor (p = list.head; p; p = p->next)\n    printf(\"%d\\t\", p->value);\n```\n\n* 可以借此改写成**链表输出函数**。\n\n```c\nvoid print(List *pList) {\n    Node *p;\n    for (p = pList->head; p; p = p->next)\n        printf(\"%d\\t\", p->value);\n    printf(\"\\n\");\n}\n```\n\n**2.链表搜索函数**\n\n```c\nint search(List *pList, int number)\n{\n    int ret = 0;\n    Node *p;\n    for (p = pList->head; p; p = p->next) {\n        if (p->value == number) {\n            ret = 1;\n            break;\n        }\n    }\n\n    return ret;\n}\n```\n\n**14.2-4链表的搜索**\n\n```c\n//从上述16行延续\nvoid print(List *pList)//函数原型置顶，另外说明我没有把这个置顶到其他代码块里面\nvoid add(List*pList,int number)\n\nint main(int argc,char const argv[])\n{\n    Node*head = NULL;\n    List list;\n    int number;\n    list.head = list.tail = NULL;\n    do{\n        scanf(\"%d\",&number);\n        if( number != -1 ){\n            add(&list,number);\n        }\n    }while( number != -1);\n\n    printf(&list);\n    scanf(\"%d\",&number);\n    Node*p;\n    int isFound = 0;\n    for( p = list.head; p; p = p->next){\n        if( p->value == number ){\n            printf(\"找到了\\n\");\n            isFound = 1;\n            break;\n        }\n    }\n    if ( !isFound ){\n        printf(\"没找到\\n\");\n    }\n    //Node*p;\n    //for( p=list.head; p; p = p->next){\n    //    printf(\"%d\\t\",p->value);\n    //}//遍历，把链表每个节点的值打出来\n    //printf(\"\\n\");  这些内容转移到下面了，并且有了一部分的改动\n\n    return 0;\n}\n\nvoid add(List*pList,int number)\n{\n       //add to linked-list\n    Node*p = (Node*)malloc(size(Node));\n    p->value = number;\n    p->next = NULL;\n    //find the last\n    Node*last = pList->head;\n       if( last ){\n          while (last ->next){\n              last = last->next;\n            }\n          //attach\n           last->next = p;\n       }else{\n            head = p;\n       }\n}\n\nvoid print(List *pList){\n    Node*p;\n    for( p=list->head; p; p = p->next){\n        printf(\"%d\\t\",p->value);\n    }//遍历，把链表每个节点的值打出来\n    printf(\"\\n\");\n}\n```"},{"page":128,"body":"**本节完整补充**\n\n**链表的删除**\n\n* 链表的删除其实要做两件不同的事，一是将目标删除节点前面那个节点指向目标删除节点后面的节点，二是将目标删除节点内存空间释放。（插入节点时**先连后断**，删除节点时**先接后删**）\n\n* 使用两个指针 p 和 q，q 指针指向 p 的**前一个节点**。\n\n```c\nvoid del(List *pList, int number)\n{\n    Node *p, *q;\n    for (q = NULL, p = pList->head; p ; q = p, p = p->next) {\n        if (p->value == number) {\n            if (q)\n                q->next = p->next;\n            else\n                pList->head = p->next;\n            free(p);\n            break;\n        }\n    }\n}\n```\n\n删除节点有边界问题，可以通过之前提到的最机械的一个“小技巧”，由于 p 和 q 都曾出现在指向运算符`->`的左边，而 for 循环的条件是 p 不为 NULL，所以 p 不会有边界问题，而 q 存在边界问题，此时链表的第一个头结点就是待删除节点，需要特殊处理。\n\n链表删除（删除值为 1 的节点）\n\n**14.2-5链表的删除**\n\n```c\n//从上述第5行开始\nint main(int argc,char const argv[])\n{\n    Node*head = NULL;\n    List list;\n    int number;\n    list.head = list.tail = NULL;\n    do{\n        scanf(\"%d\",&number);\n        if( number != -1 ){\n            add(&list,number);\n        }\n    }while( number != -1);\n\n    printf(&list);\n    scanf(\"%d\",&number);\n    Node*p;\n    int isFound = 0;\n    for( p = NULL; p=list.head; q = p,p = p->next){\n        if( p->value == number ){\n            //需要考虑到边界效应，q没有进行限制需要进行限制\n            if(q){\n                q->next = p->next;\n            } else {\n                list.head = p->next;\n            }\n            //q->next = p->next;\n            free(p);\n            break;\n        }\n    }\n    //Node*p;\n    //for( p=list.head; p; p = p->next){\n    //    printf(\"%d\\t\",p->value);\n    //}//遍历，把链表每个节点的值打出来\n    //printf(\"\\n\");  这些内容转移到下面了，并且有了一部分的改动\n\n    return 0;\n}\n```"},{"page":129,"body":"**本节完整补充**\n\n**链表清除**\n\n* 因为我们的链表所有节点都是 malloc 出来的，所以在最后需要将它们的内存空间释放掉，即把整个链表清除干净。\n\n和之前删除节点的思路是类似的，只是从删除一个节点，变成删除一整串节点。\n\n* （注：在释放内存后，指针的值仍然可以打印出来，原因在于释放操作只是**将内存标记为可用**，而**不是将内存内容清空或改变**。）\n\n```c\nvoid clear(List *pList)\n{\n    Node *p, *q;\n    for (p = pList->head; p; p = q) {\n        q = p->next;\n        free(p);\n    }\n}\n```\n\n**14.2-6链表的清除**\n\n如何整个链表都清除掉\n\n```c\n    //从上述19行开始\n    for( p = NULL; p=list.head; q = p,p = p->next){\n        if( p->value == number ){\n            //需要考虑到边界效应，q没有进行限制需要进行限制\n            if(q){\n                q->next = p->next;\n            } else {\n                list.head = p->next;\n            }\n            //q->next = p->next;\n            free(p);\n            break;\n        }\n    }\n    for( p=head; p; p=q ){\n        q = p->next;\n        free(p);\n    }//链表这样就删除掉了\n\n    return 0;\n}\n```\n\n链表的每个节点都由 `malloc` 申请，因此程序结束前必须逐个调用 `free`，否则产生内存泄漏。清除整个链表时，关键不是“把指针置空”，而是先保存下一个节点的地址，再释放当前节点。\n\n```c\nvoid clear(List *list)\n{\n    Node *current = list->head;\n    while (current) {\n        Node *next = current->next;\n        free(current);\n        current = next;\n    }\n    list->head = NULL;\n}\n```\n\n这里 `next = current->next` 必须在 `free(current)` 之前完成；一旦释放当前节点，再访问 `current->next` 就是使用已经释放的内存，属于未定义行为。\n\n释放与删除节点的区别也值得记住：\n\n- 删除一个节点：先找到前驱，修改前驱的 next，再 `free` 当前节点。\n\n- 清除整条链表：反复保存 next、释放当前节点、移动到 next，最后把头指针设为 NULL。\n\n- 释放只是把内存标记为可用，并不保证内容被清零；释放后仍打印指针值，可能看到旧值，但这不代表可以继续访问。\n\n如果链表是循环链表，循环条件要从“当前节点是否为空”改为判断是否回到头节点；如果节点里还有动态申请的子资源，也要在释放节点前逐一释放。写链表代码时，先用纸画出 `head`、`current`、`next` 三个指针，再逐行移动，能有效避免指针丢失和重复释放。"}];
