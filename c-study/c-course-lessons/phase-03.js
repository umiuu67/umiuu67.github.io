window.COURSE_CHUNKS=window.COURSE_CHUNKS||{};window.COURSE_CHUNKS["控制流"]=[{"page":21,"body":"**本节完整补充**\n\n**4.在线评判系统 (Online Judge、OJ)**\n\n**注意事项**\n\n* 除了题目要求的输出，不能输出其他内容，也不用设置任何用户友好型提示。\n\n**各类提交状态提示**\n\n1. 通过**(Accept，AC)**\n\n* 表示代码通过了 OJ 的所有测试点。有些 OJ 系统设有部分通过的提示，说明有些测试点或者边界条件没有通过。\n\n2. 答案错误**(Wrong Answer，WA)**\n\n* 表示代码有错误，需要 debug\n\n3. 运行时间超限**(Time Limit Exceeded，TLE)**\n\n* 有两种情况：\n\n* 代码运行过程中出现了死循环，需要进行 debug\n\n* 代码时间复杂度过高，无法在规定时间之内处理所有数据，需要优化\n\n* 解决方法：printf() 调试大法、优化算法时间复杂度\n\n4. 编译错误**(Compile Error，CE)**\n\n* 说明代码中有不合法语句，需根据提示进行修改\n\n5. 运行空间超限**(Memory Limit Exceeded，MLE)**\n\n* 一般有两种情况\n\n* 递归栈爆了\n\n* 数组空间开太多了\n\n6. 格式错误**(Presentation Error，PE)**\n\n* 说明结果没按题目要求输出，请检查空格、换行符等等。\n\n7. 段错误**(Segmentation Fault，SF)**\n\n* 错误原因比较多，就是访问了不可访问的内存。\n\n* 比如数组下标越界。\n\n8. 运行时错误**(Runtime Error，RE)**\n\n* 运行时错误，这个一般是程序在运行期间执行了非法的操作造成的。可能是下列情况之一\n\n* 数组越界：把定义的数组开大点\n\n* 指针越界\n\n* 数组开得太大，超出了栈的范围，造成栈溢出。\n\n* 除以 0\n\n* 非法输入，比如不在合法范围内的 n 的输入有没有特殊处理\n\n9. 非零返回**(Non zero return，NZR)**\n\n* 当函数执行过程中遇到错误或异常情况时\n\n* 主函数返回了一个非 0 的值，比如说错把主函数当成子函数写了，返回了一个变量。\n\n**边界测试**\n\n* 程序的边界最容易出错，所以测试程序经常使用边界数据，如有效范围两端的数据、特殊的倍数等等\n\n* 常用的边界数据：端点、0、负数等等\n\n* **章末习题：逆序的三位数**\n\n程序每次读入一个正 3 位数，然后输出按位逆序的数字。注意：当输入的数字含有结尾的 0 时，输出不应带有前导的 0。比如输入 700，输出应该是 7。\n\n**输入格式：**\n\n每个测试是一个 3 位的正整数。\n\n**输出格式：**\n\n输出按位逆序的数。\n\n**输入样例：**\n\n```in\n> 123\n>\n```\n\n**输出样例：**\n\n```out\n> 321\n>\n```\n\n解答：\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int n = 0;\n    scanf(\"%d\",&n);\n    printf(\"%d\",(n % 10) * 100 + (n % 100 / 10) * 10 + (n / 100);\n    //printf(\"%d\",(n % 10) * 100 + (n / 10 % 10) * 10 + (n / 100);也可以\n    return 0;\n}\n```"},{"page":22,"body":"**本节完整补充**\n\n正确示例：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    int n;\n    if (scanf(\"%d\", &n) != 1) return 1;\n\n    int ones = n % 10;\n    int tens = n / 10 % 10;\n    int hundreds = n / 100;\n    int reversed = ones * 100 + tens * 10 + hundreds;\n\n    printf(\"%d\\n\", reversed);\n    return 0;\n}\n```\n\n这道题的核心是“拆位”和“重新组合”，而不是用一个字符串倒序。输入 123：\n\n- 个位：`123 % 10 = 3`；\n\n- 十位：`123 / 10 % 10 = 2`；\n\n- 百位：`123 / 100 = 1`；\n\n- 结果：`3 * 100 + 2 * 10 + 1 = 321`。\n\n输入 700 时，个位是 0，十位是 0，百位是 7，组合后得到 `7`。不要把它写成字符串 `\"007\"`，题目要求的是数值输出，前导零不应该出现。\n\n一个完整且带括号正确的写法如下：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    int n;\n    scanf(\"%d\", &n);\n\n    int ones = n % 10;\n    int tens = n / 10 % 10;\n    int hundreds = n / 100;\n    int reversed = ones * 100 + tens * 10 + hundreds;\n\n    printf(\"%d\\n\", reversed);\n    return 0;\n}\n```\n\n如果题目只保证三位正整数，代码可以简化；如果题目允许负数，需要先记录符号并对绝对值处理。在 OJ 中，`printf` 里的 `%d` 会按十进制输出，`n % 10` 对正数得到非负余数。检查时多试 `100`、`101`、`700`、`709`、`999`，并确认每一行输出末尾换行符合要求。\n\n逆序三位数的核心不是记住一个公式，而是把“数字位置”翻译成“十进制权重”：个位原值乘以 100，十位原值乘以 10，百位原值乘以 1，最后相加。先拆位，再拼接，可以减少一整段表达式里同时混合除法、取余和乘法的错误。\n\n为了提交时可复用，建议把读入、计算、输出拆开，并检查 `scanf` 是否成功：\n\n手工测试 100：`ones = 0`，`tens = 0`，`hundreds = 1`，结果是 1；测试 709：个位 9、十位 0、百位 7，结果是 907；测试 999：每一位都是 9，结果是 999。要注意 700 的输出不是 007，而是用整数算术得到 7，因此不要给结果补固定宽度。\n\n这类题最容易出现的是括号错误、运算符优先级错误和格式错误。把一个复杂表达式拆成 `ones`、`tens`、`hundreds` 三个变量，即使用 `-Wall` 也能更快定位；在 OJ 提交时只保留规定输出，不要在结果前后加整数或其他文字。"},{"page":23,"body":"**本节完整补充**\n\n**程序中的错误**\n\n* C 语言的代码可以写在不同的两行，是符合语法的。\n\n* 编译的时候发现的错误所在的地方会以红色的底表示出来 (Dev-C++为例)\n\n* 具体的错误原因以英文列在下方的窗口里\n\n* C 的编译器给出的错误提示往往不那么好“猜”\n\n* 不要用中文，C 编译器只能识别半角的英文符号\n\n* 关于数字 0，为了防止和英文字母 O 混淆，编译器往往在 0 的中间加点或者斜线区分，这个是有一定历史渊源的\n\n* 调试是在程序中查找错误并修改错误的过程。(错误包括**逻辑错误(语义错误)**和**语法错误**)\n\n* 有时调试可以不借助编译器的调试工具，而是在程序某些节点插入 printf()，把值直接打印出来观察是否有异常。\n\n“0 的故事”表面上讲一个数字，实际上讲的是“程序为什么出错”和“如何确认状态”。C 语言中 0 有多种身份：整数 0、浮点数 0.0、空字符 `'\\0'`、空指针 `NULL`、逻辑假。它们的写法不同，用途也不同，不能因为都“看起来是 0”就直接混用。\n\n初学阶段最值得建立的是错误分类：\n\n- 语法错误：缺少分号、括号不匹配、关键字拼错、中英文符号混用，编译器会报错。\n\n- 逻辑错误：程序能编译运行，但结果不对，例如判断条件写反、循环少一次。\n\n- 运行时错误：除数为 0、数组越界、访问空指针，程序可能崩溃或产生不确定行为。\n\n调试时不要只盯着最终输出。用 `printf` 在关键位置打印变量的值，观察它是否符合预期；更系统的方法是设置断点，单步执行每一行，看变量在赋值前、赋值后分别是什么。\n\n注意 C 编译器只认识半角英文符号，中文引号、中文分号、中文括号都会造成神秘错误。看似不起眼的 0 与字母 O 也容易混淆，编译器常把数字 0 显示成带斜线或点的形状。遇到“代码明明和教程一样却编译不过”时，先检查是否使用了全角符号，再检查是否把 `0` 写成了 `O`。\n\n这一讲表面讲数字 0，实际是在讲“空值家族”和错误定位。整数 0、浮点数 0.0、字符终止符 `'\\0'`、空指针 `NULL`、逻辑假在许多条件里都会表现为非真，但它们的类型、大小和用途并不一样。\n\n- `0` 是 `int` 的合法值，表示数量为零。\n\n- `0.0` 是 `double` 的合法值，表示小数意义上的零。\n\n- `'\\0'` 是字符常量，常用于字符串结束标记；它与字符 `'0'` 不同，后者是数字 0 的字符表示。\n\n- `NULL` 表示指针不指向有效对象，用来初始化指针或检查分配失败，不能当作整数使用。\n\n- 逻辑假是条件表达式的结果，通常由 0 表示；逻辑真通常由非零值表示。\n\n代码写错时，还要区分三类错误：语法错误在编译阶段被发现，例如漏掉分号、括号不匹配或使用中文标点；类型或格式不匹配通常由编译器警告揭示，例如用 `%d` 输出 `long long`；运行时错误则要在执行阶段出现，例如除零、越界或未初始化变量。\n\n调试不是“把代码改到碰巧能跑”，而是先看第一条错误定位，再回读相关变量的初值和边界。发生“奇怪输出”时，优先打印或单步观察相关状态；发生“程序没有反应”时，检查是否等待 `scanf`、变量是否从未推进、循环条件是否永远成立。小步修改、及时编译，比一次改动一大片更容易找到问题。"},{"page":24,"body":"**本节完整补充**\n\n**if 语句-根据条件做还是不做**\n\n* 根据之前计算时间差的讨论，如果这次我们就采用直接分别减的方案，判断有没有出现借位可以吗？\n\n* 借位的结果是分钟小于 0，出现借位我们再调整，我们从这里入手。\n\n```c\nint hour1, minute1;\nint hour2, minute2;\n\nscanf(\"%d %d\", &hour1, &minute1);\nscanf(\"%d %d\", &hour2, &minute2);\n\nint ih = hour2 - hour1;\nint im = minute2 - minute1;\nif (im < 0)\n{\n    im = 60 + im;\n    ih--;\n}\n\nprintf(\"时间差是 %d 时 %d 分。\", ih, im);\n```\n\n上述代码中，我们使用了 C 语言中的条件判断语句，我们可以根据条件成立与否来判断接下来的动作是做还是不做。\n\n```c\n//if 语句 写法\nif (条件成立)\n{\n    ... 做复合语句内动作 ...\n}\n```\n\n* if 语句后面可以不用大括号，但是 if 只对紧随其后的一句语句有效，注意，if 语句结束的地方不在条件成立那个圆括号后，而是在后面的语句之后。\n\n**如果**\n\n1.用分别减的方案，然后判断有没有出现借位行不行？\n\n2.借位的表现是，分钟减的结果小于0，找小时借一位，如下:im代指分钟，ih代指小时\n\n```c\nif(im < 0){\n    im = 60 + im;\n    ih--;\n}\n```\n\nif(条件成立){\n\n....\n\n}\n\n**判断的条件**\n\n计算两个值之间的关系，所以叫做关系运算\n\n- 运算符 · 意义\n\n- == · 相等\n\n- != · 不相等\n\n- > · 大于\n\n- >= · 大于或等于\n\n- < · 小于\n\n- <= · 小于或等于\n\nif 的职责是让程序根据条件选择路径。条件写在圆括号里，条件成立时执行后面的语句；条件不成立时跳过。`if` 本身不是“判断句子”，而是一个控制结构，真正决定走向的是括号里的表达式的值：非 0 为真，0 为假。\n\n时间差问题是最常见的一个入口：\n\n```c\nint hour1, minute1;\nint hour2, minute2;\nscanf(\"%d %d\", &hour1, &minute1);\nscanf(\"%d %d\", &hour2, &minute2);\n\nint hours = hour2 - hour1;\nint minutes = minute2 - minute1;\nif (minutes < 0) {\n    minutes += 60;\n    hours--;\n}\nprintf(\"%d %d\\n\", hours, minutes);\n```\n\n这个方案先做直接相减，再检查是否借位。借位时把分钟加 60、小时减 1，因此不需要写“是否跨小时”的长条件。写分支前要把状态枚举清楚：正常、分钟不足、小时不足、恰好相等。\n\n`if` 后面只有一条语句时可以省略大括号，但省略后容易让后来者误以为第二行也属于分支。初学阶段统一使用大括号，能让边界和嵌套关系更明确。多个互斥条件不要用多个独立 `if` 穷举，而应使用 `if / else if / else`，因为独立 if 可能同时执行多个分支，破坏互斥逻辑。"},{"page":25,"body":"**本节完整补充**\n\n**判断的条件-关系运算符**\n\n* 计算两个值之间的关系运算，所以又称为**关系运算符**。\n\n表-关系运算符及意义\n\n- 运算符 · 意义\n\n- == · 相等\n\n- != · 不相等\n\n- > · 大于\n\n- >= · 大于或等于\n\n- < · 小于\n\n- <= · 小于或等于\n\n* 关系运算的结果是个**逻辑量**，取值只有 0 和 1，如果关系成立，则为 1，否则为 0。\n\n* 再次强调运算优先级：单目 > **算术 > 关系** > 逻辑 > 赋值 > 逗号\n\n* 而同样在关系运算符内，判断相等的`==`和判断不等的`!=`优先级比其他关系运算符更低，为什么这么设定，是由于可以方便的做出诸如`5 > 3 == 6 > 4`的表达式。\n\n* 连续的关系运算符是 从左到右 进行的\n\n```c\n5 > 3 == 6 > 4    //结果为 1\n6 > 5 > 4        //结果为 0\na == b == 6        //结果为 0\na == b > 0        //结果和 a、b 的值有关\n```\n\n**判断的条件**\n\n计算两个值之间的关系，所以叫做关系运算\n\n**关系运算的结果**\n\n1. 当两个值的关系符合关系运算符的预期时，关系运算的结果为整数1，否则为整数0\n\n2. printf(\"%d\\n\",5==3);\n\n3. printf(\"%d\\n\",5>3);\n\n4. printf(\"%d\\n\",5<=3);\n\n**优先级**\n\n1. 所有的关系运算符的优先级比算数运算的低，但是比赋值运算的高\n\n1. 7>=3+4 在这里面3+4先运算然后等于7，所以式子是可以运行的\n\n2. int r = a > 0; 在这里a>0先运算，式子成立，答案为1，所以r = 1\n\n关系运算符用于比较两个值，结果是 1（真）或 0（假），这个结果本身也是 int 值。常见关系运算符包括 `>`、`>=`、`<`、`<=`、`==`、`!=`。最容易写错的是比较相等：`a == b` 是比较，`a = b` 是赋值。\n\n下面的表达式可以帮助你理解优先级：\n\n```c\nint a = 4, b = 5;\nprintf(\"%d\\n\", a < b);      // 1\nprintf(\"%d\\n\", a == b);     // 0\nprintf(\"%d\\n\", a != b);     // 1\nprintf(\"%d\\n\", a = b);      // 5，但这是赋值表达式\n```\n\nC 中不能直接写数学里的 `0 = 0 && x <= 10`。\n\n- 关系运算的结果是逻辑量，逻辑运算符再把它组合成更复杂的条件：`&&` 表示两个条件都成立，` · ` 表示至少一个成立，`!` 表示取反。由于 `&&` 和 ` · ` 有短路特性，`a != 0 && 10 / a > 2` 可以避免除零，但不要依赖它写出难以理解的代码。\n\n条件表达式的优先级通常低于算术运算，高于赋值。写复杂条件时加括号不仅不是多余，还能让编译器和你自己都看清意图。"},{"page":26,"body":"**本节完整补充**\n\n**找零计算器-判断、注释、流程图**\n\n* 找零计算器需要用户做的两个操作：输入购买的金额、输入支付的票面\n\n* 找零计算器根据用户输入做的动作：计算并打印找零或者提示用户余额不足\n\n* 从计算机角度看，就是意味着程序需要读用户的两个输入，然后进行一些计算和判断，最终输出结果。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    //    初始化\n    int price = 0;\n    int bill = 0;\n    //    读入金额和票面\n    printf(\"请输入金额：\");\n    scanf(\"%d\", &price);\n    printf(\"请输入票面：\");\n    scanf(\"%d\", &bill);\n    //    判断票面够不够并计算找零\n    if (bill >= price)\n        printf(\"应该找您：%d\\n\", bill - price);\n    else\n        printf(\"你的钱不够\\n\");\n\n    return 0;\n}\n```\n\n* 上述程序我们使用了**注释 (comment)**，把程序分成了清晰的三部分。注释是插入在程序代码内部，用来向读者提供解释信息，它们对程序功能没有任何影响，编译程序如何处理注释的请看课后讨论 3.1.2。\n\n* 延续多行的注释，使用`/*... ...*/`来写，注释内容由`/*`开始到`*/`结束，也可以用于一行内的注释。\n\n* 注意，`//`单行注释是 C99 的注释，ANSI C 不支持，而多行注释是一直都有的形式。\n\n为了加深对条件判断的理解，这里提供第二个例子，可以画流程图辅助理解。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    const int MINOR = 35;\n    int age = 0;\n\n    printf(\"请输入你的年龄: \");\n    scanf(\"%d\", &age);\n\n    printf(\"你的年龄是 %d 岁。\\n\", age);  //人机交互常用方案：先输出刚才读取的内容，方便用户验证和阅览\n\n    if (age < MINOR)\n        printf(\"年轻是美好的，\");\n\n    printf(\"年龄决定了你的精神世界，好好珍惜吧。\\n\");\n\n    return 0;\n}\n```\n\n年龄判断流程图\n\n**找零计算器**\n\n1.找零计算器需要用户做两个操作：输入购买的金额，输入支付吧票面，而找零计算器则根据用户的输入做出相应的动作：计算并打印找零，或告知用户余额不足以购买。\n\n2.从计算机程序的角度看，这就是意味着程序需要读用户的两个输入，然后进行一些计算和判断，最后输出结果\n\n```c\n//初始化\nint price = 0;\nint bill = 0;\n//读入金额和票面\nprintf(\"请输入金额：\");\nscanf(\"%d\",&price);\nprintf(\"请输入票面：\");\nscanf(\"%d\",&bill);\n//计算找零\nprintf(\"应该找您：%d\\n\",bill - price);\n```\n\n**双斜杠//注释**\n\n双斜杠//是注释(comment)的意思：插入在程序代码中，用来向读者提供解释信息。它们对于程序的功能没有影响，但是往往能使得程序更容易被人类读者理解\n\n**/* */注释**\n\n1.延续多行数行的注释，要用多行注释的格式来写。多行注释由一对字符序列“/*”开始，而以“*/”结束\n\n2.也可以用于一行内的注释\n\n3.int ak = 47/*36*/，y = 9;\n\n**当票面不够的情况**\n\n1.当票面不够的情况怎么办？这个时候就需要进行判断用户读入的金额是否超过了票面\n\n2.对计算找零这个步骤进行优化:\n\n```c\nif( bill >= price){\n   printf(\"需要找您：%d\\n元\",bill - price);\n}\n```\n\n这个是当票面够的情况才会显示出需要找多少钱，如果钱不够的话则就不会显示出需要找钱了\n\n3.如果当钱不够的情况需要提示用户\"你的钱不够\"怎么做?\n\n错误示范:\n\n```c\nif( bill >= price){\n   printf(\"需要找您：%d/n元\",bill - price);\n}\nprintf(\"你的钱不够\\n\");\n```\n\n这种情况不管钱够不够都会输出 你的钱不够"},{"page":27,"body":"**本节完整补充**\n\n**else{}语句-条件不成立应该做什么？**\n\n* 我们前面知道，if 语句满足条件判断，就会执行内部的复合语句，不满足则会执行 if 语句的下一条语句。可是，如果钱不够，条件不成立，应当做什么呢？简单的在 if 语句后面添加一条`printf(\"你的钱不够\\n\");`，会导致无论够不够，都会输出这句话，在钱足够的时候，该程序就会出现 BUG。\n\n* C 语言提供了一种方法，可以在 if 条件不满足时，执行另外的动作，这就是 else()语句，else 表示否则。\n\n找零计算器流程图\n\n* else 和 if 一样，后面可以不加大括号，但是 else 只对紧随其后的一句语句有效。\n\n利用条件分支结构，可以很容易实现一个简单的功能——比较两数大小。\n\n```c\nint max1(int a, int b)\n{\n    int max = b;\n    if (a > b)\n        max = a;\n\n    return max;\n}\n\nint max2(int a, int b)\n{\n    int max = 0;\n    if (a > b)\n        max = a;\n    else\n        max = b;\n\n    return max;\n}\n```\n\n* max1()似乎更聪明，更简洁，但 max2()足够的傻，傻到任何人都能看懂，从某些评价角度看，也许第二种——让更多人能读懂的代码，要更好一些。\n\n【典例一】应付薪水\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    const double RATE = 8.25;\n    const int STANDARD = 40;\n    double pay = 0.0;\n    int hours;\n\n    printf(\"请输入工作的小时数: \");\n    scanf(\"%d\", &hours);\n    printf(\"\\n\");\n    if (hours > STANDARD)\n        pay = STANDARD * RATE + (hours-STANDARD) * (RATE * 1.5);\n    else\n        pay = hours * RATE;\n    printf(\"应付工资: %f\\n\", pay);\n\n    return 0;\n}\n```\n\n【典例二】成绩是否通过\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    const int PASS=60;\n    int score;\n\n    printf(\"请输入成绩: \");\n    scanf(\"%d\", &score);\n\n    printf(\"你输入的成绩是 %d.\\n\", score);\n    if (score < PASS)\n        printf(\"很遗憾，这个成绩没有及格。\");\n    else {\n        printf(\"祝贺你，这个成绩及格了。\");\n        printf(\"再见\\n\");\n    }\n\n    return 0;\n}\n```\n\n【多选题】以下程序段____的功能是：输入一批整数，用负数作为输入的结束标志，统计其中大于 85 的数据个数。\n\n```c\n//A\nint count = 0, score;\nwhile (score >= 0)\n{\n    scanf (\"%d\", &score);\n    if (score > 85)\n        count++;\n}\nprintf(\"%d\\n\", count);\n//B\nint count = 0, score;\nscanf (\"%d\", &score);\nwhile (score >= 0) {\n    scanf (\"%d\", &score);\n    if (score > 85)\n        count++;\n}\nprintf(\"%d\\n\", count);\n//C\nint count = 0, score;\nwhile (1) {\n    scanf (\"%d\", &score);\n    if (score < 0) break;\n    if (score > 85)\n        count++;\n}\nprintf(\"%d\\n\", count);\n//D\nint count = 0, score;\nscanf (\"%d\", &score);\nwhile (score >= 0) {\n    if (score > 85)\n        count++;\n    scanf (\"%d\", &score);\n}\nprintf(\"%d\\n\", count);\n```\n\n解析：第一个选项进循环前没有对 score 赋值，有可能出问题。第二个选项无法只读入一个合法成绩，而且读入两个或更多合法成绩时第一个成绩会被覆盖，不会被统计，由于是多选题，所以选 CD。\n\n* **课后讨论 3.1.1：为什么强调 if 和 else 后面要用{ }**\n\n讨论：\n\n* 人类可以采用多层缩进的书写格式，使程序层次分明，但 C 语言编译器会忽略缩进符。使用{...}能够明确地标识出这些语句属于 if 和 else 语句的范围，避免了可能的歧义，增加了代码的可读性和可维护性。 如果没有使用花括号，if 和 else 语句只能控制其后的单个语句。 如果在单个语句后紧接着编写另一条语句，那么这条语句将始终会被执行，而不管 if 或 else 语句的条件是否为真或假。 因此，强调在 if 和 else 语句后使用花括号是为了确保代码的正确性和可读性。\n\n* **课后讨论 3.2.2：/* */会被替换为什么？**\n\n这个讨论是深入的讨论，第一次学习 C 语言的同学可以围观一下，看不懂也没关系。\n\n/* */是注释，编译器不管其中的内容。但是有没有想过编译器会把它替换成什么呢？是一个空格？还是不留痕迹？可以写代码来测试你用的编译器吗？\n\n* 把代码翻译成汇编代码前，有个预处理的步骤，在处理过程中会把注释代码删掉。编译器替换内容，需要看注释在语句中的位置，位置不同，替换内容也不同，语句前及语句中的注释会被替换成空格，语句前注释被替换成等长空格，句中注释被替换成一个空格，语句末的注释 是直接删除。\n\n**else**\n\nelse = 否则的话\n\n正确的做法是：\n\n```c\nif( bill >= price){\n   printf(\"需要找您：%d/n元\"，bill - price)；\n}else{\n   printf(\"你的钱不够\\n\");\n}\n```\n\n**比较数的大小**\n\n**比较数的大小的各种方法**\n\n```c\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = 0;\nif(a>b){\n    max = a;\n}\n\nprintf(\"大的那个是%d\\n\", max);\n```\n\n这个代码里面没有解决b>a的问题，当a>b的条件不成立时，程序就结束了，max没有得到值\n\n方案有很多，列举3个\n\n```c\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = 0;\nif(a>b){\n    max = a;\n}\nif(b>a){\n    max = b;\n}\nprintf(\"大的那个是%d\\n\", max);\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = 0;\nif(a>b){\n    max = a;\n}else{\n    max = b;\n}\n\nprintf(\"大的那个是%d\\n\", max);\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = b;\nif(a>b){\n    max = a;\n}\n\nprintf(\"大的那个是%d\\n\", max);\n```"},{"page":28,"body":"**本节完整补充**\n\n**比较数的大小**\n\n**比较数的大小的各种方法**\n\n```c\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = 0;\nif(a>b){\n    max = a;\n}\n\nprintf(\"大的那个是%d\\n\", max);\n```\n\n这个代码里面没有解决b>a的问题，当a>b的条件不成立时，程序就结束了，max没有得到值\n\n方案有很多，列举3个\n\n```c\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = 0;\nif(a>b){\n    max = a;\n}\nif(b>a){\n    max = b;\n}\nprintf(\"大的那个是%d\\n\", max);\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = 0;\nif(a>b){\n    max = a;\n}else{\n    max = b;\n}\n\nprintf(\"大的那个是%d\\n\", max);\nint a,b;\nprintf(\"请输入两个整数：\");\nscanf(\"%d %d\", &a, &b);\n\nint max = b;\nif(a>b){\n    max = a;\n}\n\nprintf(\"大的那个是%d\\n\", max);\n```\n\n**if语句**\n\n在上面中我们已经充分了解到了if语句的用法跟含义\n\n但其实if也可以不带中括号去执行(不建议)，只能执行if接下来的一句内容\n\n例如：\n\n```c\nconst double RATE = 8.25;\nconst double STANDARD = 40;\ndouble pay = 0.0;\nint hours;\n\nprintf(\"请输入工作的小时数：\");\nscanf(\"%d\",&hours);\nprintf(\"\\n\");\nif(hours > STANDARD)\n    pay = STANDARD * RATE + (hours - STANDARD) * (RATE * 1.5);\nelse\n    pay = hours * RATE;\nprintf(\"应付工资:%f\\n\",pay);\n#include <stdio.h>\n\nint main()\n{\n\n    const int MINOR = 35;\n\n    int age = 0;\n\n    printf(\"请输入你的年龄: \");\n    scanf(\"%d\", &age);\n\n    printf(\"你的年龄是%d岁。\\n\", age);\n\n    if (age < MINOR) {\n        printf(\"年轻是美好的，\");\n    }\n\n    printf(\"年龄决定了你的精神世界，好好珍惜吧。\\n\");\n\n    return 0;\n}\n```"},{"page":29,"body":"**本节完整补充**\n\n**嵌套的 if-else 语句**\n\n* 当 if 或 else 条件满足或者不满足的时候执行的语句也可以是一条 if 或者 else 语句，这就是嵌套的 if-else 语句。\n\n* 下面的代码就实现了 4 路分支，必要时第三层语句还可以是基本的 if else 语句，从而实现更多路的分支。\n\n```c\nint a, b, c;\nscanf(\"%d %d %d\", &a, &b, &c);\n\nint max = 0;\n//虽然下面采用省略{}的 if-else 语句，但要注意，这样做编译器、其他阅读者有误解这些 if-else 对应匹配的可能。\n//缩进字符只能让人类更便于阅读和理解，并不能在 C 语言中表明 if-else 的匹配\nif (a > b)\n    if (a > c)\n        max = a;\n    else\n        max = c;\nelse\n    if (b > c)\n        max = b;\n    else\n        max = c;\n\nprintf(\"Tha max number is %d\", max);\n```\n\n比较三个数的大小 流程图\n\n* else 总是和之前与其最近的且不带 else 的那个 if 匹配，通过加大括号，可以改变 if-else 对应的匹配，比如将上面的程序修改为下面这段程序，此时第二个 if 就没有对应的 else 和它匹配了。\n\n```c\nif (a > b)\n{\n    if (a > c)\n    max = a;\n}\n    else\n        max = c;\n```\n\n* 代码中使用缩进字符只能让人类更便于阅读和理解，并不能在 C 语言中表明 if-else 对应的匹配关系。有时候，虽然某个 else 和 if 书写格式对齐，但由于它与第二个 if 距离最近，于是会与第二个 if 匹配。\n\n* 我们可以保持一个好的代码习惯，使用 C 语言时，在 if 和 else 后面，即使只有一条语句也始终加上大括号{... ...}，这样可以避免很多不必要的麻烦。\n\n* 如果不使用{ }，则需要注意对应关系，且不要随意省略 else 部分，即使其为空语句，这样 if 和 else 数量相同，从内层到外层一一对应，结构清晰，不易出错。\n\n**3.switch-case 多路分支**\n\n**3.2分支**\n\n**嵌套的判断**\n\n当if的条件满足或者不满足的时候要执行的语句也可以是一条if或者if-else语句，这就是嵌套的if语句\n\n**else的匹配**\n\nelse总是和最近的那个if匹配\n\n**缩进**\n\n缩进格式不能暗示else的匹配\n\n```c\nif( code ==READY )\n    if( count < 20 )\n        printf(\"一切正常\\n\");\nelse\n        printf(\"继续等待\\n\");\n```\n\n**tips**\n\n1.在if或者else后面总是用{}\n\n2.即使只有一条语句的时候"},{"page":30,"body":"**本节完整补充**\n\n**级联的 else if 语句**\n\nelse if 语句是最常用的实现多分支（多路选择）的方法，其一般形式为：\n\n```c\nif (表达式 1)\n    语句 1;\nelse if (表达式 2)\n    语句 2;\n...\nelse if (表达式 n-1)\n    语句 n-1;\nelse\n    语句 n;\n```\n\n这里把它称为级联的 else if 语句，执行流程图如下所示。\n\nelse if 流程图\n\n* else if 语句在仍然保持逻辑清晰的前提下，简化了代码的写法。当然，我们可以把任何一个级联的 else if 分支结构改成只用 if 和 else 语句的分支结构，不过这样就需要利用缩进不断向右缩进，层数很多时代码会变得不简洁、不美观。\n\n* 利用 else if 语句，可以方便地描述下面的分段函数，并打印不同自变量下 f 的值。\n\ny=f(x) = \\begin{cases}\n\n-1 & x < 0 \\\\\n\n0 & x= 0 \\\\\n\n2x & x>0\n\n```c\nint f;\nif (x < 0)\n    f = -1;\nelse if (x == 0)\n    f = 0;\nelse\n    f = 2 * x;\nprintf(\"%d\", f);\n//另外的实现方式\nif (x < 0)\n    printf(\"%d\", -1);\nelse if (x == 0)\n    printf(\"%d\", 0);\nelse\n    printf(\"%d\", 2 * x);\n```\n\n* 上述两段代码各自都能实现相同的功能，但是从程序设计角度看，更倾向于选择第一种写法。因为它属于**单一出口**(出口是指程序结束的地方，第二段代码有三处地方直接用 printf() 把结果返回给用户看了，第一段代码只有最后一处地方向用户打印值)。\n\n* 第一种写法最后才打印 f 的值，之前不涉及对 f 的操作，如果之后要额外对 f 进行其他计算或者通过函数返回到高层都很容易方便、灵活的添加；\n\n* 而第二种写法，固定了这段代码就只能把分段函数不同自变量下的函数值返回给用户看，不够灵活。如果一个很复杂的工程文件，有个复杂的模块中间有好几个出口，也会给软件测试增大难度。\n\n级联 `else if` 是用来描述“多个互斥条件”的结构：从上到下检查，找到第一个成立的分支就执行，执行后跳到整个结构之后。它比一层层嵌套 `if` 更平，也比多个独立 `if` 更能表达互斥关系。\n\n分段函数问题可以这样建模：\n\n```c\nint f;\nif (x < 0) {\n    f = -1;\n} else if (x == 0) {\n    f = 0;\n} else {\n    f = 2 * x;\n}\nprintf(\"%d\\n\", f);\n```\n\n第一种写法先把结果放进 `f`，最后统一输出；第二种写法在每个分支里直接 `printf`。从功能上看两者都可以，但从可维护性看，单一出口的版本更容易继续计算、返回函数或让测试观察结果。程序越长，越要养成“先计算，后输出”的习惯。\n\n画流程图时注意：条件从上到下依次判断，只有前一个条件为假，才会进入下一个分支；`else` 必须和最近的 `if` 配对，嵌套时更要依靠大括号明确归属。\n\n练习时可以做一个小表格：什么输入触发第一个分支、什么输入触发第二个分支、什么输入落到 `else`。再用端点 `x 0` 分别手工验证，避免只测一个常见值。"},{"page":31,"body":"**本节完整补充**\n\n**switch-case 语句：多分支选择**\n\nswitch 语句可以处理多分支选择问题，根据其中 break 语句的不同用法，可以实现不同的功能。\n\n```c\nswitch (控制表达式)\n{\n    case 常量表达式 1:\n        语句段 1\n        ...\n    case 常量表达式 2:\n        语句段 2\n        ...\n    default:\n        语句段 3\n        ...\n}\n```\n\n* 控制表达式只能是整数型的结果\n\n* 常量表达式包括整型或字符型，也可以是常量的运算表达式，所有的常量表达式的**值不能相等**。相等的话编译器会报错，[Error] duplicate case value 和 [Error] previously used here。\n\n* case 后面**不能**是**关系表达式**，也不能是**变量**或**变量表达式**，比如\n\n```c\ncase op == '+': printf(\"%d\\n\", value1 + value2); break;\n//\ncase n % 3: printf(\"one\");\n```\n\n* 语句段可以有若干条语句，也可以为空语句\n\n* default 可以省略，省略之后，若控制表达式不满足任何一个常量表达式的值，则什么都不做。\n\n* case 也可以没有，没有 case 就直接执行 default 后面的语句。\n\n**break 语句**\n\n* switch 语句可以看作是一种基于计算的跳转，由下面虚线框可以看到，switch 语句中的“case 常量表达式”和“default”的作用相当于语句标号（分支标号）。计算控制表达式的值后，程序会跳转到相匹配的 case（分支标号）处。\n\n* 分支标号只是说明 switch 内部位置的路标，在执行完分支中的最后一条语句后，如果后面没有 break，就会 顺序执行到下面的 case 里去，直到遇到一个 break ，或者 switch 结束为止。\n\n* 由此可见，在 switch 语句的所有语句段的末尾使用 break，可以简单、清晰地实现多分支选择，这也是 switch 语句的主要使用方法。\n\nswich 语句流程图\n\nbreak 的用法：\n\n1. **只能**在循环体内或 switch 语句体内使用 break 语句。\n\n2. 当 break 出现在循环体中的 switch 语句体内时，其作用只是跳出该 switch 语句体。\n\n3. break 用于循环体中，用于提前中断**本层**循环(区别 continue 只是跳过本轮循环 continue 后面的语句，进入一下轮，而且 continue 只能用于循环体中)\n\n4. break 和 continue 都只能对它所在的那层循环生效，continue 一般也要与 if 语句配合使用。\n\n5. break 执行之后，不会执行步进(i++)而是直接跳出整个循环，但是 continue 执行之后，仍会执行步进。\n\nfor 结构中 break 和 continue 的区别如下图，左一是宏观描绘，右边两幅图是微观描绘，虚线框为 for 的循环体。\n\n图-for 结构中 break 和 continue 的区别\n\n也可以选择仅仅在某些语句段末尾使用 break 以实现特殊功能，比如下述代码可以识别读入字符的是不是回车或者空格字符二者其中之一。\n\n```c\nchar ch = getchar();\nint blank = 0;\nswitch (ch) {\n    case ' ':\n    case '\\n':\n        blank++;\n        break;\n}\n```\n\n下面是一个将一个百分制成绩转换为五分制成绩的程序。类似地，也可以编写一个输入月份转换为对应英文单词的程序。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int grade;\n    scanf(\"%d\", &grade);\n    grade /= 10;\n\n    switch (grade) {\n        case 10:\n        case 9:\n            printf(\"A\\n\");\n            break;\n         case 8:\n            printf(\"B\\n\");\n            break;\n         case 7:\n            printf(\"C\\n\");\n            break;\n         case 6:\n            printf(\"D\\n\");\n            break;\n         default:\n            printf(\"E\\n\");\n            break;\n    }\n\n    return 0;\n}\n```\n\n* 这段代码不符合“单一出口”的原则，学习过字符或者字符串数据的处理章节之后可以回过头来把这个程序改写成单一出口的程序。月份转换的程序也可以在学习过数组章节再回过头改写得更加简洁和漂亮。\n\n* **课后讨论 4.3.1：分段函数可以用 switch-case 吗？划算吗？**\n\n分段函数可以用 switch-case 吗？划算吗？视频中最后的那个分段函数，真的可以用 switch-case 吗？\n\n讨论：\n\n* 可以使用，但是还是少不了判断，使用了相当于多了很多步骤，不划算；而且分段函数的自变量 x 是范围，如果硬要用 switch-case 语句实现，需要先做一次判断转化为整型。\n\n```c\nint main()\n{\n    int x, n;\n\n    scanf(\"%d\", &x);\n\n    if (x < 0)\n        n = 1;\n    else if (x == 0)\n        n = 2;\n    else\n        n = 3;\n\n    switch (n) {\n        case 1: printf(\"f = -1\\n\"); break;\n        case 2: printf(\"f = 0\\n\"); break;\n        case 3: printf(\"f = %d\\n\", 2 * x); break;\n    }\n\n    return 0;\n}\n```\n\n**switch-case**\n\n1.控制表达式只能是整数型的结果\n\n2.常量可以是常数，也可以是常数计算的表达式\n\n3.根据表达式的结果，寻找匹配的case,并执行case后面的语句，一直到break为止\n\n4.如果所有的case都不匹配，那么就执行default后面的语句；如果没有default，那么就什么都不做\n\n```c\nswitch(控制表达式){\ncase常量:\n        语句\n        ...\ncase常量：\n        语句\n        ...\ndefault：\n        语句\n        ...\n}\n```\n\n**break**\n\nswitch语句可以看作是一种基于计算的跳转，计算控制表达式的值后，程序会跳转到相匹配的case(分支标号)处。分支标号只是说明switch内部位置的路标，在执行完分支中的最后一条语句后，如果后面没有break，就会顺序执行到下面的case里去，直到遇到一个break，或者switch结束为止"}];
