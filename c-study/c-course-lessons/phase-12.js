window.COURSE_CHUNKS=window.COURSE_CHUNKS||{};window.COURSE_CHUNKS["工程"]=[{"page":106,"body":"**本节完整补充**\n\n**1.全局变量**\n\n* 定义在函数外面的变量是全局变量\n\n* 全局变量具有全局的**生存期**和**作用域**：\n\n1. 它们与任何函数都无关\n\n2. 在任何函数内部都可以使用它们\n\n```c\n#include <stdio.h>\nint f(void);\nint gAll = 20;\n\nint main(int argc, char const *argv[])\n{\n    printf(\"in %s gAll = %d\\n\", __func__, gAll);\n    f();\n    printf(\"again %s gAll = %d\\n\", __func__, gAll);\n    return 0;\n}\n\nint f(void)\n{\n    printf(\"in %s gAll = %d\\n\", __func__, gAll);\n    gAll += 5;\n    printf(\"again %s gAll = %d\\n\", __func__, gAll);\n    return gAll;\n}\n```\n\n```c\nin main gAll = 20\nin f gAll = 20\nagain f gAll = 25\nagain main gAll = 25\n```\n\n注：`__func__`可以让编译器自动帮我们填充函数名。\n\n* 如果函数内部存在与全局变量同名的变量，则**全局变量被隐藏**。（可以无限套娃定义，更小作用域里的同名变量会把外面的同名变量隐藏掉）\n\n* 函数可以**封装**问题解决的实现，使问题解决过程局部化，避免功能间的干扰。函数设计时应注意如下问题：\n\n* ①限制函数长度：一个函数语句数不宜过多，便于阅读、理解、调试。若太长，可考虑进一步分解实现。\n\n* ②避免函数功能间的重复：对于多处使用的同一个计算或操作过程，应当将其封装为一个独立函数，以达到一处定义，多处使用的目的，避免功能模块间的重复。\n\n* ③减少全局变量的使用：应采用定义局部变量作为函数的临时工作单元，使用参数和返回值作为函数与外部进行数据交换的方式。只有当确实需要多个函数共享的数据时，才定义其为全局变量。\n\n* 以函数作为基本功能实现载体，以函数调用结构图为函数关联，可有效组织多个函数，实现复杂问题的结构化程序设计。\n\n**全局变量初始化**\n\n* 没有做初始化的全局变量会得到 **0** 值。（本地变量不会做这个自动初始化的事情）\n\n* 没有做初始化的全局指针变量会得到 **NULL**。\n\n* 只能用**编译时刻已知的值**来初始化全局变量。\n\n* 它们的初始化发生在 main() 函数之前。\n\n```c\n//甚至下面这种写法都是不可以通过编译的，虽然人类可以容易看出 g2 = 20\nint gAll = 20;\nint g2 = gAll\n//加上 const 之后可以过编译，但不建议将两个全局变量互相联系起来初始化，语句顺序互换可能就编译不通过了。\nconst int gAll = 20;\nint g2 = gAll\n```\n\n**12.1-1全局变量：定义在函数之外的变量，全局的生存期和作用域**\n\n**全局变量**\n\n1. 定义在函数外面的变量是全局变量\n\n2. 全局变量具有全局的生存期和作用域\n\n1. 1. 他们和任何函数无关\n\n2. 在任何函数内部都可以使用他们\n\n```c\n#include<stdio.h>\n\nint f(void);\n\nint gAll = 12;//全局变量\n\nint main(int argc,char const *argv[])\n{\n    printf(\"in %s gAll=%d\\n\",__func__,gAll);\n    f();\n    printf(\"agn in %s gAll = %d\\n\",__func__,gAll);\n    return 0;\n}\n//_func_是一个字符串，表达的是当前函数的名字，也就是main的名字\nint f(void)\n{\n    printf(\"in %s gAll=%d\\n\",__func__,gAll);\n    gAll += 2;\n    printf(\"agn in %s gAll=%d\\n\",__func__,gAll);\n    return gAll;\n}\n```\n\n1. 没有做初始化的全局变量会得到0值\n\n1. 1. 指针会得到NULL值\n\n1. 只能用编译时刻已知的值来初始化全局变量\n\n2. 它们的初始化发生在main函数之前\n\n**被隐藏的全局变量**\n\n1. 如果函数内部存在与全局变量同名的变量，则全局变量被隐藏(局部的优先度会更高噢，会覆盖掉外面的变量)"},{"page":107,"body":"**本节完整补充**\n\n**静态本地变量**\n\n* 在本地变量定义时加上`static`修饰符就成为**静态本地变量**\n\n* 当函数离开的时候，静态本地变量会继续存在并保持其值\n\n* 静态本地变量的初始化只会在第一次进入这个函数时做，以后进入函数时会保持上次离开时的值。\n\n```c\n#include <stdio.h>\n\nvoid f(void);\n\nint main(int argc, char const *argv[])\n{\n    f();\n    f();\n    f();\n    return 0;\n}\n\nvoid f(void)\n{\n    static int stall = 20;\n    int all = 20;\n    printf(\"in %s all=%d, stall=%d\\n\", __func__, all, stall);\n    all++;\n    stall++;\n}\n```\n\n```c\nin f all=20, stall=20\nin f all=20, stall=21\nin f all=20, stall=22\n```\n\n* 静态本地变量实际上是特殊的全局变量\n\n* 它们位于相同的内存区域（下面这个程序样例，静态本地变量和全局变量完全是紧挨着存放的）\n\n* 静态本地变量**具有全局的生存期**，函数内的**局部作用域**（生存期和作用域不一致）\n\n* `static`在这里的意思是局部作用域（本地可访问）\n\n```c\n#include <stdio.h>\nint f(void);\nint gAll = 25;\n\nint main(int argc, char const *argv[])\n{\n    f();\n    return 0;\n}\n\nint f(void)\n{\n    int k = 0;\n    static int all = 1;\n    printf(\"&gAll = %p\\n\", &gAll);\n    printf(\"&all  = %p\\n\", &all);\n    printf(\"&k    = %p\\n\", &k);\n\n    return 0;\n}\n```\n\n```output\n&gAll = 0000000000403010\n&all  = 0000000000403014\n&k    = 000000000062FDEC\n```\n\n**12.1-2静态本地变量：能在函数结束后继续保有原值的本地变量**\n\n1. 在本地变量定义时加上static修饰符就成为静态本地变量\n\n2. 当函数离开的时候，静态本地变量会继续存在并保持其值\n\n3. 静态本地变量的初始化只会在第一次进入这个函数时做，以后进入函数时会保持上次离开时的值\n\n```c\n#include<stdio.h>\n\nint f(void);\n\nint gAll = 12;//全局变量\n\nint main(int argc,char const *argv[])\n{\n    f();\n    f();\n    f();\n    return 0;\n}\n//_func_是一个字符串，表达的是当前函数的名字，也就是main的名字\nint f(void)\n{\n    static int all = 1;\n    printf(\"in %s gAll=%d\\n\",_func_,gAll);\n    gAll += 2;\n    printf(\"agn in %s gAll=%d\\n\",_func_,gAll);\n    return gAll;\n}\n```\n\n1. 静态本地变量实际上是特殊的全局变量\n\n2. 它们(静态本地变量和全局变量)位于相同的内存区域\n\n3. 静态本地变量具有全局的生存期，函数内的局部作用域\n\n4. static 在这里的意思是局部作用域(本地可访问)"},{"page":108,"body":"**本节完整补充**\n\n**返回指针的函数**\n\n* 返回本地变量的地址是危险的\n\n* 返回全局变量或者静态本地变量的地址是安全的\n\n* 返回在函数内 malloc() 的内存是安全的，但是容易造成问题\n\n* 最好的做法是返回**传入的指针**\n\n```c\n//返回本地变量的地址是危险的\n#include <stdio.h>\n\nint* f(void);\nvoid g(void);\n\nint main(int argc, char const *argv[])\n{\n    int *p = f();\n    printf(\"*p=%d\\n\", *p);\n    g();\n    printf(\"*p=%d\\n\", *p);\n\n    return 0;\n}\n\nint* f(void)\n{\n    int i = 12;\n    return &i;\n}\n\nvoid g(void)\n{\n    int k = 24;\n    printf(\"k = %d\\n\", k);\n}\n```\n\n```output\n*p=12\nk = 24\n*p=24\n```\n\n编译器会悉心提示你不要将本地变量的地址返回出来\n\n```c\n[Warning] address of local variable 'i' returned [-Wreturn-local-addr]\n```\n\n对于上面程序运行结果的解读：\n\n* Q：不是说出函数后本地变量就不存在了吗，为什么还能得到`*p = 12.`\n\n* A：不存在指的是变量出了函数就**不受控制**了，不能保证 12 一定在那个位置，而且还保持原来的值，但那个位置的值有可能还是在的，也有可能还是 12。\n\n* 从后面输出`*p = 24`可以发现，没有人对 *p 和 i 做任何修改，最终值却改变了。可以验证，这两个函数返回的 i 和 k 的地址是一样的，也就是 f() 函数使用完的变量地址，又被分配给 g() 函数使用了，所以这样是有风险的。\n\n关于全局变量使用的**小贴士**：\n\n* 不要使用全局变量来在函数间传递参数和结果\n\n* 尽量避免使用全局变量——丰田汽车的案子。\n\n* 使用全局变量和静态本地变量（对于**多线程**的环境）的线程是不安全的：如果你的函数大量使用全局变量或者静态本地变量，这个函数是**不可重入**的。\n\n* **课后讨论 12.1.1：本地变量的秘密**\n\n上面程序段中，f 和 g 两个函数之间存在什么关系？为什么 f 里的 i 和 g 里的 k 的地址是一样的？\n\n讨论：\n\n* **并列关系**，因为先调用 f 函数结束后，内存释放，这段内存被接下来的 g 函数接着使用。\n\n**12.1-3后记：返回指针的函数，使用全局变量的贴士**\n\n***返回指针的函数**\n\n1. 返回本地变量的地址是危险的\n\n2. 返回全局变量或静态本地变量的地址是安全的\n\n3. 返回在函数内malloc的内存是安全的，但是容易造成问题\n\n4. 最好的做法是返回传入的指针\n\n**tips(贴士)**\n\n1. 不要使用全局变量来在函数间传递参数和结果\n\n2. 尽量避免使用全局变量\n\n1. 1. 丰田汽车的案子(ks) 这里给出传送门(想知道的可以看看)：https://www.sohu.com/a/133455549_464086\n\n1. *使用全局变量和静态本地变量的函数是线程不安全的"},{"page":109,"body":"**本节完整补充**\n\n**2.编译预处理和宏**\n\n**宏定义**\n\n* 宏定义 #define 是 C 语言中常用的功能。用宏来定义一些符号常量，可以方便程序的编制。\n\n* 宏定义的格式：\n\n```c\n#define 宏名 宏定义字符串\n```\n\n**编译预处理指令**\n\n* 编译预处理是 C 语言编译程序的组成部分，它用于解释处理 C 语言源程序中的各种预处理指令。\n\n* 编译预处理**功能**主要包括：(1)**文件包含(#include)** (2)**宏定义(#define)** (3)**条件编译**\n\n* `#`开头是编译预处理指令（第一个 hello world 程序其实就见过）\n\n* 它们不是 C 语言的成分，不是真正的 C 语句，但是 C 语言程序离不开它们（它们增强了 C 语言的编程功能，改进 C 语言设计环境，提高编程效率）\n\n`#define`用来定义一个宏\n\n```c\n#include <stdio.h>\n\n//ANSI C(C89)标准要用 define 定义宏,PI 是宏的符号,3.14159 是宏的值\n#define PI 3.14359\n//C99 可以使用 const\nconst double PI = 3.14159;\n\nint main(int argc, char const *argv[])\n{\n    printf(\"%f\", 2*PI*3.0);\n    //printf(\"%f\", 2*3.14159*3.0);\n    return 0;\n}\n```\n\n**#define**\n\n```c\n#define <名字> <值>\n```\n\n* 注意没有结尾的分号，因为不是 C 的语句\n\n* 名字必须是一个单词，值可以是各种东西\n\n* 在 C 语言的编译器开始**编译之前**，编译预处理程序（cpp）会把程序中的名字替换为值\n\n* 这种替换是非常原始的**完全的文本替换**，直接把文本(包括空格、其他符号等等)填到原来的位置。（也称宏替换）\n\n* `gcc --save-temps`可以保存编译过程中生成的中间文件（）\n\n(1).预处理（Pre-process）：把宏替换，删除注释，展开头文件，产生 .i 文件\n\n(2).编译（Compliling）：把之前的 .i 文件转换成汇编语言，产生 .s 文件\n\n(3).汇编（Asembly）：把汇编语言文件转换为机器码文件，产生 .o 文件\n\n(4).链接（Link）：对.o 文件中的对于其他的库的引用的地方进行引用，生成最后的可执行文件（同时也包括多个 .o 文件进行 link）\n\n（注意，编译过程分为：**预处理、编译、汇编、链接**，这四个阶段总用时称为**编译时间**，所以预处理发生在编译之前，但是仍然占用编译时间）\n\n**宏**\n\n* 如果一个宏的值中有其他宏的名字，也是会被替换的\n\n* 如果一个宏的值超过一行，最后一行之前的行末需要加``\n\n* 宏的值后面出现的注释不会被当做宏的值的一部分\n\n**没有值的宏**\n\n```c\n#define _DEBUG\n```\n\n* 这类宏是用于**条件编译**的，后面有其他的编译预处理指令来检查这个宏是否已经被定义过了。\n\n**预定义的宏**\n\n* 这些宏一般是用来表达一些特殊的东西，可以让编译器替你插入一些特殊的值。\n\n```c\n__LINE__   /*表示源代码文件当前所在的行号*/\n__FILE__   /*表示源代码文件的文件名*/\n__DATE__   /*编译时候的日期*/\n__TIME__   /*编译时候的时间*/\n__STDC__   /*用于标识编译器是否符合 C 语言标准*/\n```\n\n**12.2-1宏定义**\n\n1. #开头的是编译预处理指令\n\n2. 它们不是c语言的成分，但是C语言程序离不开它们\n\n3. #define用来定义一个宏 ，其实只是一个原始的文本替换\n\n1. #define\n\n2. 注意没有结尾的分号，因为不是c的语句\n\n3. 名字必须是一个单词，值可以是各种东西\n\n4. 在C语言的编译器开始编译之前，编译预处理程序(cpp)会把程序中的名字换成值\n\n1. 1. 完全的文本替换\n\n1. gcc——save-temps\n\n1. 如果一个宏的值中有其他宏的名字，也是会被替换的\n\n2. 如果一个宏的值超过来一行，最后一行之前的行末需要加\\\n\n3. 宏的值后面出现的注释不会被当作宏的值的一部分\n\n1. #define_DEBUG\n\n2. 这里宏是用于条件编译的，后面有其他的编译预处理指令来检查这个宏是否已经被定义过了\n\n1. _LINE_表达代码所在的行号\n\n2. _FILE_表达代码所在的文件名\n\n3. _DATE_编译时候的日期\n\n4. _TIME_编译时候的时间\n\n5. _STDC_"},{"page":110,"body":"**本节完整补充**\n\n**带参数的宏**\n\n**1.像函数的宏**\n\n```c\n#define cube(x) ((x)*(x)*(x))\n```\n\n* 宏可以带参数，这里的 cube(x) 就很像函数，只不过没有指定类型，也**不需要定义类型**。\n\n* 宏引用形式与函数调用非常相似，但二者的实现过程完全不同。\n\n**2.定义带参数的宏的原则——一切都要括号**\n\n* 整个值要括号\n\n* **参数出现的每个地方**都要括号\n\n```c\n//下面这个宏定义，如果去掉任意一组括号都可能会产生问题\n#define RADTODEG(x) ((x) * 57.29578)\n```\n\n* 可以带多个参数\n\n```c\n//非常常见的三个数找最值的宏\n#define MIN(a,b) ((a)>(b)?(b):(a))\n```\n\n* 也可以组合（嵌套）使用其他宏\n\n```c\n#define PI 3.1415926\n#define S PI * r * r\n```\n\n* 一定不要加分号（因为#号开头这些预处理指令并不属于 C 语言，其他语言也有这些用法，比如汇编语言）\n\n```c\n//宏定义时不小心加分号，写程序时语句结尾又加分号就会导致错误\n//下面程序中 if 和 else 被两个分号分隔开了\n#define PRETTY_PRINT(msg) printf(msg);\n...\nif (n<10)\n    PRETTY_PRINT(\"n is less than 10\");\nelse\n    PRETTY_PRINT(\"n is at least 10\");\n```\n\n* 带参数的宏在大型程序的代码中使用非常普遍\n\n* 可以非常复杂，如“产生”函数\n\n* 在类函数宏中(带参数的宏)，`#`号作为一个**预处理运算符**，可以把记号转换成字符串。`##`相当于**连接运算符**，它会将两边的内容连接起来。\n\n* 存在中西方文化、习惯差异（西方程序员更喜欢使用宏）\n\n* 有个隐藏问题就是带参数的宏**不会进行类型检查**。\n\n* 部分宏会被`inline`函数替代。\n\n**其他编译预处理指令**\n\n* ① 条件编译\n\n* ② error\n\n* ③ ... ...\n\n**3.大程序结构**\n\n**12.2-2带参数的宏**\n\n**像函数的宏**\n\n1. #define cube(x)((x)*(x)*(x))\n\n2. 宏可以带参数\n\n```c\n#include <stdio.h>\n\n#define cube(x)((x)*(x)*(x))\n\nint main(int argc,char const *argv[])\n{\n    printf(\"%d\\n\",cube(5));\n    //cube(5)会被替换成cube((5)*(5)*(5));\n\n    return 0;\n}\n```\n\n**错误定义的宏**\n\n1. #define RADTODEG(x)(x*57.29578)\n\n2. #define RADTODEG(x)(x)*57.29578\n\n**带参数的宏的原则**\n\n1. 一切都要括号\n\n1. 1. 整个值要括号\n\n2. 参数出现的每个地方都要括号\n\n1. #define RADTODEG(x)((x)*57.29578)\n\n1. 可以带多个参数\n\n1. 1. #define MIN(a,b)((a)>(b)?(b):(a))\n\n1. 也可以组合(嵌套)使用其他宏\n\n2. 定义宏的时候后面千万不要加分号\n\n3. 在大型程序的代码中使用非常普遍\n\n4. 可以非常复杂，如\"产生\"函数\n\n1. 1. 在#和##这两个运算符的帮助下\n\n1. 存在中西方文化差异\n\n2. 部分宏会被inline函数替代\n\n**宏的缺陷**\n\n没有可以去检查宏有没有问题的机制\n\n**其他预编译**\n\n1. 条件编译\n\n2. error\n\n3. ..."},{"page":111,"body":"**本节完整补充**\n\n**多个源代码文件**\n\n* main()里的代码太长了，适合分成几个函数\n\n* 一个源代码文件太长了，适合分成几个文件\n\n* 两个独立的源代码文件不能编译形成可执行程序\n\n**项目**\n\n* 在 Dev C++ 中新建一个项目，然后把几个源代码文件加入进去（Dev C++ 属于比较特别的 IDE 了，其他很多 IDE 就算一个文件也需要你先建一个项目）\n\n* 对于项目，Dev C++的编译会把一个项目中所有的源代码文件都编译后，链接起来\n\n* 有的 IDE 有分开的**编译**和**构建**两个按钮，前者是对单个源代码文件编译，后者是对整个项目做链接\n\n12.3 main.c 文件:\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n\n/* run this program using the console pauser or add your own getch, system(\"pause\") or input loop */\n\nint max(int a, int b);\n\nint main(int argc, char *argv[])\n{\n    int a = 2;\n    int b = 5;\n    printf(\"%d\\n\", max(a, b));\n\n    return 0;\n}\n```\n\n12.3 max.c 文件:\n\n```c\nint max(int a, int b)\n{\n    return a>b?a:b;\n}\n```\n\n**编译单元**\n\n* 有些 IDE 会划分**编译**(产生 .c 文件)和**构建**（链接成 .exe 文件）两种模式。\n\n* 实际上一个 .c 文件是一个编译单元\n\n* 编译器每次编译只处理一个编译单元（编译完成之后形成 .o 文件）\n\n**12.3-1多个源代码文件**\n\n**多个.c文件**\n\n1. main()里的代码太长了适合分成几个函数\n\n2. 一个源代码文件太长了适合分成几个文件\n\n3. 两个独立的源代码文件不能编译形成可执行的程序\n\n因为看的是翁恺老师2014年的版本(比后来的课程内容多不少，后来的课程缩水了)，所以这部分已经有更好的就进行省略了\n\n1. 一个.c文件是一个编译单元\n\n2. 编译器每次编译只处理一个编译单元\n\n当程序变大时，把代码放进多个文件不是“炫技”，而是让职责更清楚：main.c 负责入口和主流程，max.c 负责一个具体能力，头文件负责告诉其他文件“有哪些函数可用”。\n\n一个最小多文件项目可以这样组织：\n\n```text\nproject/\n├── max.h\n├── max.c\n└── main.c\n```\n\n```c\n/* max.h */\n#ifndef MAX_H\n#define MAX_H\nint max(int a, int b);\n#endif\n\n/* max.c */\n#include \"max.h\"\nint max(int a, int b) { return a > b ? a : b; }\n\n/* main.c */\n#include <stdio.h>\n#include \"max.h\"\nint main(void) {\n    printf(\"%d\\n\", max(2, 5));\n    return 0;\n}\n```\n\n每个 `.c` 文件是一个编译单元，编译器先分别生成目标文件，再由链接器把它们合并成可执行文件。链接器需要找到函数定义；如果 `main.c` 调用了 `max`，却没有声明它，编译器会报隐式声明；如果声明了但没有链接 `max.c`，则会出现 `undefined reference`。\n\n头文件用 `#ifndef` / `#define` 防止重复包含，`#include \"max.h\"` 表示先从当前目录找，`#include ` 表示从系统 include 路径找。头文件放函数原型、类型和宏，通常不放函数定义，这样多个源文件引用时不会重复定义。\n\nIDE 中“编译”和“构建”的区别也来自这里：编译单个文件只处理一个编译单元，构建/链接才会把所有目标文件组织成一个程序。"},{"page":112,"body":"**本节完整补充**\n\n**头文件**\n\n* 项目小节，我们组建了一个项目，把 main.c 和 max.c 两个文件一起编译成一个文件了。\n\n* 注意到，在 main.c 文件中，我们在函数外部对 max 进行了原型声明。这里即使不进行原型声明，编译、运行也都可以正常进行，因为编译器会猜测你的 max 函数参数全部都是 int 类型。\n\n* 但如果 max 函数参数是 double 类型，或者返回值是 double，这时就会导致编译通过，但是有错误的隐患。\n\n* 那么，如何能保证在 main.c 中对 max 的使用，和在 max.c 中定义的 max 函数是一致的呢？我们需要一个中间的媒介，这个媒介就是**头文件**。\n\n* 把函数原型放到一个头文件（以 .h 结尾）中，在需要调用这个函数的源代码文件（.c 文件）中`#include`这个头文件，就能让编译器在编译的时候知道函数的原型。\n\n12.3 head.h 文件:\n\n```c\nint max(double a, int b);\nextern int gAll;\n```\n\n**#include**\n\n* 文件包含（include）并不是新的内容，第一章就已经见过，作用就是将指定的文件模块内容插入到`#include`位置\n\n* `#include`是一个编译预处理指令，和宏一样，在编译之前就处理了\n\n* 它把那个文件的全部文本内容原封不动地插入到它所在的地方\n\n* 所以也不是一定要在.c 文件的最前面`#include`\n\n**`\"\"`还是` `**\n\n```c\n#include <需包含的文件名>\n//\n#include\"需包含的文件名\"\n```\n\n* `#include`有两种形式来指出要插入的文件\n\n* `\"\"`要求编译器首**先在当前目录**（.c 文件所在的目录）寻找这个文件，如果**没有，则到编译器指定的目录去找**\n\n* ` `让编译器**只在指定的目录去找**（所以一般自己写的头文件都是用双引号）\n\n* 编译器自己知道自己的标准库的头文件在哪里\n\n* 环境变量和编译器命令行参数也可以指定寻找头文件的目录\n\n* **`#include`的误区**\n\n* `#include`不是用来引入库的\n\n* stdio.h 里只有 printf 的原型，printf 的代码在另外的地 方，某个 .lib(Windows) 或 .a(Unix)中\n\n* 现在的 C 语言编译器默认会引入所有的标准库\n\n* `#include `只是为了让编译器知道 printf 函数的原型，保证你调用时给出的参数值是正确的类型\n\n**使用头文件的小贴士**\n\n* 在**使用**和**定义**这个函数的地方都应该`#include`这个头文件\n\n* 一般的做法就是任何 .c 都有对应的同名的 .h，把所有对外公开的函数的原型和**全局变量**的声明都放进去(全局变量也是可以在多个 .c 文件中共享的，同样用头文件告诉别的文件这个全局变量的原型是什么)\n\n常用标准头文件\n\n- 头文件名 · 作用\n\n- ctype.h · 字符处理\n\n- math.h · 与数学处理函数有关的说明与定义\n\n- stdio.h · 输入输出函数中使用的有关说明和定义\n\n- string.h · 字符串函数的有关说明和定义\n\n- stddef.h · 定义某些常用内容\n\n- stdlib.h · 杂项说明\n\n- time.h · 支持系统时间函数\n\n**不对外公开的函数**\n\n* 函数是一个完成确定工作的完整程序块，只要经过适当的定义和声明，函数可以被其他函数调用。\n\n* 如果一个程序包括多个文件模块，要实现在一个模块中调用另一模块中的函数时，就需要对函数进行外部声明。\n\n`extern`表示所声明的函数是外部函数，其定义体在其他文件模块中。一般情况下关键字`extern`可以省略。\n\n```c\nextern 函数类型 函数名(参数表说明)\n```\n\n* 在函数前面加上`static`就使得它成为只能在所在的编译单元中被使用的函数。\n\n```c\nstatic 函数类型 函数名(参数表说明)\n```\n\n* 在全局变量前面加上`static`就使得它成为只能在所在的编译单元中被使用的全局变量。\n\n* 单模块程序，静态全局变量与一般的全局办理作用**完全相同**\n\n* 多模块程序中，静态全局变量可以**限制作用范围**在当前的文件模块中\n\n**12.3-2头文件**\n\n1. 把函数原型放到一个头文件(以.h结尾)中,在需要调用这个函数的源代码文件(c文件)中#include这个头文件，就能让编译器在编译的时候知道函数的原型\n\n1. #include是一个编译预处理指令,和宏一样，在编译之前就处理了\n\n2. 它把那个文件的全部文本内容原封不动地插入到它所在的地方\n\n3. 所以也不是一定要在.c文件的最前面#include\n\n**“”还是**\n\n1. #include有两种形式来指出要插入的文件\n\n1. 1. “”要求编译器首先在当前目录(.c文件所在的目录)寻找这个文件,如果没有,到编译器指定的目录去找，例如“MAX.h”啥的自己设定的那些\n\n2. 让编译器只在指定的目录去找，例如\n\n1. 编译器自己知道自己的标准库的头文件在哪里\n\n2. 环境变量和编译器命令行参数也可以指定寻找头文件的目录\n\n**#include的误区**\n\n1. #include不是用来引入库的\n\n2. stdio.h里只有printf的原型，printf的代码在另外的地方，某个.lib(Windows)或.a(Unix)中\n\n3. 现在的C语言编译器默认会引入所有的标准库\n\n4. #include 只是为了让编译器指定printf函数的原型，保证年调用时给出的参数值是正确的类型\n\n1. 在使用和定义这个函数的地方都应该%include这个头文件\n\n2. 一般的做法就是任何.c都有对应的同名的.h，把所有对外公开的函数的原型和全局变量的声明都放进去\n\n3. 全局变量是可以在多个.c之间共享的\n\n1. 在函数前面加上static就使得它成为只能在所在的编译单元中被使用的函数\n\n2. 在全局变量前面加上static就使得它成为只能在所在的编译单元中被使用的全局变量了"},{"page":113,"body":"**本节完整补充**\n\n**声明**\n\n**1.变量的声明和定义**\n\n* 可以通过定义`static`全局变量，将其作用范围仅限制在一个文件模块中，也可以通过外部变量的声明，使全局变量的作用范围扩展到其他文件模块；\n\n* 全局变量在整个程序所有的文件模块中起作用，如果在每一个文件模块中都定义一次全局变量，模块单独编译时不会发生错误，一旦把各模块连接在一起时，就会产生对同一个全局变量名**多次定义的错误**。\n\n* 全局变量只能在某一个模块中定义一次，如果其他模块要使用该全局变量，需要通过外部变量的声明，当程序连接时会统一指向全局变量定义的模块。否则不经声明直接使用全局变量，程序编译时会出现“**变量未定义**”的错误。\n\n* 对于全局变量来说，还有一种称为**外部变量**的形式。即全局变量的使用位置**先于**该全局变量的定义，在使用之前需要声明为外部变量。\n\n```c\n//变量的定义\nint i;\n//变量的声明\nextern int i;\n```\n\n* 声明是不产生代码的东西\n\n* ①函数原型 ②变量声明 ③结构声明\n\n* ④宏声明 ⑤枚举声明 ⑥inline 函数\n\n* 定义是产生代码的东西\n\n* ①函数 ②全局变量\n\n* 不产生代码，意思就是编译器只是记下你的声明，记下有这么一个东西的存在。\n\n**2.头文件中的声明**\n\n* 只有声明可以被放在头文件中——这是规则，不是法律。\n\n（学习 C 语言时，遵循一定的规则和规范是非常重要的，但这些规则并不具备法律的强制性。换句话说，虽然遵循 C 语言编程规范可以使代码更加清晰、易于维护，但如果不完全遵守这些规范，代码仍然可以编译和运行。这表明，虽然遵循规则是推荐的做法，但它更多是一种行业内的最佳实践，而不是必须遵守的法律条文。）\n\n* 否则会造成一个项目中多个编译单元里有重名的实体。\n\n* 某些编译器允许几个编译单元中存在同名的函数，或者用`weak`修饰符来强调这种存在。\n\n**3.重复声明**\n\n* 同一个编译单元里，同名的结构不能被重复声明。\n\n* 如果你的头文件里有结构的声明，很难这个头文件不会在一个编译单元里被`#include`多次\n\n* 所以需要“标准头文件结构”\n\n**4.标准头文件结构**\n\n```c\n#ifndef __LIST_HEAD__\n#define __LIST_HEAD__\n\n#include \"node.h\"\n\ntypedef struct _list {\n    Node* head;\n    Node* tail;\n} List;\n\n#endif\n```\n\n* 运用**条件编译和宏**，保证这个头文件在一个编译单元中只会被`#include`一次\n\n* `#progma once`也能起到相同的作用，但是不是所有的编译器都支持。\n\n* 条件编译指令和 C 语言的 if-else 语句完全不同，C 语言的 if-else 语句的两个分支程序段都会被生成到目标代码中。\n\n* 而条件编译的某个程序段的可执行代码可能根本就不存在。\n\n采用**条件编译的好处**：\n\n* 目标代码精简，不包含无关代码；\n\n* 系统代码的保护性更好。\n\n* **课后讨论 12.3.1：stdio.h 的问题**\n\n小明忘了在程序开头写#include ，但是 main()中的 printf(\"hello\\n\");还是通过了编译而且运行正确。说明为什么会这样，并举出例子说明什么情况下不#include 相应的头文件会通过编译但是不能正确运行\n\n讨论：\n\n因为这个头文件太常用了，所以很多老的编译器会**默认隐含包含** stdio.h 和 stdlib.h 两个头文件。这时候即使你没有自己手动包含 stdio.h 也是不会报错的。\n\n还有一种情况，是虽然你没有包含这个头文件，但是你包含的其他头文件包含了这个头文件，这时候你是用其中的函数也是不会报错的。\n\n不过很多新的编译器已经不在默认隐含包含这些常用的头文件了(Dev C++ 5.11 版本报错，编译未通过)，如果使用其中的函数必须自己手动包含，否则会报错。所以为了养成好的编程习惯，用到里面的东西就手动包含一下吧。\n\n能通过编译但是不能运行的情况就是函数的形参类型和编译器猜测的类型不一样时就可能会无法正确运行。\n\n* **章末习题：最小包围矩形（10 分）**\n\n**题目内容：**\n\n给定一组二维坐标，表示直角坐标系内的一个多边形的连续的顶点的坐标序列。计算能包围这个多边形的平行于坐标轴的最小矩形，输出它的左下角和右上角的坐标。\n\n**输入格式:**\n\n第一行是一个正整数 n 表示顶点的数量，第二行是 n 组整数，依次表示每个顶点坐标的 x 和 y 值。\n\n**输出格式：**\n\n四个整数，依次表示所计算的矩形的左下角的坐标的 x、y 值和右上角坐标的 x、y 值。输出最后带有回车换行。\n\n**输入样例：**\n\n```c\n> 5\n> 1 1 1 4 3 7 4 4 4 1\n>\n```\n\n**输出样例：**\n\n```c\n> 1 1 4 7\n>\n```\n\n解答：\n\n幸好这个最小矩形要求**四条边平行于坐标轴**，否则相当困难，还要考虑斜的矩形等等...\n\n如果最小矩形是四边平行于坐标轴，其实就是**找多边形各顶点横纵坐标的最小值和最大值**。\n\n初始化时把**最小坐标的变量设为最大**，**最大坐标的变量设为最小**，保证进循环**将变量初始化**。\n\nOnline Judge 关闭了，没法验证 www，样例是通过的。\n\n```c\n#include <stdio.h>\n#define MAXINT 2147483647\n#define MINXY 0\n\nint main(void)\n{\n    int n, i;\n    int minx, miny, maxx, maxy;\n    int x, y;\n    scanf(\"%d\", &n);\n    minx = miny = MAXINT;\n    maxx = maxy = MINXY;\n    for (i = 0; i < n; i++) {\n        scanf(\"%d %d\", &x, &y);\n        if (x > maxx)\n            maxx = x;\n        if (y > maxy)\n            maxy = y;\n        if (x < minx)\n            minx = x;\n        if (y < miny)\n            miny = y;\n    }\n\n    printf(\"%d %d %d %d\", minx, miny, maxx, maxy);\n\n    return 0;\n}\n```\n\n**12.3-3声明**\n\n**变量的声明**\n\n1. int i;是变量的定义\n\n2. extern int i;是变量的声明\n\n**声明和定义**\n\n1. 声明是不产生代码的东西\n\n1. 1. 函数原型\n\n2. 变量声明\n\n3. 结构声明\n\n4. 宏声明\n\n5. 枚举声明\n\n6. 类型声明\n\n7. inline函数\n\n1. 定义是产生代码的东西\n\n**头文件**\n\n1. 只有声明可以被放在头文件中\n\n1. 1. 是规则不是法律\n\n1. 否则会造成一个项目中多个编译单元里有重名的实体\n\n1. 1. *某些编译器允许几个编译单元中存在同名的函数，或者用week修饰符来强调这种存在\n\n**重复声明**\n\n1. 同一个编译单元里，同名的结构不能被重复声明\n\n2. 如果你的头文件里有结构的声明，很难这个头文件不会在一个编译单元里被#include多次所以需要\"标准头文件\"\n\n**标准头文件结构**\n\n1. 运用条件编译和宏，保证这个头文件在一个编译单元中只会被#include一次\n\n2. #pragma once也能起到相同的作用，但是不是所有的编译器都支持\n\n```c\n#ifndef __LIST_HEAD__\n#define __LIST_HEAD__\n\n#include\"node.h\"\n\ntypedef struct _list{\n    Node*head;\n    Node*tail;\n}List;\n\n#endif\n```"}];
