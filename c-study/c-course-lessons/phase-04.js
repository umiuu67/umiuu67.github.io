window.COURSE_CHUNKS=window.COURSE_CHUNKS||{};window.COURSE_CHUNKS["循环"]=[{"page":32,"body":"**本节完整补充**\n\n**有些事情就得用循环才能解决**\n\n* if 语句可以判断条件满足时才做相应的动作，而循环语句可以在满足条件时，不断地重复执行一些动作。\n\n* 判断数的位数——程序要读入一个 4 位(含 4 位)的正整数， 然后输出这个整数的位数。\n\n* 人直接通过眼睛看数位，判断数的位数，而计算机不擅长做这个，计算机擅长通过比较数在什么区间判断数的位数，而人是不擅长的，人对文字的处理能力比对数字的处理能力更强。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int n = 1;\n\n    scanf(\"%d\", &x);\n    if (x > 999)\n        n = 4;\n    else if (x > 99)\n        n = 3;\n    else if (x > 9)\n        n = 2;\n    else\n        n = 1;\n\n    printf(\"%d\", n);\n\n    return 0;\n}\n```\n\n上述程序有一个经典错误，就是将 else 全部改成 if，即四个 if 判断四次。但是你需要注意，你写的程序是对步骤的描述，而不是关系或者说明，如果改写成四个 if，那每个都会运行判断一次，最后都会输出 n = 1，显然是不对的。\n\n* 题目明确了输入的是不超过四位的正整数，所以我们的程序可以按上面那样简化判断条件。\n\n* 而且注意我们上面的程序是从高处往低处判断，这样每次判断可以排除掉一个确定的数位区间或者直接确定数位。\n\n```c\nif (x > 0)\n    n = 1;\nelse if (x > 9)\n    n = 2;\nelse if (x > 99)\n    n = 3;\nelse\n    n = 4;\n```\n\n* 如果像这样反过来，我们发现任何数都满足`x > 0`于是后面的条件判断都失效了，所以反过来是不可以的。\n\n* 也可以得到推论，判断 x 大于某数，应该从高处往低处判断，x 小于某数，应该从低处往高处判断。\n\n* 但是，本节开篇的代码也有一定局限性，如果位数有很多位的时候，就需要非常多层 if-else 嵌套才能实现数位的判断，如何写一个通用的整数数位判断程序呢？受到人数数（数一个划掉一个）的启发，我们也可以让计算机“数数”，通过 x = x/10，“数”一个数把最右边的一个数划掉，并且让计数器 n++，最终统计划掉所有数后 n 的值即可。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int n = 0;\n\n    scanf(\"%d\", &x);\n\n    while (x > 0) {\n        n++;\n        x /= 10;\n    }\n\n    printf(\"%d\", n);\n\n    return 0;\n}\n```\n\n* int 型在计算机内部有表示整数的范围，所以上述程序依然不能判断任意一个数的整数位数哦！\n\n* **课后讨论 3.2.1：最大能数多大的数**\n\n用课程中的那个数数的位数的程序，你能数到多大的数？\n\n讨论：\n\n* C 语言中，int 型数据在内存中的存储形式是**补码**，32 位补码所能表示的整数范围为[-2^{-31},2^{31}-1]，2^{31}-1 = 2147483647，所以最大可以数到 10 位正整数。\n\n**4.1循环**\n\n**三位数逆序的题**\n\n```c\n#include <stdio.h>\nint main(){\n\tint x;\n\tint n=0;\n\tscanf(\"%d\", &x);\n\tn++;\n\tx /= 10;\n\twhile(x>0){\n\t\tn++;\n\t\tx /= 10;\n\t}\n\tprintf(\"%d\\n\", n);\n\treturn 0;\n}\n```\n\n**while循环**\n\n1.如果我们把while翻译作\"当\"，那么一个while循环的意思就是：当条件满足的时候，不断的重复循环体内的语句\n\n2.循环执行前判断是否继续循环，所以可能一次循环一次也没有执行\n\n3.条件成立是循环继续的条件\n\n**验证**\n\n1.测试程序常使用边界数据，如有效范围两端的数据、特殊的倍数等\n\n2.个位数，10，0，负数\n\n**调试**\n\n在程序适当的地方插入printf()来输出变量的内容\n\n**do while循环**\n\n在进入循环的时候不做检查，而是在执行完一轮循环体的代码之后，再来检查循环的条件是否满足，如果满足则继续下一轮循环，不满足则循环结束\n\n```c\ndo{\n  循环体语句\n}while(循环条件);\n```\n\n**两种循环**\n\ndo while循环 和 while循环很像，区别是在循环体执行结束的时候才来判断条件。也就是说，无论如何，循环都会执行一遍，然后再来判断条件。与while循环相同的是，条件满足时执行循环，条件不满足时结束循环"},{"page":33,"body":"**本节完整补充**\n\n**while 循环-条件满足就不停执行后面的语句**\n\n* 如果我们把 while 翻译作“当”，while 循环就是条件满足时，不断重复循环体内的语句。while 的条件判断表达式可以是任意合法的表达式，循环体语句只能是一条语句（一般为 { } 构成的复合语句）。\n\n```c\nwhile (x > 0) {\n    x /= 10;\n    n++;\n}\n```\n\n**执行流程**\n\n- 起点：前面的语句\n\n- 处理：循环体\n\n- 判断：条件满足？\n\n- 终点：后续语句\n\n执行到“条件满足？”时：满足条件则进入“循环体”并继续判断；不满足则结束循环。\n\n图 while 循环流程图\n\n* 循环体内要有改变循环条件真假性的操作，否则会导致死循环（或称无限循环）。\n\n* 根据 while 的执行逻辑，while 里的循环体有可能一次都不执行。\n\n* 没有计算机如何验证循环程序的结果？——答案是**手工模拟**：在纸上列出所有变量，随着程序的进行，按步骤不断重新计算变量的值并记录。当程序运行结束之后，手工模拟表格最后得到的值就是程序的最终运行结果。\n\n**while循环**\n\n1.如果我们把while翻译作\"当\"，那么一个while循环的意思就是：当条件满足的时候，不断的重复循环体内的语句\n\n2.循环执行前判断是否继续循环，所以可能一次循环一次也没有执行\n\n3.条件成立是循环继续的条件\n\n**验证**\n\n1.测试程序常使用边界数据，如有效范围两端的数据、特殊的倍数等\n\n2.个位数，10，0，负数\n\n**调试**\n\n在程序适当的地方插入printf()来输出变量的内容\n\nwhile 循环的执行顺序是“先问条件，再执行，再回来问条件”。如果条件一开始就是假，循环体一次都不会执行；如果循环体内没有改变条件的状态，程序就会陷入死循环。\n\n写循环时先用三个问题组织代码：\n\n1. 进入循环前，状态是什么？\n\n2. 继续循环的条件是什么？\n\n3. 每轮如何改变状态？\n\n例如：\n\n```c\nint count = 1;\nwhile (count <= 10) {\n    printf(\"%d\\n\", count);\n    count++;\n}\n```\n\n初始状态是 1，继续条件是 `count <= 10`，每轮 `count++` 推进状态。如果条件写成 `< 10`，会少输出 10；如果忘记 `count++`，会无限循环；如果把推进语句放在 `printf` 后但漏掉大括号，则只有第一条语句属于循环体。\n\n`while` 适合“知道条件、不知道次数”的场景，比如持续读入直到 `-1`、读到文件结束、遍历链表直到空指针。调试时可以在循环里打印当前变量，也可以利用断点观察每一轮值的变化。最终提交前把调试输出删掉，否则 OJ 会因为多余输出判错。"},{"page":34,"body":"**本节完整补充**\n\n**do-while 循环：不管三七二十一，先做一次循环**\n\n* 进入循环的时候不做检查，而是在执行完一轮循环体的代码之后，再来检查循环的条件是否满足，如果满足，则进入下一轮循环，不满足则结束循环。\n\n* do-while 和 while 很像，区别只是什么时候判断条件，结果是 do-while 至少会做一次，而 while 有可能一次都不做。\n\n* 记得 do - while 语句结束小括号外有个分号`;`表示语句结束。语句结束一般用`{ }`或`;`，某些语句没结束会导致 C 语言编译不通过。\n\n```c\ndo{\n    <循环体语句>\n}while (<循环条件>);\n```\n\n**执行流程**\n\n- 起点：前面的语句\n\n- 处理：循环体\n\n- 判断：条件满足？\n\n- 终点：后续语句\n\n执行到“条件满足？”时：满足条件则进入“循环体”并继续判断；不满足则结束循环。\n\n图 do-while 循环流程图\n\n**do while循环**\n\n在进入循环的时候不做检查，而是在执行完一轮循环体的代码之后，再来检查循环的条件是否满足，如果满足则继续下一轮循环，不满足则循环结束\n\n```c\ndo{\n  循环体语句\n}while(循环条件);\n```\n\n**两种循环**\n\ndo while循环 和 while循环很像，区别是在循环体执行结束的时候才来判断条件。也就是说，无论如何，循环都会执行一遍，然后再来判断条件。与while循环相同的是，条件满足时执行循环，条件不满足时结束循环\n\ndo-while 与 while 的唯一本质区别是判断时机。do-while 先执行一次循环体，再判断条件，因此至少执行一次；while 先判断条件，可能一次都不执行。\n\n最典型的用途是“必须先输入再验证”：\n\n```c\nint n;\ndo {\n    printf(\"请输入 1 到 10：\");\n    scanf(\"%d\", &n);\n} while (n < 1 || n > 10);\n```\n\n用户第一次输入 `0` 时，循环体已经执行过一次；条件 `n 10` 为真，于是再次输入。这个结构天然表达了“至少尝试一次，不合法就重试”。如果改用 while，往往需要先读取一次，再进入循环，代码会多一段重复。\n\n容易出错的地方有三个：`while (...) ;` 末尾的分号不能漏；循环体内必须更新被判断的变量；如果输入格式错误或 `scanf` 失败，`n` 可能没有被更新，导致死循环。更稳健的版本会同时检查 `scanf` 返回值，例如：\n\n```c\nint result = scanf(\"%d\", &n);\nif (result != 1) break;\n```\n\n选择循环时问“是否必须先走一次”。菜单、输入校验、第一次读取后才知道何时结束，都适合 do-while；一般的范围遍历、未知输入流，while 更直接。"},{"page":35,"body":"**本节完整补充**\n\n**猜数游戏**\n\n* 让计算机来想一个数，然后让用户来猜，用户每输入一个数，就告诉它是大了还是小了，直到用户猜中为止，最后还要告诉用户它猜了多少次。\n\n* 因为需要不断重复让用户猜，所以需要用到循环\n\n* 在实际写出程序之前，我们可以先用文字描述程序的思路\n\n* 核心重点是循环的条件，人们往往会考虑循环终止的条件\n\n* 文字描述：\n\n1. 计算机随机想一个数，记在变量 number 里；\n\n2. 一个负责计次数的变量 count 初始化为 0；\n\n3. 让用户输入一个数字 a；\n\n4. count 递增（加一）；\n\n5. 判断 a 和 number 的大小关系，如果 a 大，就输出“大”；如果 a 小就输出“小”；\n\n6. 如果 a 和 number 是不相等的（无论大还是小），程序转回到第 3 步；\n\n7. 否则，程序输出“猜中”和次数，然后结束。\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <time.h>\n\nint main()\n{\n    srand(time(0));\n    int number = rand() % 100 + 1;\n    int count = 0 ;\n    int a = 0;\n\n    printf(\"我已经想好了一个 1 到 100 之间的数。\");\n    do{\n        printf(\"请猜这个 1 到 100 之间数：\");\n        scanf(\"%d\", &a);\n        count++;\n        if (a > number)\n            printf(\"你猜的数大了。\");\n        else if (a < number)\n            printf(\"你猜的数小了。\");\n    }while (a != number);\n\n    printf(\"太好了，你用了 %d 次就猜到了答案。\\n\", count);\n\n    return 0;\n}\n```\n\n* `rand()`是一个库函数，每次调用可以得到一个随机数。\n\n* 使用`srand(time(0));`语句保证每次产生的随机数不一样。\n\n* **课后讨论 4.4.1：为什么 100 以内的数猜 7 次就够了？**\n\n课程中的猜数游戏，为什么 100 以内的数猜 7 次就够了？\n\n讨论：\n\n* 使用二分查找在每次进行猜数的时候范围都会缩小 1/2，换句话来说就是每猜测一次，剩下数的数量就会减少一半，而 2 的 7 次方是 128，所以最多猜 7 次。\n\n**循环应用(猜数游戏)**\n\n**需求**\n\n让计算机来想一个数，然后让用户来猜，用户每输入一个数，就告诉用户是大了还是小了，直到用户猜到为止，最后还要告诉用户他猜了几次\n\n**思路**\n\n因为需要不断重复让用户猜，所以需要用到循环\n\n在实际写出程序之前，我们可以先用文字描述程序的思路\n\n核心重点是循环的条件\n\n人们往往会考虑循环终止的条件\n\n**随机数**\n\n每次召唤rand()就可以得到一个随机数\n\n**代码块**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <time.h>\n\nint main(){\n    //不加srand(time(0));的话，每次循环的数都一样\n    srand(time(0));\n\tint number = rand()%100+1;\n\tint count = 0;\n\tint a = 0;\n\tprintf(\"我已经想好了一个1到100之间的数。\");\n\tdo {\n\t\tprintf(\"请猜这个1到100之间数：\");\n\t\tscanf(\"%d\", &a);\n\t\tif ( a > number ) {\n\t\t\tprintf(\"你猜的数大了。\");\n\t\t} else if ( a < number ) {\n\t\t\tprintf(\"你猜的数小了。\");\n\t\t}\n\t\tcount ++;\n\t} while (a != number);\n\tprintf(\"太好了，你用了%d次就猜到了答案。\\n\", count);\n\n\treturn 0;\n}\n```\n\n**%100**\n\nx%n的结果是[0,n-1]的一个整数"},{"page":36,"body":"**本节完整补充**\n\n**求平均数**\n\n思考，该程序需要哪些变量？\n\n* 平均数要怎么算？\\displaystyle{Average = \\frac{\\sum_{i = 1}^{n}{x_i}}{n} }\n\n* 一个读整数的变量变量。\n\n* 一个累加变量。不需要永久记录每个成绩数据，只需要把它依次加到累加里变量里，读完所有成绩后再除以读到个数即可。\n\n* 一个记录累加次数的变量。\n\n求平均数 流程图\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int number;\n    int sum;\n    int count;\n\n    scanf(\"%d\", number);\n    while (number != -1) {\n        sum += number;\n        count++;\n        scanf(\"%d\", &number);\n    }\n\n    printf(\"%.2f\", 1.0 * sum / count);\n\n    return 0;\n}\n```\n\n**算平均数**\n\n**需求**\n\n1.让用户输入一系列的正整数，最后输入-1表示输入结束，然后程序计算出这些数字的平均数，输出输入的数字的个数和平均数\n\n2.变量->算法->流程图->程序\n\n**变量**\n\n1.一个记录读到的整数的变量\n\n2.平均数要怎么算？\n\n3.只需要每读到一个数，就把它加到一个累加的变量里，到全部数据读完，再拿它去除读到的数的个数就可以了\n\n4.一个变量记录累加的结果，一个变量记录读到的数的个数\n\n**算法**\n\n1.初始化变量sum和count为0;\n\n2.读入number;\n\n3.如果number不是-1，则将number加入sum，并将count加1，回到2;\n\n4.如果number是-1，则计算和打印出sum/count(注意换成浮点数来计算)\n\n**代码块**\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int sum = 0;\n    int count = 0;\n    int number;\n\n    scanf(\"%d\", &number);\n    while ( number != -1 ) {\n        sum += number;\n        count ++;\n        scanf(\"%d\", &number);\n    }\n\n    double dsum = sum;\n    printf(\"The average is %f.\\n\", dsum / count);\n\n    return 0;\n}\n```\n\n算平均数不是只有一条除法公式，而是要同时维护三个状态：当前读到的数、累计总和、已经读到的个数。程序不保存所有输入，每次读入一个数就立即累加，这是“流式处理”的雏形。\n\n一个应该改正的示范如下：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    int number;\n    int sum = 0;\n    int count = 0;\n\n    while (scanf(\"%d\", &number) == 1 && number != -1) {\n        sum += number;\n        count++;\n    }\n\n    if (count > 0) {\n        printf(\"%.2f\\n\", 1.0 * sum / count);\n    }\n    return 0;\n}\n```\n\n这里必须注意三点：\n\n- `sum` 和 `count` 都要初始化为 0。\n\n- `scanf` 要写 `&number`，否则函数没有地址，无法把读到的值放回变量。\n\n- 输出时用 `1.0 * sum` 或让 `sum` 是浮点数，否则整数除法会先丢掉小数。\n\n循环结束后的 `count > 0` 是必要的边界保护：如果一开始就输入 `-1`，没有任何有效成绩，就不能除以 0。调试时输入 `10 20 30 -1`，手算总和为 60、个数为 3、平均值应输出 `20.00`；再输入 `-1`，程序应能安全结束。"},{"page":37,"body":"**本节完整补充**\n\n**整数求逆**\n\n整数的分解：\n\n1. 一个整数是由一至多位数字组成的，要想获得它的逆序，一个思路是分解出它每一位上的数字，然后处理。\n\n2. 对一个整数做 %10 运算，可以获得它的个位。\n\n3. 对一个整数做 /10 的操作，就去掉了它的个位，然后再对结果 %10，就得到原来整数的十位，以此类推。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    scanf(\"%d\", &x);\n\n    int digit;\n    int ret = 0;\n\n    while (x > 0) {\n        digit = x % 10;\n        ret = 10 * ret + digit;\n        //printf(\"x = %d, digit = %d, ret = %d\\n\", x, digit, ret); //调试信息\n        x /= 10;\n    }\n\n    printf(\"%d\", ret);\n\n    return 0;\n}\n```\n\n* 如果要让逆序之后的数，即使为 0 也保留数位并逆序，比如输入 700，输出 007，可以简单修改上述代码获得。\n\n```c\nwhile (x > 0)\n{\n    digit = x % 10;\n    printf(\"%d\", digit);\n    ret = 10 * ret + digit;\n    x /= 10;\n}\n\n//printf(\"%d\", ret);\n```\n\n**5.判断和循环的常见错误**\n\n**整数的分解**\n\n1.一个整数是由1-多位数字组成的，如何分解出整数的各个位上的数字，然后加以计算\n\n2.对一个整数做%10的操作，就得到它的个位数\n\n3.对一个整数做/10的操作，就去掉了它的个位数\n\n4.然后再对2的结果做%10,就得到原来数的十位数\n\n....以此类推\n\n**数的逆序**\n\n1.输出一个正整数，输出逆序的数\n\n2.结尾的0的处理\n\n整数求逆练习“取余丢位”的标准循环。\n\n```c\nint n, result = 0;\nscanf(\"%d\", &n);\ndo {\n    int digit = n % 10;\n    result = result * 10 + digit;\n    n /= 10;\n} while (n > 0);\nprintf(\"%d\\n\", result);\n```\n\n追踪 123：第一轮取 3，result=3，n=12；第二轮取 2，result=32，n=1；第三轮取 1，result=321。用 do-while 可以保证 n=0 也输出 0；用 while 时要先单独处理 n=0。\n\n若输入可能为负，可先记录符号并对绝对值处理，最后补符号。若逆序后可能超出 int 范围，结果本身需要 long long 或按题目说明处理。前导零在数值表示中自然消失，因此 700 逆序为 7。"},{"page":38,"body":"**本节完整补充**\n\n**for 循环**\n\n* for 循环像一个计数循环：设定一个计数器，初始化它，然后在计数器达到某值之前重复执行循环体，而每执行一次循环体，计数器就进行一次值的调整。for 中的三个表达式，每个表达式都是可以省略的`for (;条件;)`等价于`while (条件)`\n\n```c\nfor (初始动作, 条件, 每轮的动作) {\n    ... ...\n}\n```\n\n* 事实上，for 循环一定可以和 while 循环相互转化。\n\n```c\nfor (int i = 1; i <= n; i++) {\n    fact *= i;\n}\n//等价于\nint i = 1;\nwhile (i <= n) {\n    fact *= i;\n    i++;\n}\n```\n\n**执行流程**\n\n- 起点：前面的语句\n\n- 处理：循环体\n\n- 处理：i++\n\n- 处理：i = 1\n\n- 判断：条件满足？\n\n- 终点：后续语句\n\n执行到“条件满足？”时：满足条件则进入“循环体”并继续判断；不满足则结束循环。\n\n图 for 循环流程图\n\n* for 可以读作“对于”，于是下面的 for 循环可以读成：“对于一开始`i = 10`，当`i > 0`时，重复执行循环体，每一轮执行完循环体内语句后，令`i--`。”\n\n```c\nfor (int i = 10; i > 0; i--) {\n    .../*Code*/...\n}\n```\n\n下面两种 for 循环的循环次数都是一样的，都为 n，区别在于第一次循环 i 的值以及循环结束后 i 的值。注意，循环结束后，第一段代码的 i 为 n，第二段代码的 i 为 n+1，即对于 for 循环，i 最终的值是**不满足**循环条件的值。\n\n```c\nfor (i = 0; i < n; i++) {\n    .../*Code*/...\n}\nfor (i = 1; i <= n; i++) {\n    .../*Code*/...\n}\n```\n\n试编写程序，让用户输入 n，计算阶乘 n! = 1 × 2×3 ... ×n\n\n```c\ndouble factorial_a(int n) {\n    double product = 1;          //小套路：求积初值设为 1，求和初值设为 0。\n    for (int i = 1; i <= n; i++)  //仅有 C99 支持 for 语句内定义初值 i，这里 i 从 2 开始也没问题，可以自行验证\n        product *= i;\n\n    return product;\n}\n//也可以逆向递减计数，实现的功能相同\ndouble factorial_b(int n) {\n    double product = 1;\n    for (int i = n; i > 1; i--)\n        product *= i;\n\n    return product;\n}\n```\n\n下面是常见的计算**阶乘和**的方法（假设下列变量都已经正确定义），也可以调用函数计算。\n\n```c\n//二重循环写法\nsum = 0;\nfor (i = 1; i <= n; i++) {\n    item = 1;\n    for (j = 1; j <= i; j++) {\n        item = item * j;\n    }\n    sum = sum + item;\n}\nprintf(\"%.0f\\n\", sum)\n```\n\n```c\n//单重循环写法\nsum = 0;\nitem = 1;\nfor (i = 1; i <= n; i++) {\n    item = item * i;\n    sum = sum + item;\n}\nprintf(\"%.0f\\n\", sum);\n```\n\n* **课后讨论 3.2.2：为什么老师说 for 循环很怪？**\n\nfor 循环是高级语言出现的第一种循环，它和现代的程序设计理念是否存在差距？为什么 Python 语言就没有 C 语言这种形式的 for 循环了？\n\n讨论：\n\n* for 循环不符合人类的语言逻辑，不易理解，因此就造成了第一次接触语言的人觉得 for 循环很怪。而现代程序设计理念更强调与人类语言的逻辑统一性，所以在新兴的 python 语言中祛除掉了 C 中的 for 循环逻辑。但由于 for 循环抽象程度高，所以在掌握语言基础后，其表现逻辑更为简洁易于回溯反而更方便使用。\n\n* Python 语言就没有 for 循环的另一个原因是 Python 里没有累加器。\n\n**5.1 for循环(第三种循环)**\n\nfor循环像是一个计数循环:设定一个计数器，初始化它，然后在计数器到达某值之前，重复执行循环体，而每执行一轮循环，计数器值以一定 步进 进行调整，比如加一或者减一\n\n```c\n例如：\nfor(i = 0;i < 5;i++){\n  printf(\"%d\",i);\n}\n\nfor(初始动作;条件;每轮的动作){\n}\n//for中的每一个条件都是可以省略的，for(;条件;) == while(条件)\n```\n\n**for = 对于**\n\nfor(count = 10; count > 0; count-- )\n\n可以解读成：对于一开始的count = 10，当count > 0的时候，重复做循环体，每一轮循环在做完循环体内语句后，使得count--\n\n**小套路**\n\n做求和的程序时，记录结果的变量应该初始化为0，而做求积的变量时，记录结果的变量应该初始化为1\n\n循环控制变量i只在循环里被使用了，在循环外面它没有任何用处。因此，我们可以把变量i的定义写到for语句里面去\n\n**尝试(try)**\n\n1. 1×1还是1，所以程序的循环需不需要从1开始，那么改成多少开始合适呢？这样修改之后，程序对于所有的n都正确吗？这样的改动有价值吗？\n\n2. 除了可以从1乘到n来计算n！，还可以从n乘到1来计算吧？试试换个方向来计算n。这时候，还需要循环控制变量i吗？\n\n**for == while**\n\n```c\nfor( int i = 1;i <= n;i++ ){\n    fact *= i;\n}\n-------------------------------------------\nint i = 1;\nwhile(i <= n){\n    fact *=i;\n    i++;\n}\n```\n\n**循环次数**\n\nfor( i = 0; i < n; i++)\n\n这样循环的次数是n，而循环次数结束以后，i的值是n。循环的控制变量i，是选择从0开始还是从1开始，是判断 i<n还是判断i <=n，对循环次数，循环结束后变量的值都是有影响的\n\n**Tips for Loops(小贴士循环)**\n\n1. 如果有固定次数，用for\n\n2. 如果必须执行一次，用 do while\n\n3. 其他情况用while"},{"page":39,"body":"**本节完整补充**\n\n**循环的选择**\n\n本节三种循环体应该如何选择？\n\n1. 如果有固定循环次数，首选 for 语句。它看起来最清晰，组成部分一目了然。\n\n2. 如果必须先进入循环，循环条件要在循环体中明确，用 do-while\n\n3. 其他情况——循环次数未知、但循环条件进入循环时已经明确的，用 while\n\n程序设计时一般如果没有指定输入数据的个数，需要自己设置，一般有两种方法\n\n1. 先输入一个正整数 n，代表数据个数，再输入 n 个数据，循环重复 n 次，此时可以用 for 实现。\n\n2. 设定一个**特殊的数据（伪数据）**作为循环结束的标志，比如成绩输入系统可以选负数作为输入结束的标志。由于循环次数未知，此时可以考虑用 while 来实现。\n\n* **章末习题：时间换算（5 分）**\n\n**题目内容：**\n\nUTC 是世界协调时，BJT 是北京时间，UTC 时间相当于 BJT 减去 8。现在，你的程序要读入一个整数，表示 BJT 的时和分。整数的个位和十位表示分，百位和千位表示小时。如果小时小于 10，则没有千位部分；如果小时是 0，则没有百位部分；如果小时不是 0 而分小于 10 分，需要保留十位上的 0；如果小时是 0 而分小于 10 分的，则不需要保留十位上的 0。如 1124 表示 11 点 24 分，而 905 表示 9 点 5 分，36 表示 0 点 36 分，7 表示 0 点 7 分。\n\n有效的输入范围是 0 到 2359，即你的程序不可能从测试服务器读到 0 到 2359 以外的输入数据。\n\n你的程序要输出这个时间对应的 UTC 时间，输出的格式和输入的相同，即输出一个整数，表示 UTC 的时和分。整数的个位和十位表示分，百位和千位表示小时。如果小时小于 10，则没有千位部分；如果小时是 0，则没有百位部分；如果小时不是 0 而分小于 10 分，需要保留十位上的 0；如果小时是 0 而分小于 10 分的，则不需要保留十位上的 0。\n\n提醒：要小心跨日的换算。\n\n**输入格式：**\n\n一个整数，表示 BJT 的时和分。整数的个位和十位表示分，百位和千位表示小时。如果小时小于 10，则没有千位部分；如果小时是 0，则没有百位部分；如果小时不是 0 而分小于 10 分，需要保留十位上的 0；如果小时是 0 而分小于 10 分的，则不需要保留十位上的 0。\n\n**输出格式：**\n\n一个整数，表示 UTC 的时和分。整数的个位和十位表示分，百位和千位表示小时。如果小时小于 10，则没有千位部分；如果小时是 0，则没有百位部分；如果小时不是 0 而分小于 10 分，需要保留十位上的 0；如果小时是 0 而分小于 10 分的，则不需要保留十位上的 0。\n\n**输入样例：**\n\n803\n\n**输出样例：**\n\n3\n\n解答：\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int BJT;\n    scanf(\"%d\", &BJT);\n\n    int hour1, minute1, t1;\n    int hour2, minute2, t2;\n\n    hour1 = BJT / 100;\n    minute1 =  BJT  % 100;\n    t1 = 60 * hour1 + minute1;\n    t2 = t1 - 60 * 8;\n\n    if (t2 >= 0) {\n        hour2 = t2 / 60;\n        minute2 = t2 % 60;\n    } else {\n        t2 = 24 * 60 + t2;\n        hour2 = t2 / 60;\n        minute2 = t2 % 60;\n    }\n    if (hour2 == 0)\n        printf(\"%d\", minute2);\n    else if (minute2 < 10)\n        printf(\"%d%02d\", hour2, minute2);\n    else\n        printf(\"%d%d\", hour2, minute2);\n\n    return 0;\n}\n```\n\n* **章末习题：分队列（5 分）**\n\n班级第一次列队，通常老师会让同学按身高排成一列，然后 1、2 报数，喊到 1 的同学向前一 步，就这样，队伍就变成两列了。假设现在一个班级有 n 个同学，并且他们已经按身高排成了一列，同学按身高从 1 到 n 编号，你能告诉我最后哪些编号的同学站在了第一列么?\n\n**输入格式:**\n\n输入一个正整数 n，表示班级的人数。\n\n按顺序输出所有在第一列的同学对应的编号，每两个编号之间用一个空格隔开，最后一个编号后面没有空格。\n\n11\n\n1 3 5 7 9 11\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int n;\n    scanf(\"%d\", &n);\n\n    int odd = 1;\n    if (n % 2 == 0)\n        odd = 0;\n\n    for (int i = 1; i <= n; i = i + 2) {\n        printf(\"%d\", i);\n        if (odd == 1) {\n            if (i != n)\n                printf(\" \");\n        } else {\n            if (i != n - 1)\n                printf(\" \");\n        }\n    }\n\n    return 0;\n}\n```\n\n**第四章：进一步的判断与循环**\n\n**1.逻辑类型与运算**\n\n**4.2循环计算**\n\n**小套路**\n\n计算之前先保存原始的值，后面可能有用。重新定义一个变量来存放计算后的值\n\n**计数循环**\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int n = 3;\n\n    while ( n>= 0 ) {\n        printf(\"%d \", n);\n        n--;\n    }\n    printf(\"发射\\n\");\n\n    return 0;\n}\n```\n\n1.这个循环需要执行多少次？\n\n2.循环停下来的时候，有没有输出最后的0？\n\n3.循环结束后，count的值是多少？\n\n技巧：如果要模拟运行一个很大次数的循环，可以模拟较少的循环次数，然后做出推断"},{"page":40,"body":"**本节完整补充**\n\n**循环控制**\n\n判断素数的程序：\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n\n    scanf(\"%d\", &x);\n\n    int i;\n    int isPrime = 1;\n    for (i = 2; i < x; i++) {\n        if (x % i == 0) {\n            isPrime = 0;\n            break;\n        }\n    }\n    if (isPrime == 1)\n        printf(\"是素数\\n\");\n    else\n        printf(\"不是素数\\n\");\n\n    return 0;\n}\n```\n\n* `isPrime`的初值设置是有讲究的，不是乱设置的。首先能保证边界条件，x = 2 的时候即使不进入循环，程序也可以判断 2 是素数。\n\n* 其次由于该程序实质上是枚举找反例证伪，找到反例就证明该数不是素数，所以初值设置为 1，如果找到反例就把`isPrime`设为 0，找完全部可能的值都找不到，就说明是素数，让它保持初值 1。\n\n* 由于 x 不可能被大于 x/2 的数整除，所以上述 i 的区间可以缩小为[2，x/2]，由于 C 语言中 int 型的运算特性——x/2 是向下取整的，所以应该是闭区间。\n\n* 然而…… ……还有高手！其实，i 只需要在区间[2，\\sqrt{x}]取值即可。现作简要说明：因为如果一个数不是素数是合数，那么一定可以由两个自然数相乘得到，其中一个大于或等于它的平方根，一个小于或等于它的平方根，并且成对出现。所以找不到小于或等于 x 的平方根的一个数能整除 x，就说明 x 是素数了。比如 6 是合数，2<=根号 6<=3，是可以找到 2 整除 6 的。\n\n* **课后讨论 5.1.1：利用循环变量来判断素数不好吗？**\n\n课程中提到有种“聪明”的做法，可以不设 isPrime，直接利用循环出口处循环变量和终点值的关系来判断循环是否 break 了。你觉得这种做法好吗？\n\n```c\n>   for (i = 2; i < x; i++) {\n>        if (x % i == 0)\n>            break;\n>    }\n>    if (i < x)\n>        printf(\"是素数\\n\");\n>    else\n>        printf(\"不是素数\\n\");\n>\n```\n\n讨论：\n\n这段代码可能存在以下问题：\n\n* 当输入的数小于等于 1 时，程序无法正确判断素数。因为在这种情况下，程序内部的 for 循环不会执行，导致无法区分该数是素数还是合数。\n\n* 声明的变量调用的目的越单一越好，减少多处不同目的的调用造成的错误的可能性，也便于阅读理解。\n\n* 变量的命名不够规范，例如变量 i 不易于理解其含义。\n\n* 没有对用户输入进行错误处理。如果用户输入的不是整数，程序就会出现意料之外的错误。变量 i 的类型为 int，如果要判断的数比较大，可能会导致整型溢出的问题。\n\n* 程序中没有考虑多线程的情况，可能会导致线程安全性问题。\n\n**2.多重循环**\n\n**5.2循环控制**\n\n**素数**\n\n只能被1和自身整除的数，不包括1\n\n**break VS continue**\n\n1. break:跳出循环\n\n2. continue:跳过循环这一轮剩下的语句进入下一轮\n\n3. 都只能对它所在的那层循环生效\n\n**嵌套的循环**\n\n1. 意思：循环的里面还是循环\n\n2. 嵌套循环时的break只会跳出当前所在的循环，如果嵌套了多层循环就会被卡在下一层循环上而无法真正的跳出所有循环"},{"page":41,"body":"**本节完整补充**\n\n**嵌套的循环：在循环里面还是循环**\n\n输出 100 以内的素数：\n\n* 将前一小节的程序稍加改造即可，此时 x 就不是读出来的，而是额外加一层 for 循环，在外层再从 2 开始到 100 依次把 x 的值赋给内层 for 循环，判断为素数的输出。这种循环体外面还是循环体，这种循环就称为嵌套循环(或者多重循环)。\n\n* 内外层循环变量不能相同。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int i;\n\n    for (x = 2; x < 100; x++) {\n        int isPrime = 1;\n        for (i = 2; i < x; i++) {\n            if (x % i == 0) {\n                isPrime = 0;\n                break;\n            }\n        }\n        if (isPrime == 1)\n            printf(\"%d \", x);\n    }\n\n    return 0;\n}\n```\n\n* 类似地，增加计数器变量`int cnt = 0`，对上面的程序稍微改造，也可以输出前 100 个素数，不过此时用 while 循环会更合适一点，可以参考章末习题——素数和。\n\n**从嵌套的循环中跳出**\n\n**100以内的素数**\n\n如何写程序输出100以内的素数?\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    //我们需要有一个循环，从 1 到 100\n    for(x=2; x<=100; x++){\n\t    int i;\n\t    int isPrime = 1; // x是素数\n\t    for ( i=2; i<x; i++ ) {\n\t        if ( x % i == 0 ) {\n\t            isPrime = 0;\n\t            break;\n\t        }\n\t    }\n\t    //当它是素数的时候就输出出来，不是素数就不要输出任何东西了\n\t    if ( isPrime == 1 ) {\n\t        printf(\"%d \", x);\n\t    }\n    }\n    return 0;\n}\n```\n\n**凑硬币**\n\n如何用1角、2角和5角的硬币凑出10元以下的金额？\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int one, two, five;\n    int exit = 0;\n    scanf(\"%d\", &x);\n    for ( one = 1; one < x*10; one++ ) {\n        for ( two = 1; two < x*10/2; two++ ) {\n            for ( five = 1; five < x*10/5; five++ ) {\n                if ( one + two*2 + five*5 == x*10 ) {\n                    printf(\"可以用%d个1角加%d个2角加%d个5角得到%d元\\n\",\n                           one, two, five, x);\n                    exit = 1;\n                    break;\n                }\n            }\n            if ( exit == 1 ) break;\n        }\n        if ( exit == 1 ) break;\n    }\n    return 0;\n}\n```"},{"page":42,"body":"**本节完整补充**\n\n**1.“接力”break**\n\n利用一角、两角、五角凑硬币，使得面额为 x 元，注意：只需要输出一种方案。\n\n```c\nint x;\nint one, two, five;\nint exit = 0;\n\nscanf(\"%d\", &x);\nfor (one = 1; one < x * 10; one++) {\n    for (two = 1; one < x * 10 / 2; two++) {\n        for (five = 1; five < x * 10 / 5; five++) {\n            if (one + two * 2 + five * 5 == x * 10) {\n                printf(\"可以用 %d 个 1 角加 %d 个 2 角加 %d 个 5 角得到 %d 元\\n\", one, two, five, x);\n                exit = 1;\n                break;\n            }\n        }\n        if (exit) break;\n    }\n    if (exit) break;\n}\n```\n\n* 计算机最擅长做这种枚举的问题，只要从 1 开始把三种硬币的组合方案都枚举一遍，所以利用 3 层 for 循环解决问题。为什么用 for 循环呢？因为其实枚举的上限，也就是每层循环次数上限是固定的，10x 个 1 角、5x 个 2 角、2x 个 5 角就已经足够凑出 x 元了，所以不用枚举更多。\n\n* 题目要求只输出一种方案，可是：break 只能跳出其所在的循环，如果利用 break 只能跳出最内层的 for 循环，仍然有可能输出很多组方案。于是我们设想，在每层都设一个 break，内层的 break 跳到外一层的 break 以此类推，这种方案称为**接力 break**。为了防止不满足条件也执行语句 break，所以我们要在每层的 break 外添加上一个条件判断，当最内层找到一种方案时，才把 exit 设为 1，这样才能正确地跳出外面所有的循环。\n\n**2.goto 语句**\n\n* goto 的语法如下，out 为**标号**，自定义其他合法的标识符也是可以的。当程序遇到 goto 语句，就会跳转到 out 所指的地方开始执行。\n\n```c\ngoto out;\n//\n//\nout:\n```\n\n* goto 语句被很多人认为是有害的，不过在上面凑硬币问题中，需要从最内层循环直接跳出多重循环的场合，goto 语句还是很方便的，但其他地方尽量不要使用 goto 语句。\n\n* **课后讨论 5.2.1：goto 怎么了？**\n\n这是扩展的讨论内容，初学者可以只是围观看看，看不懂也没关系。\n\n为什么课程中说 goto 的名声不好？用 goto 会怎么不好了？\n\n讨论：\n\n* 如果谨慎使用，goto 或许不会让人感到迷惑，然而一旦引入 goto 语句，它们就会遍布于代码之间，就像房子里有了白蚁。一旦允许使用 goto，那么坏的 goto 就会与好的混在一起，所以最好还是禁止使用它们。\n\n1.反对 goto 的人的观点\n\n* 普遍理由是：没有使用 goto 的代码就是高质量的代码，更容易证明其正确性。\n\n* 原因一：含有 goto 的代码很难安排好格式。用缩进来显示 goto 及其目标的逻辑结构非常难。\n\n* 原因二：使用 goto 的代码破坏编译器的优化特性。有些优化要求程序的控制流程位于不多的几个语句之间，而无条件的 goto 却使得流程变得很难分析，从而削弱了编译器优化代码的能力。\n\n* 原因三：在实践中，使用 goto 会违背代码应该严格自上而下运行的原则，破坏结构化设计风格。\n\n* 原因四：经常带来错误或者隐患，可能跳过变量的初始化以及重要的计算等。\n\n2.支持 goto 的人的观点：\n\n* goto 的支持者通常都会强调要在特定的场合下谨慎地使用 goto。\n\n* 条件一：如果使用位置恰当，goto 可以减少重复的代码。如果不能一致地修改，重复的代码就会带来问题。重复代码还增加了源代码和可执行文件的体积。与代码重复所带来的问题相比，goto 所具有的一些负面影响就显得不那么重要了。\n\n* 条件二：goto 在分配资源、使用资源后再释放资源的子程序里非常有用。通过使用 goto，你可以在一段代码里执行清理操作。这将减少你在某个检测错误的位置发现问题后忘记释放资源的可能。\n\n* 条件三：在某些情况下，使用 goto 会让代码的运行速度更快，体积更小。\n\n**3.循环应用**\n\n**break VS continue**\n\n1. break:跳出循环\n\n2. continue:跳过循环这一轮剩下的语句进入下一轮\n\n3. 都只能对它所在的那层循环生效\n\n**嵌套的循环**\n\n1. 意思：循环的里面还是循环\n\n2. 嵌套循环时的break只会跳出当前所在的循环，如果嵌套了多层循环就会被卡在下一层循环上而无法真正的跳出所有循环\n\n**100以内的素数**\n\n如何写程序输出100以内的素数?\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    //我们需要有一个循环，从 1 到 100\n    for(x=2; x<=100; x++){\n\t    int i;\n\t    int isPrime = 1; // x是素数\n\t    for ( i=2; i<x; i++ ) {\n\t        if ( x % i == 0 ) {\n\t            isPrime = 0;\n\t            break;\n\t        }\n\t    }\n\t    //当它是素数的时候就输出出来，不是素数就不要输出任何东西了\n\t    if ( isPrime == 1 ) {\n\t        printf(\"%d \", x);\n\t    }\n    }\n    return 0;\n}\n```"},{"page":43,"body":"**本节完整补充**\n\n**前 n 项求和**\n\n典例一、求 1 + 1/2 + 1/3 + ... 的前 N 项之和：\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    double sum = 0;\n    int n;\n    scanf(\"%d\", &n);\n\n    for (int i = 1; i <= n; i++)\n        sum += 1.0/i;\n\n    printf(\"sum = %.6f\", sum);\n\n    return 0;\n}\n```\n\n典例二、求交错序列 1-2/3+3/5-4/7+5/9-6/11+... 的前 N 项之和。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    double sum = 0, denominator = 1, numerator = 1;\n    int n;\n    scanf(\"%d\", &n);\n    double flag = 1;\n\n    for (int i = 1; i <= n; i++) {\n        sum += flag * numerator / denominator;\n        flag = -flag;\n        numerator++;\n        denominator += 2;\n    }\n\n    printf(\"%.3f\\n\", sum);\n\n    return 0;\n}\n```\n\n前 n 项求和的核心不是背公式，而是把“第 i 项是什么”写成表达式，然后让循环把每一项累加。先看两个差异明显的例子：\n\n1. 求 `1 + 1/2 + 1/3 + ... + 1/n`，第 i 项是 `1.0 / i`。\n\n2. 求 `1 - 2/3 + 3/5 - 4/7 + ...`，分子从 1 递增，分母从 1 开始每次加 2，符号每项取反。\n\n第二题的循环可以这样组织：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    int n;\n    double sum = 0.0;\n    int numerator = 1;\n    int denominator = 1;\n    int sign = 1;\n\n    scanf(\"%d\", &n);\n    for (int i = 1; i <= n; i++) {\n        sum += sign * numerator / (double)denominator;\n        numerator++;\n        denominator += 2;\n        sign = -sign;\n    }\n    printf(\"%.3f\\n\", sum);\n    return 0;\n}\n```\n\n关键点在 `(double)denominator`，它保证除法按浮点规则执行；如果分子、分母和 sign 都是整数，`sum` 即使声明为 double，也会在整数部分完成后才转换。`sign` 每轮取反，避免写 `if (i % 2)` 分支，但两种写法都能表达交替符号。\n\n检查边界：n=1 时只执行一次，n=2 时先加第一项再取反；n 很大时，可以先打印每一项、再观察总和是否稳定收敛，不要只看最后一位小数。"},{"page":44,"body":"**本节完整补充**\n\n**整数分解**\n\n将一个正整数按数位顺序分解，中间每个数位之间有空格间隔，但最后一位数没有空格。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    scanf(\"%d\", &x);\n\n    x = 0;\n    int mask = 1;\n    int t = x;\n    while (t > 9) {\n        t /= 10;\n        mask *= 10;\n    }\n\n    while (mask > 0) {\n        int d = x / mask;\n        printf(\"%d\", d);\n        if (mask > 9)\n            printf(\" \");\n        x %= mask;\n        mask /= 10;\n    }\n\n    return 0;\n}\n```\n\n* 第一个 while 的目的是获取和原来数一样位数的 mask\n\n* 第二个 while 目的是通过原数除以 mask 依次获取各个数位的值，这个过程中记得修改相应的变量的值，为下一轮循环做准备。\n\n* **章末习题**\n\n若变量已正确定义，以下 while 循环正常结束时，**累加到 pi 的最后一项** item 的值满足____\n\n```c\nflag = 1;\ndenominator = 1;\nitem = 1.0;\npi = 0;\nwhile (fabs(item) >= 0.0001) {\n    pi = pi + item;\n    flag = -flag;\n    denominator = denominator + 2;\n    item = flag * 1.0 / denominator;\n}\n```\n\n解析：\n\n* 注意 item 在累加到 pi 的语句前还是后是有区别，这里累加到 pi 的最后一项 item 的值满足 item>=0.0001，如果 item<0.0001，下一次条件判断就会出循环，不会累加到 pi 上。\n\n* **章末习题：素数和（5 分）**\n\n**题目内容：**\n\n我们认为 2 是第一个素数，3 是第二个素数，5 是第三个素数，依次类推。\n\n现在，给定两个整数 n 和 m，0<n<=m<=200，你的程序要计算第 n 个素数到第 m 个素数之间所有的素数的和，包括第 n 个素数和第 m 个素数。\n\n**输入格式:**\n\n两个整数，第一个表示 n，第二个表示 m。\n\n**输出格式：**\n\n一个整数，表示第 n 个素数到第 m 个素数之间所有的素数的和，包括第 n 个素数和第 m 个素数。\n\n**输入样例：**\n\n2 4\n\n**输出样例：**\n\n15\n\n解答：\n\n* 注意每处理完内层一个素数，都要在外层循环把`isPrime`置为 1，不要把循环初始化语句放错了位置，混淆了外层循环和内层循环的初始化。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int count = 0;\n    int isPrime;\n    int n, m;\n    int sum = 0;\n\n    scanf(\"%d %d\", &n, &m);\n\n    int i, j;\n    j = 2;\n    while (count < m) {\n        isPrime = 1;\n        for (i = 2; i < j; i++) {\n            if (j % i == 0) {\n                isPrime = 0;\n                break;\n            }\n        }\n        if (isPrime == 1) {\n            count++;\n            if (count >= n && count <= m)\n                sum += j;\n        }\n        j++;\n    }\n\n    printf(\"%d\", sum);\n\n    return 0;\n}\n```\n\n* **章末习题：念整数（5 分）**\n\n你的程序要读入一个整数，范围是[-100000,100000]。然后，用汉语拼音将这个整数的每一位输出出来。\n\n如输入 1234，则输出：\n\nyi er san si\n\n注意，每个字的拼音之间有一个空格，但是最后的字后面没有空格。当遇到负数时，在输出的开头加上“fu”，如-2341 输出为：\n\nfu er san si yi\n\n一个整数，范围是[-100000,100000]。\n\n表示这个整数的每一位数字的汉语拼音，每一位数字的拼音之间以空格分隔，末尾没有空格。\n\n-30\n\nfu san ling\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int mask = 1;\n    scanf(\"%d\", &x);\n\n    if (x < 0) {\n        printf(\"fu \");\n        x = -x;\n    }\n\n    int t = x;\n    while (t > 9) {\n        t /= 10;\n        mask *= 10;\n    }\n    while (mask > 0) {\n        int d = x / mask;\n        switch (d) {\n            case 0: printf(\"ling\"); break;\n            case 1: printf(\"yi\"); break;\n            case 2: printf(\"er\"); break;\n            case 3: printf(\"san\"); break;\n            case 4: printf(\"si\"); break;\n            case 5: printf(\"wu\"); break;\n            case 6: printf(\"liu\"); break;\n            case 7: printf(\"qi\"); break;\n            case 8: printf(\"ba\"); break;\n            case 9: printf(\"jiu\"); break;\n        }\n        if (mask > 9)\n            printf(\" \");\n        x %= mask;\n        mask /= 10;\n    }\n\n    return 0;\n}\n```\n\n**整数的分解**\n\n1.一个整数是由1-多位数字组成的，如何分解出整数的各个位上的数字，然后加以计算\n\n2.对一个整数做%10的操作，就得到它的个位数\n\n3.对一个整数做/10的操作，就去掉了它的个位数\n\n4.然后再对2的结果做%10,就得到原来数的十位数\n\n....以此类推\n\n**数的逆序**\n\n1.输出一个正整数，输出逆序的数\n\n2.结尾的0的处理"},{"page":45,"body":"**本节完整补充**\n\n**求最大公约数**\n\n**1.枚举**\n\n* 可以通过枚举法来利用计算机求最大公约数，枚举步骤如下\n\n* ① 设 t 为 2；\n\n* ② 如果 u 和 v 都能被 t 整除，则记下这个 t\n\n* ③ t 加 1 后重复第 2 步，直到 t 等于 u 或 v\n\n* ④ 那么，曾经记下的最大的可以同时整除 u 和 v 的 t 就是 gcd(Greatest Common Divisor)\n\n```c\nint a, b;\n\nscanf(\"%d %d\", &a, &b);\n\nint ret = 0;\nint i;\nfor (i = 1; i < ((a < b)? a : b); i++) {\n    if (a % i == 0 && b % i == 0) {\n        ret = i;\n    }\n}\n\nprintf(\"%d 和 %d 的最大公约数是 %d。\\n\", a, b, ret);\n```\n\n* 该程序如果没有最大公约数，那程序也会返回 ret = 1，这是我们所希望的。\n\n* 上面的算法可以解决问题，但是效率不高，因为要枚举所有可能的数。\n\n**2.辗转相除法**\n\n* 辗转相除法更难理解，但是效率更高，算法思想如下：\n\n* ①如果 b 等于 0，则计算结束，最大公约数就是 a\n\n* ②否则，计算 a 除以 b 的余数，让 a 等于 b，而 b 等于结果的余数\n\n* ③回到第一步\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int a, b;\n    int t;\n    scanf(\"%d %d\", &a, &b);\n    while (b != 0) {\n        t = a % b;\n        a = b;\n        b = t;\n        printf(\"a = %d, b = %d, t = %d\\n\", a, b, t);\n    }\n    printf(\"gcd = %d\", a);\n\n    return 0;\n}\n```\n\n* 利用代码内插入`printf()`可以方便的看到每一步的执行结果和变量变化，也属于调试代码的一种方法。\n\n* 两个数最小公倍数 lcm(Least Common Multiple) 和 gcm 的乘积为这两个数的乘积（证见数论或离散数学）。\n\na*b=GCD(a,b)*LCM(a,b)\n\n1. 输入两个数a和b，输出它们最大的公约数\n\n2. 输入：12，18\n\n3. 输出：6\n\n4. 枚举方法:过于麻烦，耗费较多资源\n\n```c\n#include <stdio.h>\n\nint main()\n{\nint a,b;\nint min;\nscanf(\"%d %d\", &a, &b);\nif ( a<b ) {\nmin = a;\n} else {\nmin = b;\n}\nint ret = 0;\nint i;\nfor ( i = 1; i < min; i++ ) {\nif ( a%i == 0 ) {\nif ( b%i == 0 ) {\nret = i;\n}\n}\n}\nprintf(\"%d和%d的最大公约数是%d.\\n\", a, b, ret);\nreturn 0;\n}\n```\n\n**辗转相除法**\n\n1. 如果b等于0，计算结束，a就是最大的公约数;\n\n2. 否则，计算a除以b的余数，让a等于b，而b等于那个余数；\n\n3. 回到第一步\n\n```c\nint a,b;\nint t;\n\nscanf(\"%d %d\",&a,&b);\nint origa = a;\nint origb = b;\nwhile( b != 0 ){\n    t = a%b;\n    a = b;\n    b = t;\n}\nprintf(\"%d和%d的最大公约数是%d\",orida,origb,a);\n```"}];
