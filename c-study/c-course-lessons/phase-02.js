window.COURSE_CHUNKS=window.COURSE_CHUNKS||{};window.COURSE_CHUNKS["基础"]=[{"page":10,"body":"**本节完整补充**\n\n**第二周：计算**\n\n**2.1变量**\n\n**算找零**\n\n如何能在程序运行时输入那个数字，然后计算输出结果？\n\n需要：1.有地方放输入的数字-printf(输出)，能够提示哪里输入数字\n\n2.有办法输入数字-scanf(输入)，提供输入数字的通道\n\n3.输入的数字能参与计算\n\n**如何输入**\n\n输入也在终端窗口中\n\n输入就是以行为单位进行的，行的结束标志就是你按下回车键。在你按下回车之前，你的程序是不会读到任何东西的\n\n**变量**\n\nint price = 0;\n\n这一行定义了一个变量。变量的名字是price，类型是int，初始值是0\n\n变量是一个保存数据的地方，当我们需要在程序里保存数据时，比如上面的例子中要记录用户输入的价格，就需要一个变量来保存它。用一个变量保存了数据，它才能参加到后面的计算中，比如计算找零\n\n**变量定义**\n\n变量定义的一边形式：\n\nint price;\n\nint amount;\n\nint price,amount;\n\n**变量的名字**\n\n1.变量需要一个名字，变量的名字是一种\"标识符\"，意思是它是用来识别这个和那个的不同的名字\n\n2.标识符有标识符的构造规则。基本的原则是：标识符只能由字母、数字和下划线组成，数字不可以出现在第一个位置上，c语言的关键字（有的地方叫他们保留字），不可以用作标识符\n\n第二周是一张完整的学习地图：先让程序知道“用户输入了价格”，再把价格保存到变量，接着用表达式计算找零，最后用 `printf` 把结果格式化输出。每个知识点都围绕这个最小任务展开，不要孤立地记语法。\n\n这条主线上有三个容易混淆的点：\n\n1. 定义变量和给变量赋值不是同一件事。`int price;` 只是申请了一个叫 `price` 的存储位置，`price = 0;` 才把值放进去。\n\n2. `scanf(\"%d\", &price)` 中的 `&` 告诉函数“把读到的值写到地址对应的变量里”，而不是把地址本身当作整数。\n\n3. 输入是以行为单位进入缓冲区，按下回车后程序才会读到内容。格式串中的 `%d` 会跳过空白字符，因此一行输入多个数通常也能工作，但不要依赖终端之外不存在的东西。\n\n一个可以反复练习的小程序是用变量完成“输入商品价格，输入付款金额，输出找零”。每一步都问自己：\n\n- 数据从哪里来？\n\n- 数据保存在哪个变量？\n\n- 计算时有没有类型转换问题？\n\n- 输出格式是否和题目要求一致？\n\n- 如果用户输入负数或金额不够，程序会怎样？\n\n用这三个问题检查代码，会比记住十个语法细节更接近编程的本质。\n\n把这一讲拆成一条可复用的数据流水线：`scanf` 从终端读入数据，变量负责保存，表达式负责转换，`printf` 把结果送回终端。每一步都对应一个可以单独检查的问题，读入失败不会因为后面写对了就自动成功。\n\n启动程序后，先问四个问题：用户输入什么？程序把输入放在哪里？计算步骤是什么？输出格式是否和题目完全一致？以“输入价格、付款 100 元、输出找零”为例，代码顺序是：定义 `price` 和 `paid`，读入 `price`，计算 `paid - price`，最后打印。这里 `&price` 不是取地址给用户看，而是告诉 `scanf` 把转换后的整数写入哪个存储位置；若写成 `scanf(\"%d\", price)`，程序会把输入值当作地址使用，轻则得到错误结果，重则崩溃。\n\n边界测试同样重要：价格大于付款金额会让找零变成负数；价格等于 100 会得到 0；输入字母或没有输入时，`scanf` 可能返回失败并留下未定义行为。把 `scanf` 的返回值检查加入代码，用户输入错误时至少能得到一个可观察的失败结果。\n\n练习时先在纸上列出“输入→变量→计算→输出”的状态表，逐项检查数据类型和格式符，再运行程序。这样建立的习惯会一直延续到指针、函数和文件输入。"},{"page":11,"body":"**本节完整补充**\n\n**1.变量**\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    const int AMOUNT = 100;           /*Line 1*/\n    int price = 0;                    /*Line 2*/\n\n    printf(\"请输入金额（元）：\");\n    scanf(\"%d\", &price);\n\n    int change = AMOUNT - price;\n\n    printf(\"找您 %d 元。\\n\", change);\n\n    return 0;\n}\n```\n\n* 受到之前`printf()`做计算的启发，我们想到能否不要每次都输入`printf()`，而是只给两个数字就能计算呢？这就要利用到变量来实现了。\n\n**变量的设想与定义**\n\n* 我们需要：① 有地方放输入的数字 ② 有办法输入数字 ③ 输入的数字能参与计算\n\n* 上述程序`int price = 0`，定义了一个变量，变量的名字是 price，类型是 int，初始值是 0。\n\n* 变量就是一个保存数据的地方，只有用变量保存了数据，这个数据才能参与到后面的运算中去。\n\n* 变量定义的一般形式是：\n\n* 变量需要一个名称，变量的名字就是一种“标识符”\n\n* 标识符有标识符的构造规则，基本规则是只能由**下划线、字母、数字**组成，数字不能出现在第一个位置上，C 语言的 关键字 (或叫 保留字 )，不可用做标识符。\n\n**如何输入**\n\n1. 输入也在终端窗口中。\n\n2. 输入是以行为单位进行的，行结束标志就是你按下回车键，在你按下回车键之前程序不会读到任何内容。\n\n3. C 语言中，数据的输入和输出都是通过函数调用实现的。\n\n**变量的赋值和初始化**\n\n* `int price = 0`除了定义了一个新变量，还是一个赋值语句，`=`表示将右边的值赋值给左边的变量。因为这个语句是在定义变量时赋值，所以又称为**初始化**。对 C 语言来说，赋值和初始化差异并不大，但对其他语言两者差异可能会比较大。\n\n```c\n<类型名称><变量名称> = <初始值>  /*初始化操作*/\n```\n\n* 注意，一行如果有多个变量要初始化，你必须一个一个赋值，而不能只赋给一个，那样其他变量值不确定。\n\n* 赋值与数学不同，a=b 在数学中表示关系，即 a、b 的值一样，但在 C 语言中表示动作，把 b 的值赋给 a。关系是静态的，动作是动态的，前者 a=b 和 b=a 是等价的，但后者意义却截然相反。\n\n* C 语言不强制要求所有变量都在定义的地方做初始化，但是所有变量在第一次被使用(出现在赋值运算符的右侧)之前，应该被赋值一次。\n\n* 赋值“=”也是运算符，有运算符的式子就叫**表达式**。\n\n**变量类型**\n\n* C 是一种有类型的语言，所有变量在使用之前必须定义或声明，所有的变量必须有确定的数据类型。数据类型表示变量可以存放什么样的数据，变量中也只能存放指定类型的数据，程序运行过程中也不能改变变量类型。\n\n* ANSI C 只能在开头定义变量，C99 支持任何要使用时定义变量即可。\n\n**2.1变量**\n\n**算找零**\n\n如何能在程序运行时输入那个数字，然后计算输出结果？\n\n需要：1.有地方放输入的数字-printf(输出)，能够提示哪里输入数字\n\n2.有办法输入数字-scanf(输入)，提供输入数字的通道\n\n3.输入的数字能参与计算\n\n输入也在终端窗口中\n\n输入就是以行为单位进行的，行的结束标志就是你按下回车键。在你按下回车之前，你的程序是不会读到任何东西的\n\n**变量**\n\nint price = 0;\n\n这一行定义了一个变量。变量的名字是price，类型是int，初始值是0\n\n变量是一个保存数据的地方，当我们需要在程序里保存数据时，比如上面的例子中要记录用户输入的价格，就需要一个变量来保存它。用一个变量保存了数据，它才能参加到后面的计算中，比如计算找零\n\n**变量定义**\n\n变量定义的一边形式：\n\nint price;\n\nint amount;\n\nint price,amount;\n\n**变量的名字**\n\n1.变量需要一个名字，变量的名字是一种\"标识符\"，意思是它是用来识别这个和那个的不同的名字\n\n2.标识符有标识符的构造规则。基本的原则是：标识符只能由字母、数字和下划线组成，数字不可以出现在第一个位置上，c语言的关键字（有的地方叫他们保留字），不可以用作标识符\n\n**赋值和初始化**\n\n这一行，定义了一个变量。变量的名字是price，类型是int，初始值是0\n\nprice = 0是一个式子，这里的\"=\"是一个赋值运算符，表示将\"=\"右边的值赋给左边的变量\n\n**赋值**\n\n和数学不同，a=b在数学中表示关系，即a和b的值是一样的；而在程序设计中，a=b表示要求计算机做一个动作：将b的值赋给a。关系是静态的，而动作是动态的。在数学中，a=b和b=a是等价的，而在程序设计中，两者的意思是完全相反的\n\n**初始化**\n\n当赋值发生在定义变量的时候，就像给变量price=0那样，就是变量的初始化。虽然c语言并没有强制要求所有的变量都在定义的地方做初始化，但是所有的变量在第一次被使用(出现在赋值运算符的右边)之前应该被赋值一次\n\n问：如果没有初始化？答：变量会出现一个随机数，可能很大也可能很小不固定但一定不会是你想要的那个数\n\n**变量初始化**\n\n= ;\n\nint amount = 100;\n\n组合变量定义的时候，也可以在这个定义中单独给单个变量赋初值，如：int price = 0,amount = 0;\n\n**读整数**\n\nscanf(\"%d\"，&price);\n\n要求scanf这个函数读入下一个整数，读到的结果赋值给变量price\n\n小心price前面的&\n\n**表达式**\n\n“=”是赋值运算符，有运算符的式子就叫做表达式\n\nprice = 0;\n\nchange = 100-price;\n\n这一行，定义了一个变量。变量的名字是price，类型是int，初始值是0.\n\nC是一种有类型的语言，所有的变量在使用之前必须定义或者声明，所有的变量都必须具有确定的数据类型。数据类型表示在变量中可以存放什么样的数据，变量中只能存放指定类型的数据，程序运行过程中也不能改变变量的类型\n\nc99可以在任何地方定义变量，ANSI C只能在代码开头的地方定义变量\n\n**常量**\n\nint change = 100 - price;\n\n固定不变的数，是常数。直接写在程序里，我们称为直接量(literal)\n\n更好的方式是定义一个常量\n\nconst int AMOUNT = 100；\n\n**const**\n\nconst是一个修饰符，加在int的前面，用来给这个变量加上一个const(不变的)的属性。这个const的属性表示这个变量的值一旦初始化，就不能再修改了\n\nint change = AMOUNT - price;\n\n如果试图对常量做出修改，把他放在赋值运算符的左边，就会被编译器报错\n\n**tips**\n\n程序要求读入多个数字时，可以在一行输入，中间用空格分开，也可以在多行输入\n\n在scanf的格式字符串中有几个%d，它就等待用户输入一个整数，当然，字符串后面也需要对应有那么多整数\n\n两个整数运算的结果只能是整数\n\n例如：10/3*3=9 10跟10.0在C中是完全不同的数\n\n10.0是浮点数\n\n**浮点数**\n\n1.带小数点的数值，浮点这个词的本意就是指小数点是浮动的，是计算机内部表达非整数(包括分数和无理数)的一种方式。另一种方式叫做定点数。人们借用浮点数这个词来表达所有带小数点的数。\n\n2.当浮点数和整数放到一起运算时，C会将整数转换成浮点数，然后进行浮点数的运算\n\n**double**\n\n1.inch是定义为int类型的变量，如果把int换成double，我们就把它改成double类型的浮点数变量了。\n\n2.double的意思是\"双\"，它本来是\"双精度浮点数\"的第一个单词，人们用来表示浮点数类型。除了double，还有float(意思就是浮点)表示单精度浮点数\n\n在输入的时候数据类型定义为：%lf\n\n在输出的时候数据类型定义为：%f\n\n**数据类型**\n\n整数：int printf(\"%d\"，...) scanf(\"%d\",...)\n\n带小数点的数： double printf(\"%f\") scanf(\"%lf\",....)\n\n**整数**\n\n整数类型不能表达有小数部分的数，整数和整数的运算结果还是整数。计算机里会有纯粹的整数这种奇怪的东西，是因为整数的运算比较快，而且占地方也小。其实人们日常生活中大量做的还是纯粹整数的计算，所以整数的用处还是很大的。"},{"page":12,"body":"**本节完整补充**\n\n**变量的赋值和初始化**\n\n* `int price = 0`除了定义了一个新变量，还是一个赋值语句，`=`表示将右边的值赋值给左边的变量。因为这个语句是在定义变量时赋值，所以又称为**初始化**。对 C 语言来说，赋值和初始化差异并不大，但对其他语言两者差异可能会比较大。\n\n```c\n<类型名称><变量名称> = <初始值>  /*初始化操作*/\n```\n\n* 注意，一行如果有多个变量要初始化，你必须一个一个赋值，而不能只赋给一个，那样其他变量值不确定。\n\n* 赋值与数学不同，a=b 在数学中表示关系，即 a、b 的值一样，但在 C 语言中表示动作，把 b 的值赋给 a。关系是静态的，动作是动态的，前者 a=b 和 b=a 是等价的，但后者意义却截然相反。\n\n* C 语言不强制要求所有变量都在定义的地方做初始化，但是所有变量在第一次被使用(出现在赋值运算符的右侧)之前，应该被赋值一次。\n\n* 赋值“=”也是运算符，有运算符的式子就叫**表达式**。\n\n**变量类型**\n\n* C 是一种有类型的语言，所有变量在使用之前必须定义或声明，所有的变量必须有确定的数据类型。数据类型表示变量可以存放什么样的数据，变量中也只能存放指定类型的数据，程序运行过程中也不能改变变量类型。\n\n* ANSI C 只能在开头定义变量，C99 支持任何要使用时定义变量即可。\n\n赋值是 C 语言最常见的动作之一，但它并不是数学里的“相等”。`int x = 10;` 可以读作“把 10 这个值放入名为 x 的变量”；`x = x + 1;` 则先计算右边，再把新值覆盖旧值。数学中的等式是对称的，赋值却具有方向性：左边必须是可修改的变量，右边可以是常量、变量或表达式。\n\n要特别区分三种情况：\n\n- `int a = 1, b = 2;`：两个变量都定义，但只有 a 被初始化。\n\n- `int a; a = 1;`：先定义，后赋值。\n\n- `int a, b;`：两个变量都只有定义，使用前必须赋值。\n\n很多初学者的“奇怪输出”来自未初始化变量。例如：\n\n```c\nint a;\nprintf(\"%d\\n\", a);\n```\n\nC 不保证这里打印 0，打印出来的可能是上一次函数调用留下的值，也可能是无效的垃圾值。编译器开启 `-Wall` 后会提示未初始化变量，看到警告要把它当作错误处理。\n\n在写程序时，把“先定义，再初始化，最后使用”作为三步走。对于需要累加的计数器，初始化通常是 0；对于需要求乘积的变量，初始化通常是 1；对于需要表示失败的标记，则要选择与业务含义一致的初值。调试时用断点观察赋值前后的值，就能看到赋值是“覆盖”，而不是“同时成立”。\n\n定义、初始化和赋值是三个常在同一个代码里出现的动作，必须区分：定义决定变量叫什么、是什么类型；初始化在定义时给第一个值；赋值在变量已经存在后，把右边的计算结果复制到左边的存储位置。`int a = 1, b;` 中只有 a 被初始化，b 仍然需要在使用前赋值。\n\n未初始化变量不是“一定等于 0”，而是“当前值不确定”。它可能来自上一次函数调用留在栈上的数据，也可能是任意位模式。编译器开启警告后提示 `may be used uninitialized` 时，不要用“本地运行没有出错”跳过，应给变量一个明确初值。初始化也不是所有场景都相同：计数器从 0 开始，累乘积从 1 开始，数组下标从 0 开始，标志位则要根据业务含义选择。\n\n命名变量时建议遵循三条规则：名称能表达含义，例如 `price`、`paid`、`change`；不使用关键字；不要使用容易混淆的字母或中文。C 标识符以字母或下划线开头，后面可以是字母、数字和下划线，大小写不同代表不同变量。\n\n调试赋值问题时，用断点观察变量在每条语句前后实际保存的值，而不是猜“它应该等于多少”。单步执行 `a = 1; a = a + 2; a = a * 3;`，会看到赋值先把右侧求值，再覆盖左侧，这正是程序状态逐步演化的原因。"},{"page":13,"body":"**本节完整补充**\n\n**如何输入**\n\n1. 输入也在终端窗口中。\n\n2. 输入是以行为单位进行的，行结束标志就是你按下回车键，在你按下回车键之前程序不会读到任何内容。\n\n3. C 语言中，数据的输入和输出都是通过函数调用实现的。\n\n**变量的赋值和初始化**\n\n* `int price = 0`除了定义了一个新变量，还是一个赋值语句，`=`表示将右边的值赋值给左边的变量。因为这个语句是在定义变量时赋值，所以又称为**初始化**。对 C 语言来说，赋值和初始化差异并不大，但对其他语言两者差异可能会比较大。\n\n```c\n<类型名称><变量名称> = <初始值>  /*初始化操作*/\n```\n\n* 注意，一行如果有多个变量要初始化，你必须一个一个赋值，而不能只赋给一个，那样其他变量值不确定。\n\n* 赋值与数学不同，a=b 在数学中表示关系，即 a、b 的值一样，但在 C 语言中表示动作，把 b 的值赋给 a。关系是静态的，动作是动态的，前者 a=b 和 b=a 是等价的，但后者意义却截然相反。\n\n* C 语言不强制要求所有变量都在定义的地方做初始化，但是所有变量在第一次被使用(出现在赋值运算符的右侧)之前，应该被赋值一次。\n\n* 赋值“=”也是运算符，有运算符的式子就叫**表达式**。\n\n**变量类型**\n\n* C 是一种有类型的语言，所有变量在使用之前必须定义或声明，所有的变量必须有确定的数据类型。数据类型表示变量可以存放什么样的数据，变量中也只能存放指定类型的数据，程序运行过程中也不能改变变量类型。\n\n* ANSI C 只能在开头定义变量，C99 支持任何要使用时定义变量即可。\n\n**scanf()**\n\n```c\n    scanf(\"%d\", &price);\n    scanf(\"%d %d\", &a, &b);\n    scanf(\"%d,%d\", &a, &b);\n```\n\n* `scanf()`是 C 语言中的一个输入函数,用于从标准输入设备（通常是键盘）读取数据。**它的全称是“scan formatted”**,表示按照指定的格式扫描输入数据。\n\n① 要求这个函数读入下一个整数，读到的结果赋值给变量 price。\n\n② 小心 price 前面的 &\n\n③ 特别注意，`scanf()`两个双引号之间，是你要输入的内容，而不是它会展示的内容，比如中间如果有逗号，你两个整数输入中间也要输入逗号，如果格式控制符是连续的，你也必须连续输入，`scanf()`才能正确运行，。\n\n④ `scanf()`可以读多个变量\n\n⑤ 如果格式控制符和变量类型不对应，比如 double 类型用了 %f 输入，则`scanf()`会输出 0。\n\n```C\n#include <stdio.h>\n\nint main()\n{\n    int a;\n    int b;\n\n    printf(\"请输入两个整数：\");\n    scanf(\"%d %d\", &a, &b);\n    printf(\"%d + %d = %d\\n\", a, b, a + b);\n\n    return 0;\n}\n```\n\n**读整数**\n\nscanf(\"%d\"，&price);\n\n要求scanf这个函数读入下一个整数，读到的结果赋值给变量price\n\n小心price前面的&\n\n**表达式**\n\n“=”是赋值运算符，有运算符的式子就叫做表达式\n\nprice = 0;\n\nchange = 100-price;"},{"page":14,"body":"**本节完整补充**\n\n**常量**\n\n```c\nint change = 100 - price;\nconst int AMOUNT = 100;    //更好的方式\n```\n\n* 固定不变的数，是常数。直接写在程序里，我们称作直接量 (literal)。\n\n* 更好的方式，是定义一个常量，如上面第二行代码。\n\n* 我们把出现在程序里面莫名其妙的数字叫 Magic Number（魔术数字），别人很难快速理解其含义。\n\n**const**\n\n* const 是一个修饰符，加在 int 的前面，用来给这个变量加上一个 const（不变的）属性。这个 const 属性表示这个变量只要初始化了，就不能再修改了。一般用**全大写字母**突出强调该变量是 const 变量。\n\n```c\nint change = AMOUNT -price;\n```\n\n* 如果你试图对常量做修改，把它放在赋值运算符的左边，编译器会报错。\n\n```c\n[Error] assignment of read-only variable 'a'\n```\n\n* 如果我们想让用户输入 AMOUNT 初始值，而不是使用固定初始值，可以使用下述代码。\n\n```c\n    int amount = 100;           /*给个初值没问题*/\n    int price = 0;\n\n    printf(\"请输入金额（元）：\");\n    scanf(\"%d\", &price);\n\n    printf(\"请输入票面（元）：\");\n    scanf(\"%d\", &amount);\n\n    int change = amount - price;\n\n    printf(\"找您 %d 元。\\n\", change);\n```\n\nint change = 100 - price;\n\n固定不变的数，是常数。直接写在程序里，我们称为直接量(literal)\n\n更好的方式是定义一个常量\n\nconst int AMOUNT = 100；\n\nconst是一个修饰符，加在int的前面，用来给这个变量加上一个const(不变的)的属性。这个const的属性表示这个变量的值一旦初始化，就不能再修改了\n\nint change = AMOUNT - price;\n\n如果试图对常量做出修改，把他放在赋值运算符的左边，就会被编译器报错\n\n**tips**\n\n程序要求读入多个数字时，可以在一行输入，中间用空格分开，也可以在多行输入\n\n在scanf的格式字符串中有几个%d，它就等待用户输入一个整数，当然，字符串后面也需要对应有那么多整数\n\n两个整数运算的结果只能是整数\n\n例如：10/3*3=9 10跟10.0在C中是完全不同的数\n\n10.0是浮点数\n\n常量的价值不在于“少写几个字”，而在于给一个数字赋予可读的名称。`100` 可能代表票面金额、考试满分、数组大小或税率；如果它直接散落在代码里，后人只能猜。`const int AMOUNT = 100;` 则明确告诉读者：这是一个不会改变的业务常量，它的含义是金额。\n\n使用 const 时要分清“初始化”和“赋值”：\n\n- `const int AMOUNT = 100;` 合法，这是定义时的初始化。\n\n- `AMOUNT = 120;` 非法，const 变量不能出现在赋值号左边。\n\n- `const int AMOUNT;` 可能不会被编译器接受，因为它没有初始值，后续也无法赋予一个确定值。\n\n建议把一个程序中的“可变数据”和“业务规则”分开。价格、用户输入、循环计数是可变的，用普通变量；折扣率、税率、最大数组长度等固定值，用 const 或宏表达。const 变量仍然有类型，适合做类型检查；宏替换通常只是文本替换，使用不当会造成优先级和副作用问题，初学阶段优先用 const。\n\n再看一个容易踩坑的例子：`10 / 3 * 3` 在 C 中先做整数除法，结果是 `9`；`10.0 / 3 * 3` 则得到浮点数结果。一个数字后面是否有小数点，不只影响书写，还决定它属于整数还是浮点数。"},{"page":15,"body":"**本节完整补充**\n\n**2.计算**\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    printf(\"请分别输入身高的英尺和英寸，\"\n        \"如输入\\\"5 7\\\"表示 5 英尺 7 英寸：\");\n\n    double foot;\n    double inch;\n\n    scanf(\"%lf %lf\", &foot, &inch);\n\n    printf(\"身高是 %f 米。\\n\",\n        ((foot + inch / 12) * 0.3048));\n\n    return 0;\n}\n```\n\n**C 语言世界的数字运算**\n\n* 两个整数的运算结果只能是整数\n\n```c\n10/3*3 = 9\n```\n\n* 10 和 10.0 在 C 中是完全不同的数\n\n```c\n10.0      //是浮点数\n```\n\n**浮点数**\n\n* 带小数点的数值。浮点意思就是这个数小数点是可以浮动的，是计算机内部表达数字的一种方式，另一种方式叫**定点数**。不过 C 语言中你不会遇到定点数，人们借用浮点数这个词来表达所有带小数点的数。\n\n* 但整数和浮点数放在一起运算时，C 语言会将整数转换为浮点数，然后进行浮点数的运算。\n\n* 根据上一条，我们发现只要把其中一个变量设为浮点数并参与运算，表达式的结果就会转换为浮点数了，但需要注意，将整型变量赋值`sum = 0.5;`sum 的结果依然为 0。\n\n```c\n//执行以下程序段，sum 的值是 1。\nint i, sum;\nsum = 0;\nfor (i = 1; i <= 2; i++)\n    sum = sum + 1.0/i;\n//执行以下程序段，sum 的值是 0。\nint i;\ndouble sum;\nsum = 0;\nfor (i = 2; i <= 4; i = i + 2)\n    sum = sum + 1/i;\n```\n\n**double**\n\n* double 的意思是“双”，它本来是“双精度浮点数”的第一个单词，人们用来表示浮点数类型。除了 double，还有 float，表示单精度浮点数。\n\n* double 类型的变量，`scanf()`和`printf()`函数都需要更改相应格式控制符。\n\n```c\nint a; float b; double c;\nscanf(\"%d\", &a); printf(\"%d\", a);\nscanf(\"%f\", &b); printf(\"%f\", b);\nscanf(\"%ld\", &c); printf(\"%f\", c);\n```\n\n**整数**\n\n* 数据类型不能表达小数部分的数，整数和整数的运算结果还是整数。\n\n* 计算机里面会有纯粹的整数，因为它运算快，占空间小。而现实生活中也在大量做整数的运算，所以整数的作用还是很大的。\n\n**3.表达式**\n\n* **表达式**是一系列运算符和操作数的组合，用来计算一个值。\n\n```c\ntotal = 57;\ncount = count + 1;    //注意，count 和 赋值运算符 和 右边的 count + 1 一起构成表达式。\n```\n\n1.带小数点的数值，浮点这个词的本意就是指小数点是浮动的，是计算机内部表达非整数(包括分数和无理数)的一种方式。另一种方式叫做定点数。人们借用浮点数这个词来表达所有带小数点的数。\n\n2.当浮点数和整数放到一起运算时，C会将整数转换成浮点数，然后进行浮点数的运算\n\n1.inch是定义为int类型的变量，如果把int换成double，我们就把它改成double类型的浮点数变量了。\n\n2.double的意思是\"双\"，它本来是\"双精度浮点数\"的第一个单词，人们用来表示浮点数类型。除了double，还有float(意思就是浮点)表示单精度浮点数\n\n在输入的时候数据类型定义为：%lf\n\n在输出的时候数据类型定义为：%f\n\n**数据类型**\n\n整数：int printf(\"%d\"，...) scanf(\"%d\",...)\n\n带小数点的数： double printf(\"%f\") scanf(\"%lf\",....)\n\n整数类型不能表达有小数部分的数，整数和整数的运算结果还是整数。计算机里会有纯粹的整数这种奇怪的东西，是因为整数的运算比较快，而且占地方也小。其实人们日常生活中大量做的还是纯粹整数的计算，所以整数的用处还是很大的。"},{"page":16,"body":"**本节完整补充**\n\n**运算符&操作数、取余运算**\n\n* 运算符 (operator) 是指进行运算的动作，比如加法运算符“+”，减法运算符“-”；\n\n* 操作数 (operand) 是指参与运算的值（也可以称为算子），这个值可能是常数，也可能是变量，还可能是一个方法的返回值。\n\n* 取余运算就是求两个整数相除后的余数，用 A%B 来表示。C 语言中，求余运算符两边，必须为整型数。\n\n* `x % n`的结果是 [0，n - 1] 的一个整数。\n\n**计算时间差**\n\n如果直接分别减，有可能会出现分钟借位的情况，所以要有所处理。\n\n```c\nint hour1, minute1;\nint hour2, minute2;\n\nscanf(\"%d %d\", &hour1, &minute1);\nscanf(\"%d %d\", &hour2, &minute2);\n\nint t1 = hour1 * 60 + minute1;    //转换为分钟单位\nint t2 = hour2 * 60 + minute2;\n\nint t = t2 - t1;\n\nprintf(\"时间差是 %d 时 %d 分。\", t/60, t%60);  //t÷60 小时部分 t%60 分钟部分\n```\n\n**运算符优先级**\n\n```c\n//求平均值\n#include <stdio.h>\n\nint main()\n{\n    int a,b;\n\n    scanf(\"%d %d\", &a, &b);\n\n    double c = (a+b)/2.0;\n\n    printf(\"%d 和 %d 的平均值 = %f\\n\", a, b, c);\n\n    return 0;\n}\n```\n\n表-运算符优先级\n\n- 优先级 · 运算符 · 运算 · 结合关系 · 举例\n\n- 1 · + · 单目不变 · 自右向左 · a* +b\n\n- 1 · - · 单目取负 · 自右向左 · a* -b\n\n- 2 · * · 乘 · 自左向右 · a * b\n\n- 2 · / · 除 · 自左向右 · a / b\n\n- 2 · % · 取余 · 自左向右 · a % b\n\n- 3 · + · 加 · 自左向右 · a + b\n\n- 3 · - · 减 · 自左向右 · a - b\n\n- 4 · = · 赋值 · 自右向左 · a = b\n\n* 运算符优先级：单目 > 算术 > 关系 > 逻辑 > 赋值 > 逗号\n\n**2.2表达式计算**\n\n**表达式**\n\n一个表达式是一系列运算符和算子的组合，用来计算一个值\n\n**运算符**\n\n1.运算符是指进行运算的动作，比如加法运算符\"+\"，减法运算符\"-\"\n\n2.算子是指参与运算的值，这个值可能是常数，也可能是变量，还可能是一个方法的返回值\n\n```c\nint hour1,minute1;\nint hour2,minute2;\n\nscanf(\"%d %d\",&hour1,&minute1);\nscanf(\"%d %d\",&hour2,&minute2);\n\nint t1 = hour1 * 60 + minute1;\nint t2 = hour2 * 60 + minute2;\n\nint t = t2 - t1;\n\nprintf(\"时间差是%d小时%d分钟\",t/60,t%60);\n```"},{"page":17,"body":"**本节完整补充**\n\n**运算符优先级**\n\n```c\n//求平均值\n#include <stdio.h>\n\nint main()\n{\n    int a,b;\n\n    scanf(\"%d %d\", &a, &b);\n\n    double c = (a+b)/2.0;\n\n    printf(\"%d 和 %d 的平均值 = %f\\n\", a, b, c);\n\n    return 0;\n}\n```\n\n表-运算符优先级\n\n- 优先级 · 运算符 · 运算 · 结合关系 · 举例\n\n- 1 · + · 单目不变 · 自右向左 · a* +b\n\n- 1 · - · 单目取负 · 自右向左 · a* -b\n\n- 2 · * · 乘 · 自左向右 · a * b\n\n- 2 · / · 除 · 自左向右 · a / b\n\n- 2 · % · 取余 · 自左向右 · a % b\n\n- 3 · + · 加 · 自左向右 · a + b\n\n- 3 · - · 减 · 自左向右 · a - b\n\n- 4 · = · 赋值 · 自右向左 · a = b\n\n* 运算符优先级：单目 > 算术 > 关系 > 逻辑 > 赋值 > 逗号\n\n**赋值运算符**\n\n* 赋值也是运算，也有结果\n\n* `a = 6`的结果是 a 被赋予值 6，`a = b`的结果是 b\n\n* 由于赋值运算符是**自右向左**结合，所以`a = b = 6`等价于`a = (b = 6)`\n\n* 赋值表达式的运算过程：\n\n1. 计算赋值运算符右侧表达式的值\n\n2. 将赋值运算符右侧表达式的值赋给赋值运算符左侧的变量\n\n3. 将赋值运算符左侧的变量的值作为赋值表达式的值\n\n* **嵌入式赋值**:不利于阅读，且容易产生错误，不建议使用。\n\n* 根据赋值运算符的优先级，x = 2 和 y = 2 可以写成 x = y = 2 的**连赋值**形式。\n\n```c\nint a = 6;\nint b;\nint c = 1 + (b = a);\n```\n\n* **复合赋值**：+ - * / %都可以和赋值运算符“=”结合起来，形成复合赋值运算符\n\n* 如“+=”、“-=”、“*=”、“/=”、“%=”\n\n表-复合算术赋值运算符\n\n- 运算符 · 名称 · 等价关系\n\n- += · 加赋值 · x+=a 等价于 x=x+a\n\n- -= · 减赋值 · x-=a 等价于 x=x-a\n\n- *= · 乘赋值 · x*=a 等价于 x=x*a\n\n- /= · 除赋值 · x/=a 等价于 x=x/a\n\n- %= · 取余赋值 · x%=a 等价于 x=x%a\n\n注：a 可以常量、变量、表达式，若 a 为表达式，则外侧自带括号。比如 x /= y + 5 等价于 x =x / (y + 5)\n\n* 注意符合赋值运算符的**两个运算符中间**不要有空格，否则编译器会提示语法错误，“**[Error] expected expression before '=' token**”\n\n```c\ntotal += 2;\ntotal = total + 2;   //二者等价\n```\n\n* **课后讨论 2.3.1：不好的表达式**\n\n看看如何分析这个表达式：\n\na=5;\n\nc = (b=a+2) - (a=2);\n\n讨论：\n\n* 括号先算，从左往右，所以应该是 b=5+2=7，后面的括号 a=2，再是 7-2=5，这个值在赋给 c，所以 c 是 5。最后结果是 a = 2，b = 7，c = 5.\n\n**求平均值**\n\n写一个程序，输入两个整数，输出他们的平均值\n\n```c\nint a,b;\nscanf(\"%d %d\",&a &b);\ndouble c = (a + b)/2.0;\nprintf(\"%d和%d的平均数是:%f\",a,b,c);\n```\n\n###\n\n**单目运算符**\n\n只有一个算子的运算符：+,-\n\n例如-a，-b，+a，+b\n\n表达式可以使用a*-b类似的形式\n\n1.赋值也是运算，也有结果\n\n2.a=6的结果是a被赋予的值，也就是6\n\n3.a=b=6 在计算机中形成原理 a = (b=6)\n\n**嵌入式赋值**\n\n提示：尽量避免\"嵌入式赋值\"，不利于阅读也容易产生错误\n\n例如：\n\n```c\nresult = a = b = 3 + c;\nresult = 2;\nresult = (result = result * 2) * 6 * (result = 3 + result);\n```\n\n**计算复利**\n\n在银行存定期的时候，可以选择到期后自动转存，并将到期的利息计入本金合并转存。如果1年期的定期利率是3.3%，那么连续自动转存3年后，最初存入的x元定期会得到多少本息金额？\n\n本息合计：x(1 + 3.3%)三次方\n\n```c\nint x;\nscanf(\"%d\",&x);\ndouble amount = x * (1 + 0.033) * (1 + 0.033) * (1 + 0.033);\nprintf(\"%f\",amount);\n```\n\n要计算任意年以后的本息金额，就需要做(1+0.033)的n次方计算\n\n**交换两个变量**\n\n如果已经有：int a=6，b = 5 如何交换a，b两个变量值\n\n答：需要有第三个变量来进行缓冲，如下：\n\nint t = a ，a= b ，b = t ;\n\n**复合赋值**\n\n5个算术运算符，加 减 乘 除 取余可以和赋值运算符“=”结合起来，形成复合赋值运算符：“+=”“-=”“*=”“/=”“%=”\n\ntotal += 5;\n\ntotal = total + 5;\n\n注意两个运算符中间不要有空格\n\n**递增递减运算符**\n\n1.\"++\"\"--\"是两个很特殊的运算符，它们是单目运算符，这个算子还必须是变量。这两个运算符分别叫做递增和递减运算符，他们的作用就是给这个变量+1或者-1\n\n2.以下都是同一个意思：\n\ncount++ count +=1 count = count + 1\n\n3.递增递减可以放在前面(前缀)马上生效,也可以放在后面(后缀)延迟生效\n\n4.可以单独使用，但不要组合进表达式"},{"page":18,"body":"**本节完整补充**\n\n**交换两个变量的值**\n\n* 程序是顺序执行的，所以简单的`a = b`，`b = a`是不行的，这样只能让两个变量都成为 b 的值。\n\n* 为了交换两个容器的值，我们想到可以借用第三个容器。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int a = 2;\n    int b = 5;\n    int t;\n    t = a;\n    a = b;\n    b = t;\n    printf(\"a = %d, b = %d\", a, b);\n\n    return 0;\n}\n```\n\n* 可以通过调试功能，观察上述各变量在交换过程中值是如何变化的。调试过程中可以在某一行设置断点，程序运行到此处时就会暂停，调试界面可以看到各变量的值。\n\n* 上述代码就是 C 语言中，交换两个数的一般做法。学习编程，只学习语法是不够的，还需要积极积累“套路”，了解这个问题一般别人是怎么解决的，这样以后遇到类似问题处理起来就能得心应手了。学习套路的方式之一就是阅读。\n\n```c\n    a = a + b;\n    b = a - b;\n    a = a - b;\n//上述代码可以实现不依靠中间变量交换两者的值\n```\n\n**交换两个变量**\n\n如果已经有：int a=6，b = 5 如何交换a，b两个变量值\n\n答：需要有第三个变量来进行缓冲，如下：\n\nint t = a ，a= b ，b = t ;\n\n**复合赋值**\n\n5个算术运算符，加 减 乘 除 取余可以和赋值运算符“=”结合起来，形成复合赋值运算符：“+=”“-=”“*=”“/=”“%=”\n\ntotal += 5;\n\ntotal = total + 5;\n\n注意两个运算符中间不要有空格\n\n交换两个变量是练习“程序按顺序执行”的最佳例子。画两个格子代表 a 和 b：\n\n- 初始：`a = 2`，`b = 5`。\n\n- `t = a;`：t 保存 2，a 还保留 2。\n\n- `a = b;`：a 变成 5，b 仍为 5。\n\n- `b = t;`：b 变成 2。\n\n临时变量 t 的作用是先把原值“备份”下来，否则第二步会把原 a 覆盖掉。`a = b; b = a;` 的错误会让人清楚看到：赋值不是交换，而是复制和覆盖。\n\n用加减法也可以交换：\n\n```c\na = a + b;\nb = a - b;\na = a - b;\n```\n\n这个技巧虽然简短，但可读性更差，而且大整数相加可能溢出。工程中优先使用第三个变量；如果必须交换，也可以使用临时变量、函数或专门的交换工具。\n\n同时把复合赋值纳入同一个知识点：`x += 5` 等价于 `x = x + 5`，`x -= 5` 等价于 `x = x - 5`。复合赋值不是两个运算符分开写，`+=`、`-=`、`*=`、`/=`、`%=` 中间不能有空格。它让“对同一个变量做更新”更清晰，也减少重复写变量名的机会。"},{"page":19,"body":"**本节完整补充**\n\n**递增递减运算符**\n\n* “++”、“--”是两个很特殊的运算符，它们是单目运算符，这个操作数还必须是**变量**。这两个运算符分别叫作递增和递减运算符，它们的作用就是给这个变量+1、-1。\n\n```c\ncount++;\ncount = count + 1;\ncount += 1;      //三者等价\n```\n\n* 前缀、后缀：++、-- 既可以放在变量前面，叫作前缀形式，也可以放在变量后面，叫后缀形式。\n\n* `a++`的值（结果）是 a+1 以前的值，`++a`的值是 a+1 之后的值，但无论如何，这两段代码执行后 a 自己的值都 +1 了。\n\n```c\nint a;\na = 10;\nprintf(\"a++ = %d\\n\", a);\nprintf(\"a = %d\\n\", a);\nprintf(\"++a = %d\\n\", a);\nprintf(\"a = %d\\n\", a);\n//输出结果\na++ = 10\na = 11\n++a = 12\na = 12\n```\n\n表-自增自减运算符\n\n- 表达式 · 运算 · 表达式的值\n\n- count++ · 给 count 加 1 · count 原来的值\n\n- ++count · 给 count 加 1 · count+1 之后的值\n\n- count-- · 给 count 减 1 · count 原来的值\n\n- --count · 给 count 减 1 · count-1 之后的值\n\n* 这两个运算符有其历史来源\n\n* 可以单独使用，但是不要组合进表达式里\n\n* 自增自减运算符，它们的运算对象可以是 char 型变量、int 型变量和 float 型变量\n\n```c\n++i++;\ni++++;\na = b += c++ -d + --e/-f;   //组合进表达式就太复杂了\n```\n\n* **课后讨论 2.3.2：上课提到的那个复杂表达式该怎么理解？**\n\na = b+=c++-d+--e/-f\n\n讨论：\n\n* 这个表达式包含了多个赋值和增减操作。为了更好地理解它，我们可以逐步拆解：\n\n1. `c++`: 这是后置自增操作。它返回`c`的当前值，并在返回之后将`c`增加 1。\n\n2. `--e`: 这是前置自减操作。在返回`e`的值之前，它将`e`减少 1。\n\n3. `b += ...`: 这是复合赋值操作。它将`b`的当前值加上`...`的结果，并将结果赋给`b`。\n\n4. `a = ...`: 这是普通的赋值操作。它将`...`的结果赋给`a`。\n\n* 现在，我们可以结合上述操作来分析整个表达式：\n\n```c\na = b += c++ - d + --e / -f\n```\n\n* 按照操作的优先级和结合性，这个表达式可以解释为：\n\n1. 首先计算`c++ - d`。这将使用`c`的当前值（然后`c`会增加 1）和`d`的值。\n\n2. 接下来，计算`--e / -f`。首先，`e`会减少 1，然后将结果除以`-f`。\n\n3. 将上述两个结果相加，得到`b += ...`的右侧的值。\n\n4. 更新`b`的值，并将结果赋给`a`。\n\n* 请注意，这个表达式虽然在语法上是有效的，但是从代码可读性和维护性的角度来看，它是复杂的，因为它在一个简短的语句中包含了很多操作。这使得其他开发者（或甚至是你自己在一段时间后）很难理解其含义和目的。因此，在实践中，我们通常建议将这种复杂的表达式分解为多个更简单、更直观的语句。\n\n```c\n    int a = 1;\n    int b = 3;\n    int c = 7;\n    int d = 15;\n    int e = 24;\n    int f = 35;\n\n    a=b+=c++-d+--e/-f;\n\n    printf(\"a=%d  b=%d  c=%d  d=%d e=%d  f=%d\", a, b, c, d, e, f);\n\n    //输出结果：a=-5  b=-5  c=8  d=15 e=23  f=35\n```\n\n**复合赋值**\n\n5个算术运算符，加 减 乘 除 取余可以和赋值运算符“=”结合起来，形成复合赋值运算符：“+=”“-=”“*=”“/=”“%=”\n\ntotal += 5;\n\ntotal = total + 5;\n\n注意两个运算符中间不要有空格\n\n1.\"++\"\"--\"是两个很特殊的运算符，它们是单目运算符，这个算子还必须是变量。这两个运算符分别叫做递增和递减运算符，他们的作用就是给这个变量+1或者-1\n\n2.以下都是同一个意思：\n\ncount++ count +=1 count = count + 1\n\n3.递增递减可以放在前面(前缀)马上生效,也可以放在后面(后缀)延迟生效\n\n4.可以单独使用，但不要组合进表达式"},{"page":20,"body":"**本节完整补充**\n\n**4.在线评判系统 (Online Judge、OJ)**\n\n**注意事项**\n\n* 除了题目要求的输出，不能输出其他内容，也不用设置任何用户友好型提示。\n\n**各类提交状态提示**\n\n1. 通过**(Accept，AC)**\n\n* 表示代码通过了 OJ 的所有测试点。有些 OJ 系统设有部分通过的提示，说明有些测试点或者边界条件没有通过。\n\n2. 答案错误**(Wrong Answer，WA)**\n\n* 表示代码有错误，需要 debug\n\n3. 运行时间超限**(Time Limit Exceeded，TLE)**\n\n* 有两种情况：\n\n* 代码运行过程中出现了死循环，需要进行 debug\n\n* 代码时间复杂度过高，无法在规定时间之内处理所有数据，需要优化\n\n* 解决方法：printf() 调试大法、优化算法时间复杂度\n\n4. 编译错误**(Compile Error，CE)**\n\n* 说明代码中有不合法语句，需根据提示进行修改\n\n5. 运行空间超限**(Memory Limit Exceeded，MLE)**\n\n* 一般有两种情况\n\n* 递归栈爆了\n\n* 数组空间开太多了\n\n6. 格式错误**(Presentation Error，PE)**\n\n* 说明结果没按题目要求输出，请检查空格、换行符等等。\n\n7. 段错误**(Segmentation Fault，SF)**\n\n* 错误原因比较多，就是访问了不可访问的内存。\n\n* 比如数组下标越界。\n\n8. 运行时错误**(Runtime Error，RE)**\n\n* 运行时错误，这个一般是程序在运行期间执行了非法的操作造成的。可能是下列情况之一\n\n* 数组越界：把定义的数组开大点\n\n* 指针越界\n\n* 数组开得太大，超出了栈的范围，造成栈溢出。\n\n* 除以 0\n\n* 非法输入，比如不在合法范围内的 n 的输入有没有特殊处理\n\n9. 非零返回**(Non zero return，NZR)**\n\n* 当函数执行过程中遇到错误或异常情况时\n\n* 主函数返回了一个非 0 的值，比如说错把主函数当成子函数写了，返回了一个变量。\n\n**边界测试**\n\n* 程序的边界最容易出错，所以测试程序经常使用边界数据，如有效范围两端的数据、特殊的倍数等等\n\n* 常用的边界数据：端点、0、负数等等\n\n在线判题系统（OJ）对输出有非常严格的要求。它不是看程序“逻辑对不对”，而是看程序对每一个测试点给出的结果是否和标准答案完全一致：空格、换行、大小写、前导零、多余提示都可能造成错误。\n\n提交前应建立一套固定检查流程：\n\n1. 读题：输入范围、输出格式、结束条件、是否有多个测试点。\n\n2. 写代码：只输出题目要求的内容，不要打印“请输入”“结果是”等提示。\n\n3. 测试：至少测最小、最大、边界、特殊值，例如 0、负数、恰好等于边界、结束标记。\n\n4. 排查：WA 看格式和逻辑；TLE 看循环是否死循环或复杂度；CE 看第一处编译错误；RE 看越界、除零和非法输入；PE 看空格换行；MLE 看数组或递归栈。\n\n5. 提交：删掉调试输出和中文提示后再提交。\n\n一个特别常见的坑是“本地能运行，提交却 WA”。检查时把目光从“我看到了正确结果”移到“输出是否完全一样”。比如题目要求输出 `321`，本地打印了 `answer=321`，视觉上能看懂，但判题系统会认为多出了文字。另一个坑是只测了题目给的样例，没有测输入结束条件或数组边界。\n\n`scanf` 的返回值也是有用的信号：它返回成功读取的变量个数。如果期望读取两个整数但只读到一个，程序可以根据返回值停止或报错；如果忽略返回值，非法输入可能把未初始化变量继续带入运算。"}];
