window.COURSE_CHUNKS=window.COURSE_CHUNKS||{};window.COURSE_CHUNKS["数据类型"]=[{"page":46,"body":"题目要求给定不超过 6 的正整数 A，考虑从 A 开始的连续 4 个数字，输出所有由它们组成的无重复数字的 3 位数。输出要按从大到小排列，每行 6 个整数，整数之间用空格分隔，行末不能有多余空格。\n\n例如 A=1 时，候选数字是 1、2、3、4，第一位、第二位、第三位都从这四个数字中选择，但不能重复。因为要求从大到小，外层、中层、内层都应从 A+3 向 A 递减。\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    int a;\n    int count = 0;\n    scanf(\"%d\", &a);\n\n    for (int i = a + 3; i >= a; i--) {\n        for (int j = a + 3; j >= a; j--) {\n            for (int k = a + 3; k >= a; k--) {\n                if (i != j && i != k && j != k) {\n                    printf(\"%d%d%d\", i, j, k);\n                    if (++count == 6) {\n                        putchar('\\n');\n                        count = 0;\n                    } else {\n                        putchar(' ');\n                    }\n                }\n            }\n        }\n    }\n    if (count) putchar('\\n');\n    return 0;\n}\n```\n\n三层循环枚举所有可能的三位数，`i != j && i != k && j != k` 排除重复数字。输出时先根据需要打印数字，再决定空格还是换行，这样不会在行尾留下多余空格。若题目要求递增，只需把三层循环改成 `i = a; i <= a + 3; i++`；若要求每行 6 个，就用计数器每满 6 换行。\n\n测试时提供 A=1 和 A=6，手动确认第一行、最后一行、每行数量以及最后一个数字后面没有空格。\n\n先算数量，再决定循环和排版。连续 4 个数字放在 3 个位置上，且每个位置不能重复，因此排列总数是 `4 × 3 × 2 = 24` 种；题目要求每行输出 6 个，所以正好输出 4 行。这个计算可以立刻验证程序输出数量：24 个组合、4 行、每行 6 个。\n\n倒序不仅体现在最外层，三个位置都要按 `A+3` 到 `A` 的递减顺序枚举。以 A=1 为例，第一行应从 432、431、430、423、421、420 开始；以 A=6 为例，候选数字为 6、7、8、9，第一行则是 987、986、985、976、975、974。计数器每成功输出一个数就加一，到 6 就换行并清零；循环结束后若还有第 7、13、19 个数没有换行，需要再补一次换行，否则最后一行会和下一段文字粘在一起。\n\n输出整数之间只用一个空格，并且行末不能有空格。最简单可靠的方法是先输出当前数，再判断计数器是否到 6，而不是每轮都先输出空格：`printf(\"%d%d%d\", i, j, k)` 不会额外产生前导空格，换行由 `putchar('\\n')` 完成。写完后再把候选数字减到 3 个，观察输出是否变成 6 种、每行 6 个，以验证循环边界和计数器逻辑。"},{"page":47,"body":"水仙花数题目要求找出三位数中每一位数字的立方和等于它本身的数。最直接的方法是遍历 100 到 999，把个位、十位、百位拆出来，计算立方和并比较。比如 153：`1^3 + 5^3 + 3^3 = 1 + 125 + 27 = 153`，所以 153 满足条件。\n\n一个通用版本可以先计算数字位数 n，再把每一位的 n 次方累加：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    for (int n = 100; n <= 999; n++) {\n        int ones = n % 10;\n        int tens = n / 10 % 10;\n        int hundreds = n / 100;\n        int sum = hundreds * hundreds * hundreds\n                + tens * tens * tens\n                + ones * ones * ones;\n        if (sum == n) printf(\"%d\\n\", n);\n    }\n    return 0;\n}\n```\n\n这个写法适合只有三位的情况。如果题目改成任意位数，可以先把当前数字复制到临时变量，循环取末位并除以 10；计算幂时不要用 `pow`，整数幂用乘法和循环更可控，也能避免浮点精度差异。\n\n调试时应先验证枚举边界：100、153、370、371、407、999。若缺少某个结果，优先检查十位的拆法：`n / 10 % 10` 是先除以 10、再取余 10；写成 `n / 100 % 10` 会取到百位，或者把个位和十位弄混。\n\n三位水仙花数可以直接用固定拆位，也可以把拆位写成循环，后者能推广到四位数和五位数：\n\n```c\nint value = n;\nint sum = 0;\nwhile (value > 0) {\n    int digit = value % 10;\n    sum += digit * digit * digit;\n    value /= 10;\n}\nif (sum == n) printf(\"%d\\n\", n);\n```\n\n这个版本每次取当前数的最低位，再把数字除以 10 丢掉最低位，最后用 `sum == n` 判断。三位数固定拆位直观，但必须准确处理十位：`n / 10 % 10` 是先丢掉个位，再取新的最低位；写成 `n % 100 / 10` 同样正确。\n\n测试可以按三类进行：已知答案 `153`、`370`、`371`、`407`；边界 `100`、`999`；以及普通数 `123`。不要因为打印出的某个数看着像水仙花数就提交，还要确认所有候选都被枚举，且没有靠 `pow` 做整数运算。`pow` 返回 double，浮点舍入可能让 5³ 变成 124.999…，尤其是更大的位数，用整数乘法判断最可靠。\n\n如果输出缺少答案，先检查拆位；如果输出包含了 `1` 或 `999` 之外的错误数，再检查循环范围和初值。把“拆位”“求幂”“判断”“输出”拆成独立步骤，调试时就能分别验证。"},{"page":48,"body":"九九乘法表是练习“行、列、换行、格式”四个控制点的经典题。外层循环控制第几行，内层循环控制这一行输出到第几列；每行结束时才输出换行，不能在内层每次输出后都换行。\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    for (int i = 1; i <= 9; i++) {\n        for (int j = 1; j <= i; j++) {\n            printf(\"%d*%d=%-3d\", j, i, i * j);\n        }\n        putchar('\\n');\n    }\n    return 0;\n}\n```\n\n这里 `j <= i` 让第一行一列、第二行两列，形成下三角；如果改成 `j <= 9` 就会得到完整矩形。`%-3d` 表示结果左对齐、至少占 3 个字符，因此个位数后会有两个空格，两位数后有一个空格；如果题目要求完全不同的分隔，可以调整格式串。\n\n建议手工追踪第一行和第二行：\n\n- 第一行：`1*1=1`，然后换行；\n\n- 第二行：`1*2=2 2*2=4`，然后换行。\n\n如果输出形状正确但顺序反了，检查 `printf(\"%d*%d=%d\", j, i, ...)` 中的顺序，题目通常希望 `1*9=9` 而不是 `9*1=9`。换行放在外层循环内、内层循环结束之后，是这类题最容易出错的细节。\n\n九九乘法表的三层信息可以这样组织：外层 i 是行号，内层 j 是列号，换行是每一行结束后的动作。当下三角要求列数不超过行数时，用 `j <= i`；当题目要求完整矩形时，把内层条件改成 `j <= 9`。\n\n`%-3d` 是左对齐、至少占 3 个字符：个位数输出后补两个空格，两位数补一个空格，三位数不再补位。改用 `%3d` 则右对齐；改用 `%d\\t` 则用制表符分隔，但制表位的宽度不一定固定。提交前检查题目要求的是空格、制表符还是固定宽度，不要只在编辑器里看起来“对齐”就算完成。\n\n手工追踪是检查循环顺序最直接的方法：\n\n- 第 1 行：只输出 `1*1=1`，随后换行。\n\n- 第 2 行：`1*2=2`、`2*2=4`，随后换行。\n\n- 第 9 行：从 `1*9=9` 一直到 `9*9=81`，随后换行。\n\n如果乘号两边顺序反了，检查 `printf(\"%d*%d=%d\", j, i, ...)` 是 `j*i` 还是 `i*j`；如果行首多出空格，通常是把空格放在数字前面；如果所有行都没有换行，检查换行是否被写在循环外面；如果每一列都变成 9 行，检查是否遗漏 `j <= i`。循环结构本身很小，逐项追踪一次后，以后处理表格输出会快很多。\n\n练习时还可以把“输出数量”作为检查点：下三角共有 `1 + 2 + ... + 9 = 45` 组乘法。如果程序打印的数量不是 45，通常不是格式问题，而是循环边界或输出换行位置错误。写出完整矩形时共有 `9 × 9 = 81` 组，这是另一个快速验证方法。\n\n不建议直接复制答案，而应先用 1 到 3 的小规模版本手算，再加入外层行数；这样即使最后题目改成 15 行或倒序打印，只需要调整初值、条件和步长，不需要改变整个结构。"},{"page":49,"body":"**本节完整补充**\n\n* **章末习题**\n\n若变量已正确定义，以下 while 循环正常结束时，**累加到 pi 的最后一项** item 的值满足____\n\n```c\nflag = 1;\ndenominator = 1;\nitem = 1.0;\npi = 0;\nwhile (fabs(item) >= 0.0001) {\n    pi = pi + item;\n    flag = -flag;\n    denominator = denominator + 2;\n    item = flag * 1.0 / denominator;\n}\n```\n\n解析：\n\n* 注意 item 在累加到 pi 的语句前还是后是有区别，这里累加到 pi 的最后一项 item 的值满足 item>=0.0001，如果 item<0.0001，下一次条件判断就会出循环，不会累加到 pi 上。\n\n* **章末习题：素数和（5 分）**\n\n**题目内容：**\n\n我们认为 2 是第一个素数，3 是第二个素数，5 是第三个素数，依次类推。\n\n现在，给定两个整数 n 和 m，0<n<=m<=200，你的程序要计算第 n 个素数到第 m 个素数之间所有的素数的和，包括第 n 个素数和第 m 个素数。\n\n**输入格式:**\n\n两个整数，第一个表示 n，第二个表示 m。\n\n**输出格式：**\n\n一个整数，表示第 n 个素数到第 m 个素数之间所有的素数的和，包括第 n 个素数和第 m 个素数。\n\n**输入样例：**\n\n2 4\n\n**输出样例：**\n\n15\n\n解答：\n\n* 注意每处理完内层一个素数，都要在外层循环把`isPrime`置为 1，不要把循环初始化语句放错了位置，混淆了外层循环和内层循环的初始化。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int count = 0;\n    int isPrime;\n    int n, m;\n    int sum = 0;\n\n    scanf(\"%d %d\", &n, &m);\n\n    int i, j;\n    j = 2;\n    while (count < m) {\n        isPrime = 1;\n        for (i = 2; i < j; i++) {\n            if (j % i == 0) {\n                isPrime = 0;\n                break;\n            }\n        }\n        if (isPrime == 1) {\n            count++;\n            if (count >= n && count <= m)\n                sum += j;\n        }\n        j++;\n    }\n\n    printf(\"%d\", sum);\n\n    return 0;\n}\n```\n\n* **章末习题：念整数（5 分）**\n\n你的程序要读入一个整数，范围是[-100000,100000]。然后，用汉语拼音将这个整数的每一位输出出来。\n\n如输入 1234，则输出：\n\nyi er san si\n\n注意，每个字的拼音之间有一个空格，但是最后的字后面没有空格。当遇到负数时，在输出的开头加上“fu”，如-2341 输出为：\n\nfu er san si yi\n\n一个整数，范围是[-100000,100000]。\n\n表示这个整数的每一位数字的汉语拼音，每一位数字的拼音之间以空格分隔，末尾没有空格。\n\n-30\n\nfu san ling\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int mask = 1;\n    scanf(\"%d\", &x);\n\n    if (x < 0) {\n        printf(\"fu \");\n        x = -x;\n    }\n\n    int t = x;\n    while (t > 9) {\n        t /= 10;\n        mask *= 10;\n    }\n    while (mask > 0) {\n        int d = x / mask;\n        switch (d) {\n            case 0: printf(\"ling\"); break;\n            case 1: printf(\"yi\"); break;\n            case 2: printf(\"er\"); break;\n            case 3: printf(\"san\"); break;\n            case 4: printf(\"si\"); break;\n            case 5: printf(\"wu\"); break;\n            case 6: printf(\"liu\"); break;\n            case 7: printf(\"qi\"); break;\n            case 8: printf(\"ba\"); break;\n            case 9: printf(\"jiu\"); break;\n        }\n        if (mask > 9)\n            printf(\" \");\n        x %= mask;\n        mask /= 10;\n    }\n\n    return 0;\n}\n```\n\n这道题把素数判断、循环边界和求和计数组合在一起。素数是大于 1 且只能被 1 和自身整除的整数；1 不是素数，2 是素数。判断时只需测试到平方根，因为大因子必然配对小因子。发现因子后应立刻停止测试。区间端点可能不是素数，0、1 和负数都要挡住。计数器和求和器都要在循环外初始化，输出放在循环结束后。素数判断以后会反复使用，最好抽象成独立函数。"},{"page":50,"body":"**本节完整补充**\n\n**猜数游戏**\n\n* 让计算机来想一个数，然后让用户来猜，用户每输入一个数，就告诉它是大了还是小了，直到用户猜中为止，最后还要告诉用户它猜了多少次。\n\n* 因为需要不断重复让用户猜，所以需要用到循环\n\n* 在实际写出程序之前，我们可以先用文字描述程序的思路\n\n* 核心重点是循环的条件，人们往往会考虑循环终止的条件\n\n* 文字描述：\n\n1. 计算机随机想一个数，记在变量 number 里；\n\n2. 一个负责计次数的变量 count 初始化为 0；\n\n3. 让用户输入一个数字 a；\n\n4. count 递增（加一）；\n\n5. 判断 a 和 number 的大小关系，如果 a 大，就输出“大”；如果 a 小就输出“小”；\n\n6. 如果 a 和 number 是不相等的（无论大还是小），程序转回到第 3 步；\n\n7. 否则，程序输出“猜中”和次数，然后结束。\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <time.h>\n\nint main()\n{\n    srand(time(0));\n    int number = rand() % 100 + 1;\n    int count = 0 ;\n    int a = 0;\n\n    printf(\"我已经想好了一个 1 到 100 之间的数。\");\n    do{\n        printf(\"请猜这个 1 到 100 之间数：\");\n        scanf(\"%d\", &a);\n        count++;\n        if (a > number)\n            printf(\"你猜的数大了。\");\n        else if (a < number)\n            printf(\"你猜的数小了。\");\n    }while (a != number);\n\n    printf(\"太好了，你用了 %d 次就猜到了答案。\\n\", count);\n\n    return 0;\n}\n```\n\n* `rand()`是一个库函数，每次调用可以得到一个随机数。\n\n* 使用`srand(time(0));`语句保证每次产生的随机数不一样。\n\n* **课后讨论 4.4.1：为什么 100 以内的数猜 7 次就够了？**\n\n课程中的猜数游戏，为什么 100 以内的数猜 7 次就够了？\n\n讨论：\n\n* 使用二分查找在每次进行猜数的时候范围都会缩小 1/2，换句话来说就是每猜测一次，剩下数的数量就会减少一半，而 2 的 7 次方是 128，所以最多猜 7 次。\n\n**循环应用(猜数游戏)**\n\n**需求**\n\n让计算机来想一个数，然后让用户来猜，用户每输入一个数，就告诉用户是大了还是小了，直到用户猜到为止，最后还要告诉用户他猜了几次\n\n**思路**\n\n因为需要不断重复让用户猜，所以需要用到循环\n\n在实际写出程序之前，我们可以先用文字描述程序的思路\n\n核心重点是循环的条件\n\n人们往往会考虑循环终止的条件\n\n**随机数**\n\n每次召唤rand()就可以得到一个随机数\n\n**代码块**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <time.h>\n\nint main(){\n    //不加srand(time(0));的话，每次循环的数都一样\n    srand(time(0));\n\tint number = rand()%100+1;\n\tint count = 0;\n\tint a = 0;\n\tprintf(\"我已经想好了一个1到100之间的数。\");\n\tdo {\n\t\tprintf(\"请猜这个1到100之间数：\");\n\t\tscanf(\"%d\", &a);\n\t\tif ( a > number ) {\n\t\t\tprintf(\"你猜的数大了。\");\n\t\t} else if ( a < number ) {\n\t\t\tprintf(\"你猜的数小了。\");\n\t\t}\n\t\tcount ++;\n\t} while (a != number);\n\tprintf(\"太好了，你用了%d次就猜到了答案。\\n\", count);\n\n\treturn 0;\n}\n```\n\n**%100**\n\nx%n的结果是[0,n-1]的一个整数\n\n猜数游戏的核心状态是当前猜测值和目标值的关系。程序给用户三次机会，并根据大于、小于、等于三种关系反馈。中奖时要立刻结束，否则后面的提示会不符合游戏状态。scanf 返回成功读取的项目数，检查它可以避免非法输入继续参与比较。生成固定范围随机数的通用思路是先取余得到范围宽度，再加下界；只取余 100 会缺少 100 本身。"},{"page":51,"body":"**本节完整补充**\n\n**前 n 项求和**\n\n典例一、求 1 + 1/2 + 1/3 + ... 的前 N 项之和：\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    double sum = 0;\n    int n;\n    scanf(\"%d\", &n);\n\n    for (int i = 1; i <= n; i++)\n        sum += 1.0/i;\n\n    printf(\"sum = %.6f\", sum);\n\n    return 0;\n}\n```\n\n典例二、求交错序列 1-2/3+3/5-4/7+5/9-6/11+... 的前 N 项之和。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    double sum = 0, denominator = 1, numerator = 1;\n    int n;\n    scanf(\"%d\", &n);\n    double flag = 1;\n\n    for (int i = 1; i <= n; i++) {\n        sum += flag * numerator / denominator;\n        flag = -flag;\n        numerator++;\n        denominator += 2;\n    }\n\n    printf(\"%.3f\\n\", sum);\n\n    return 0;\n}\n```\n\n“n 项求和”是一个把通项、循环边界和浮点转换结合起来的题目。不要把 `1/2` 想成数学里的二分之一，它在 C 中先做整数除法，结果是 0，除非至少一个操作数是浮点数。\n\n课程里两个例子可以分别建模：\n\n1. `1 + 1/2 + 1/3 + ... + 1/n`：第 i 项是 `1.0 / i`。\n\n2. `1 - 2/3 + 3/5 - 4/7 + ...`：第 i 项的分子是 `i`，分母是 `2 * i - 1`，符号交替。\n\n下面这个版本把第二题的状态拆清楚：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    int n;\n    double sum = 0.0;\n    int sign = 1;\n    scanf(\"%d\", &n);\n\n    for (int i = 1; i <= n; i++) {\n        sum += sign * i / (double)(2 * i - 1);\n        sign = -sign;\n    }\n    printf(\"%.6f\\n\", sum);\n    return 0;\n}\n```\n\n`sum` 必须为 double；`(double)(2 * i - 1)` 保证除法的分母转成浮点；`sign` 每轮取反。对于第一题，也可以写 `sum += 1.0 / i`，不必把整个分子写复杂。\n\n检查时输入 n=1、n=2 手工算几项，再输入较大的 n 看结果是否单调变化。如果输出全是整数或突然为 0，通常说明整数除法或循环边界出了问题。"},{"page":52,"body":"**本节完整补充**\n\n* **章末习题**\n\n若变量已正确定义，以下 while 循环正常结束时，**累加到 pi 的最后一项** item 的值满足____\n\n```c\nflag = 1;\ndenominator = 1;\nitem = 1.0;\npi = 0;\nwhile (fabs(item) >= 0.0001) {\n    pi = pi + item;\n    flag = -flag;\n    denominator = denominator + 2;\n    item = flag * 1.0 / denominator;\n}\n```\n\n解析：\n\n* 注意 item 在累加到 pi 的语句前还是后是有区别，这里累加到 pi 的最后一项 item 的值满足 item>=0.0001，如果 item<0.0001，下一次条件判断就会出循环，不会累加到 pi 上。\n\n* **章末习题：素数和（5 分）**\n\n**题目内容：**\n\n我们认为 2 是第一个素数，3 是第二个素数，5 是第三个素数，依次类推。\n\n现在，给定两个整数 n 和 m，0<n<=m<=200，你的程序要计算第 n 个素数到第 m 个素数之间所有的素数的和，包括第 n 个素数和第 m 个素数。\n\n**输入格式:**\n\n两个整数，第一个表示 n，第二个表示 m。\n\n**输出格式：**\n\n一个整数，表示第 n 个素数到第 m 个素数之间所有的素数的和，包括第 n 个素数和第 m 个素数。\n\n**输入样例：**\n\n2 4\n\n**输出样例：**\n\n15\n\n解答：\n\n* 注意每处理完内层一个素数，都要在外层循环把`isPrime`置为 1，不要把循环初始化语句放错了位置，混淆了外层循环和内层循环的初始化。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int count = 0;\n    int isPrime;\n    int n, m;\n    int sum = 0;\n\n    scanf(\"%d %d\", &n, &m);\n\n    int i, j;\n    j = 2;\n    while (count < m) {\n        isPrime = 1;\n        for (i = 2; i < j; i++) {\n            if (j % i == 0) {\n                isPrime = 0;\n                break;\n            }\n        }\n        if (isPrime == 1) {\n            count++;\n            if (count >= n && count <= m)\n                sum += j;\n        }\n        j++;\n    }\n\n    printf(\"%d\", sum);\n\n    return 0;\n}\n```\n\n* **章末习题：念整数（5 分）**\n\n你的程序要读入一个整数，范围是[-100000,100000]。然后，用汉语拼音将这个整数的每一位输出出来。\n\n如输入 1234，则输出：\n\nyi er san si\n\n注意，每个字的拼音之间有一个空格，但是最后的字后面没有空格。当遇到负数时，在输出的开头加上“fu”，如-2341 输出为：\n\nfu er san si yi\n\n一个整数，范围是[-100000,100000]。\n\n表示这个整数的每一位数字的汉语拼音，每一位数字的拼音之间以空格分隔，末尾没有空格。\n\n-30\n\nfu san ling\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int x;\n    int mask = 1;\n    scanf(\"%d\", &x);\n\n    if (x < 0) {\n        printf(\"fu \");\n        x = -x;\n    }\n\n    int t = x;\n    while (t > 9) {\n        t /= 10;\n        mask *= 10;\n    }\n    while (mask > 0) {\n        int d = x / mask;\n        switch (d) {\n            case 0: printf(\"ling\"); break;\n            case 1: printf(\"yi\"); break;\n            case 2: printf(\"er\"); break;\n            case 3: printf(\"san\"); break;\n            case 4: printf(\"si\"); break;\n            case 5: printf(\"wu\"); break;\n            case 6: printf(\"liu\"); break;\n            case 7: printf(\"qi\"); break;\n            case 8: printf(\"ba\"); break;\n            case 9: printf(\"jiu\"); break;\n        }\n        if (mask > 9)\n            printf(\" \");\n        x %= mask;\n        mask /= 10;\n    }\n\n    return 0;\n}\n```\n\n循环习题通常不是考一个新语法，而是考状态、条件和推进的组合。复盘时问四个问题：初始值是否正确，循环继续条件会不会漏掉最后一项，每次循环是否推进，输出发生在循环内还是循环外。条件过滤应放在循环体里；不要为了让循环跳过某些项而写复杂的自增表达式。死循环优先查循环变量是否被误改、条件是否永远成立、输入是否没被读取。至少测试最小 n、最大 n、无解、全部满足四类边界。"},{"page":53,"body":"**本节完整补充**\n\n**1.数据的存储和基本数据类型**\n\n**引言**\n\n* C 语言是有类型的语言，C 语言的变量必须在使用前**定义**并**确定类型**。\n\n* C 以后的语言向两个方向发展：\n\n* C++/Java 更强调类型，对类型的检查更严格\n\n* JavaScript、Python、PHP 不看重类型，甚至不需要事先定义\n\n**类型安全**\n\n* 支持强类型的观点认为明确的类型有助于尽早发现程序中的简单错误\n\n* 反对强类型的观点认为过于强调类型迫使程序员面对底层、实现而非事务逻辑\n\n* 总的来说，早期语言强调类型，面向底层的语言强调类型\n\n* C 语言需要类型，但是对类型的安全检查并不足够\n\n**C 语言的类型(下划线的是 C99 的类型)**\n\n* 整数：char、short、int、long、 long long 、 bool (逻辑)\n\n* 浮点数：float、double、 long double\n\n* 指针\n\n* 自定义类型\n\n**类型有何不同**\n\n* 类型名称：int、long、double\n\n* 输入输出时的格式化：%d、%ld、%lf\n\n* 所表达的数的范围：char < short < int < float < double < long double\n\n* 内存中所占据的大小：1 个字节到 16 个字节\n\n* 内存中的表达形式：二进制数（补码）、编码\n\n**sizeof()**\n\n* 是**长度运算符**，给出某个类型或变量在内存中所占据的字节数，如`sizeof(int)`和`sizeof(i)`。\n\n* sizeof 是一个**单目**运算符\n\n* 是静态运算符，它的结果在编译时刻就决定了，可以增强程序的可移植性，使之不受具体计算机数据类型长度的限制\n\n* 不要在 sizeof 的括号里做运算，这些运算不会做的\n\n```c\nint a = 25;\nprintf(\"sizeof(a + 1.0) = %ld\", sizeof(a + 1.0));\n//输出结果为 8，因为 1.0 是 double，a+1.0 运算结果也是 double，sizeof 在编译时就会确定为 8 了。\n```\n\n**2.整数**\n\n* char：1 字节(8 bit)\n\n* short：2 字节\n\n* int：取决于编译器（CPU），通常的意义是“1 个字”，在 32 位机器和 64 机器中 int 类型都占用 4 个字节。\n\n* long：取决于编译器（CPU），通常的意义是“1 个字”\n\n* long long：8 字节\n\n**第六周:数据类型**\n\n**编程类型解析**\n\n```c\n//给定不超过6的正整数A，考虑从A开始的连续4个数字。请输出所有由它们组成的无重复数字的3位数\n//输出格式：满足条件的3位数，要求从大到小，每行6个整数，整数间以空格分隔，但行末不能有多余空格\nint main()\n{\n    int a;\n    scanf(\"%d\",&a);\n    int i,j,k;\n    int cnt = 0;\n\n    i = a;\n    while( i<=a+3){\n        j = a；\n        while( j<=a+3){\n            k = a;\n            while( k<=a+3){\n                if( i!=j ){\n                    if( i!=k ){\n                        if( j!=k){\n                            cnt++;\n                            printf(\"%d%d%d\",i,j,k);\n                            if( cnt == 6){\n                                printf(\"\\n\");\n                                cnt = 0;\n                            }else{\n                                printf(\" \");\n                            }\n                        }\n                    }\n                }\n                k++;\n            }\n            j++;\n        }\n        i++;\n    }\n    return 0;\n}\n#include<stdio.h>\nint main() {\n\tint n;\n\t//scanf(\"%d\", &n);\n\tn=3;\n\tint first = 1;\n\tint i = 1;\n\twhile(i < n){\n\t\tfirst *= 10;\n\t\ti++;\n\t}\n\t//printf(\"first=%d\\n\", first);\n\t//遍历100-999\n\ti = first;\n\twhile(i < first*10){\n\t\t//需要一个临时的变量去记录 i\n\t\tint t = i;\n\t\t//需要一个\"和\"去记录每一位数的 n次幂\n\t\tint sum = 0;\n\t\tdo {\n\t\t\tint d = t % 10;\n\t\t\tt /= 10;\n\t\t\t//d^2 = d*d;  d^3 = d*d*d;\n\t\t\t/*\n\t\t\tint p = 1;\n\t\t\tint j = 0;\n\t\t\twhile(j < n){\n\t\t\t\tp *= d;\n\t\t\t\tj++;\n\t\t\t}\n\t\t\t//或者*/\n\t\t\tint p = d;\n\t\t\tint j = 1;\n\t\t\twhile(j < n){\n\t\t\t\tp *= d;\n\t\t\t\tj++;\n\t\t\t}\n\t\t\tsum += p;\n\t\t} while(t > 0);\n\t\tif (sum == i) {\n\t\t\tprintf(\"%d\\n\", i);\n\t\t}\n\t\ti++;\n\t}\n\treturn 0;\n}\n#include<stdio.h>\nint main() {\n\tint n;\n\t//scanf(\"%d\", &n);\n\tn = 9;\n\tint i,j;\n\ti = 1;\n\twhile( i <= n) {\n\t\tj = 1;\n\t\twhile(j <= i){\n\t\t\tprintf(\"%d*%d=%d\", j,i,i*j);\n\t\t\t//i,j会输出9*1= 9*2=\n\t\t\t//我们想要输出1*9= 2*9=\n\t\t\t//如果i*j小于10，比如1*1=1 小于 10\n\t\t\tif( i*j < 10){\n\t\t\t\t//输出三个空格\n\t\t\t\tprintf(\"   \");\n\t\t\t} else {\n\t\t\t\t//输出两个空格\n\t\t\t\tprintf(\"  \");\n\t\t\t}\n\t\t\tj++;\n\t\t}\n\t\t//还需要在每一个行加个回车\n\t\tprintf(\"\\n\");\n\t\ti++;\n\t}\n\n\treturn 0;\n}\n#include<stdio.h>\nint main() {\n\tint m,n;\n\tint i;\n\t//个数\n\tint cnt = 0;\n\tint sum = 0;\n\n\tscanf(\"%d %d\", &m, &n);\n\t//如果m是 1，单独做一个特殊的判断\n\tif(m == 1)\n\t\tm=2;\n\n\tfor(i=m; i<=n; i++){\n\t\tint isPrime = 1;\n\t\tint k;\n\t\t//需要有另外一个循环去证明 i是不是isPrime\n\t\tfor(k=2; k<i-1; k++){\n\t\t\tif(i%k == 0){\n\t\t\t\tisPrime = 0;\n\t\t\t\tbreak;\n\t\t\t}\n\t\t}\n\t\t// 判断 i 是否素数\n\t\tif(isPrime) {\n\t\t\tcnt++;\n\t\t\tsum+=i;\n\t\t}\n\t}\n\tprintf(\"%d %d\\n\", cnt, sum);\n\n\treturn 0;\n}\n#include <stdio.h>\nint main(){\n\t//随机数，猜测的最大次数\n    int number,n;\n\t//用户每次猜测的数字\n\tint inp;\n\t//  finished为 1 则表示猜中，0 没猜中\n    int finished = 0;\n    // 记录猜测次数\n    int cnt = 0;\n    scanf(\"%d %d\",&number,&n);\n    while(scanf(\"%d\",&inp)){\n        cnt++;\n        //如果这个数小于0 或 猜测次数大于最大猜测次数 ,则输入不合法\n        if(inp < 0 || cnt > n){\n            printf(\"Game Over\\n\");\n            break;\n        }\n        // 输入猜测数太大\n        if(inp > number){\n            printf(\"Too big\\n\");\n        // 输入猜测数太小\n        }else if(inp < number){\n            printf(\"Too small\\n\");\n        // 输入猜测数与随机数相等\n        }else{\n            // 1次成功\n            if(cnt == 1){\n\t\t\t\tprintf(\"Bingo!\\n\");\n             // 3次以内成功\n\t\t\t}else if(cnt <= 3){\n\t\t\t\tprintf(\"Lucky You!\\n\");\n            // 3次以上成功\n\t\t\t}else{\n\t\t\t\tprintf(\"Good Guess!\\n\");\n\t\t\t}\n            finished = 1;\n            if(finished == 1) break;\n        }\n    }\n    return 0;\n}\n#include<stdio.h>\nint main() {\n\tint n;\n    //分子，分母\n\tdouble dividend,divisor;\n\tdouble sum = 0.0;\n\tint i;\n\tdouble t;\n\n\tscanf(\"%d\", &n);\n\t// n = 2000;\n\tdividend = 2;\n\tdivisor = 1;\n\tfor(i = 1; i <= n; i++){\n\t\tsum += dividend/divisor;\n\t\tt = dividend;\n\t\tdividend = dividend + divisor;\n\t\tdivisor = t;\n\t}\n\tprintf(\"%.2f\\n\", sum);\n\n\treturn 0;\n}\n#include<stdio.h>\nint main() {\n\t//分子，分母\n\tint dividend,divisor;\n\tscanf(\"%d/%d\", &dividend, &divisor);\n\n\tint a = dividend;\n\tint b = divisor;\n\tint t;\n\t//辗转相除法算出最大公约数\n\twhile(b > 0){\n\t\tt = a % b;\n\t\ta = b;\n\t\tb = t;\n\t}\n\tprintf(\"%d/%d\\n\", dividend/a, divisor/a);\n\n\treturn 0;\n}\n#include<stdio.h>\nint main() {\n\tint x;\n\tscanf(\"%d\", &x);\n\n\tif(x < 0){\n\t\tprintf(\"fu \");\n\t\tx = -x;\n\t}\n\tint mask = 1;\n\tint t = x;\n\t//辗转相除法算出最大公约数\n\twhile(t > 9){\n\t\tt /= 10;\n\t\tmask *= 10;\n\t}\n\n\tdo{\n\t\tint d = x / mask;\n\t\tswitch(d){\n\t\t\tcase 0: printf(\"ling\"); break;\n\t\t\tcase 1: printf(\"yi\"); break;\n\t\t\tcase 2: printf(\"er\"); break;\n\t\t\tcase 3: printf(\"san\"); break;\n\t\t\tcase 4: printf(\"si\"); break;\n\t\t\tcase 5: printf(\"wu\"); break;\n\t\t\tcase 6: printf(\"liu\"); break;\n\t\t\tcase 7: printf(\"qi\"); break;\n\t\t\tcase 8: printf(\"ba\"); break;\n\t\t\tcase 9: printf(\"jiu\"); break;\n\t\t}\n\t\t// 最后不要有行末的空格\n\t\tif (mask > 9) printf(\" \");\n\t\tx %= mask;\n\t\tmask /= 10;\n\t} while(mask > 0);\n\tprintf(\"\\n\");\n\n\treturn 0;\n}\n#include<stdio.h>\nint main() {\n\tint a,n;\n\tscanf(\"%d %d\", &a, &n);\n\n\tint sum = 0;\n\tint i;\n\t//每一轮的数字\n\tint t = 0;\n\t// 0*10+2 2*10+2 (2*10+2)*10+2\n\tfor(i=0; i<n; i++){\n\t\tt = t*10+a;\n\t\tsum += t;\n\t}\n\tprintf(\"%d\\n\", sum);\n\n\treturn 0;\n}\n```"},{"page":54,"body":"**本节完整补充**\n\n**引言**\n\n* C 语言是有类型的语言，C 语言的变量必须在使用前**定义**并**确定类型**。\n\n* C 以后的语言向两个方向发展：\n\n* C++/Java 更强调类型，对类型的检查更严格\n\n* JavaScript、Python、PHP 不看重类型，甚至不需要事先定义\n\n**类型安全**\n\n* 支持强类型的观点认为明确的类型有助于尽早发现程序中的简单错误\n\n* 反对强类型的观点认为过于强调类型迫使程序员面对底层、实现而非事务逻辑\n\n* 总的来说，早期语言强调类型，面向底层的语言强调类型\n\n* C 语言需要类型，但是对类型的安全检查并不足够\n\n**C 语言的类型(下划线的是 C99 的类型)**\n\n* 整数：char、short、int、long、 long long 、 bool (逻辑)\n\n* 浮点数：float、double、 long double\n\n* 指针\n\n* 自定义类型\n\n**类型有何不同**\n\n* 类型名称：int、long、double\n\n* 输入输出时的格式化：%d、%ld、%lf\n\n* 所表达的数的范围：char < short < int < float < double < long double\n\n* 内存中所占据的大小：1 个字节到 16 个字节\n\n* 内存中的表达形式：二进制数（补码）、编码\n\n**sizeof()**\n\n* 是**长度运算符**，给出某个类型或变量在内存中所占据的字节数，如`sizeof(int)`和`sizeof(i)`。\n\n* sizeof 是一个**单目**运算符\n\n* 是静态运算符，它的结果在编译时刻就决定了，可以增强程序的可移植性，使之不受具体计算机数据类型长度的限制\n\n* 不要在 sizeof 的括号里做运算，这些运算不会做的\n\n```c\nint a = 25;\nprintf(\"sizeof(a + 1.0) = %ld\", sizeof(a + 1.0));\n//输出结果为 8，因为 1.0 是 double，a+1.0 运算结果也是 double，sizeof 在编译时就会确定为 8 了。\n```\n\n**2.整数**\n\n* char：1 字节(8 bit)\n\n* short：2 字节\n\n* int：取决于编译器（CPU），通常的意义是“1 个字”，在 32 位机器和 64 机器中 int 类型都占用 4 个字节。\n\n* long：取决于编译器（CPU），通常的意义是“1 个字”\n\n* long long：8 字节\n\n**C语言的类型**\n\n**整数**\n\n1. char：一字节（8比特）-128至127\n\n2. short：2字节 -32768至32767\n\n3. int：取决于编译器（CPU），通常的意义都是\"1个字\"\n\n4. long：取决于编译器（CPU），通常的意义是\"1个字\"(4或8字节)\n\n5. long long：8字节\n\n***整数的内部表达**\n\n1. 计算机内部一切都是二进制\n\n2. 18 -> 00010010\n\n3. 0 -> 00000000\n\n4. -18 ->?\n\n***如何表示负数**\n\n1. 十进制用\"-\"来表示负数，在做计算的时候\n\n2. 加减是做相反的运算\n\n3. 乘除时当作正数，计算完毕后对结果的符号取反\n\n***二进制负数**\n\n1. 1个字节可以表达的数：0000 0000 - 1111 1111(0-255)\n\n三种方案：\n\n1. 仿照十进制，有一个特殊的标志表示负数\n\n2. 取中间的数为0，如100 0000表示0，比它小的是负数，比它大的是正数\n\n3. 补码(实际上使用的，方案1和2都有缺陷)\n\n***补码**\n\n补码的意义就是拿补码和原码可以加出一个溢出的0\n\n例子：因为0 - 1->-1,所以-1 = (1)0000 0000 - 0000 0001 ->1111 1111\n\n1111 1111被当作纯二进制看待时，是255，被当作补码看待时是-1\n\n同理，对于-a，其补码就是0-a，实际是2的n次方-a，n是这种类型的位数\n\n**数的范围**\n\n1. 对于一个字节(8位)，可以表达的是：0000 0000 ->1111 1111\n\n2. 其中0000 0000->0 、1111 1111至1000 0000 ->-1至-128 、 0000 0001至0111 1111->1至127\n\n**浮点数**\n\nfloat、double、long double\n\n**逻辑**\n\nbool\n\n**指针**\n\n**自定义类型**\n\n1. 类型名称：int、long、double\n\n2. 输入输出时的格式化：%d、%ld、%lf\n\n3. 所表达的数的范围：char<short<int<float<double\n\n4. 内存中所占据的大小：1个字节到16个字节\n\n5. 内存中的表达形式：二进制数(补码)、编码\n\n**sizeof**\n\n1. 是一个运算符，给出某个类型或者变量在内存中所占据的字节数\n\n例如：sizeof(int)、size(i)\n\n1. 是静态运算符，它的结果在编译时刻就决定了\n\n2. 不要在sizeof的括号里做运算，这些运算不会进行的\n\n**unsigned**\n\n1. 在整形类型前加上unsigned使得它们变成无符号的整数\n\n2. 内部的二进制表达没变，变的是如何看待它们。如何输出\n\n3. 1111 1111对于char来说是-1，对于unsigned char来说是255\n\n4. 如果一个字面量常数想要表达自己是unsigned，可以在后面加上U或者u，255U\n\n5. 用L或者l表示long( long)\n\n6. unsigned初衷并非扩展数能表达的范围，而是为了做纯二进制运算，主要是为了移位\n\n**整数越界**\n\n整数是以纯二进制方式进行计算的，所以：\n\n1. 1111 1111+1 -> 1 0000 0000 ->0\n\n2. 0111 1111+1 -> 1 0000 0000 ->-128\n\n3. 1000 0000-1 -> 0111 1111 ->127\n\n**整数的输入输出**\n\n只有两种形式：int或者long long\n\n1. %d：int\n\n2. %u：unsigned\n\n3. %ld：long long\n\n4. %lu：unsigned long long\n\n**8进制和16进制**\n\n1. 一个以0开始的数字字面量是8进制\n\n2. 一个以0x开始的数字字面量是16进制\n\n3. %o用于8进制，%x用于16进制\n\n4. 8进制跟16进制只是如何把数字表达为字符串，与内部如何表达数字无关\n\n5. 16进制很适合表达二进制数据，因为4位二进制正好是一个16进制位\n\n6. 8进制的一位数字正好表达3位二进制\n\n7. 因为早期计算机的字长是12的倍数，而非8\n\n**选择整数类型**\n\n1. 为什么整数要有那么多种？为了准确表达内存，做底层程序的需要\n\n2. 没有特殊需要就选择int\n\n3. 现在CPU的字长普遍是32位或者64位，一次内存读写就是一个int，一次计算也是一个int，选择更短的类型不会更快，甚至可能更慢\n\n4. 现代的编译器一般会设计内存对齐，所以更短的类型实际在内存中有可能也占据一个int的大小(虽然sizeof告诉你更小)\n\n5. unsigned与否只是输出的不同，内部计算是一样的"},{"page":55,"body":"**本节完整补充**\n\n**整数的内部表达**\n\n* 计算机内部一切都是二进制，正数、零和负数各自都是怎么表达的呢？\n\n* 正数和零比较简单，但是负数用二进制表示比较复杂。\n\n* 正数的原码、反码和补码相同，即符号位是 0，其余各位表示数值。\n\n* 十进制用“-”来表示负数，在做计算的时， 加减是做相反的运算。乘除时当作正数，计算完毕后对结果的符号取反\n\n**二进制负数**\n\n* 1 个字节可以表达的数：00000000 — 11111111 (0-255)\n\n三种方案：\n\n1. 仿照十进制，有一个特殊的标志表示负数\n\n2. 取中间的数为 0，如 1000000 表示 0，比它小的是负数，比它大的是正数\n\n3. 补码\n\n**补码**\n\n* 补码的意义就是拿补码和原码可以加出一个溢出的“零”\n\n* 考虑-1，我们希望-1＋1→0。如何能做到？\n\n0 → 00000000 1 → 00000001\n\n11111111 + 00000001 → 100000000\n\n因为 0 - 1 = -1，\n\n所以，-1＝(1)00000000 - 00000001 → 11111111\n\n11111111 被当作纯二进制看待时，是 255，被当作补码看待时是-1\n\n* 同理，对于-a，其补码就是 0 - a，实际是2^n - a，n 是这种类型的位数\n\n***整数的内部表达**\n\n1. 计算机内部一切都是二进制\n\n2. 18 -> 00010010\n\n3. 0 -> 00000000\n\n4. -18 ->?\n\n***如何表示负数**\n\n1. 十进制用\"-\"来表示负数，在做计算的时候\n\n2. 加减是做相反的运算\n\n3. 乘除时当作正数，计算完毕后对结果的符号取反\n\n***二进制负数**\n\n1. 1个字节可以表达的数：0000 0000 - 1111 1111(0-255)\n\n2. 取中间的数为0，如100 0000表示0，比它小的是负数，比它大的是正数\n\n3. 补码(实际上使用的，方案1和2都有缺陷)\n\n***补码**\n\n补码的意义就是拿补码和原码可以加出一个溢出的0\n\n例子：因为0 - 1->-1,所以-1 = (1)0000 0000 - 0000 0001 ->1111 1111\n\n1111 1111被当作纯二进制看待时，是255，被当作补码看待时是-1\n\n同理，对于-a，其补码就是0-a，实际是2的n次方-a，n是这种类型的位数\n\n**数的范围**\n\n1. 对于一个字节(8位)，可以表达的是：0000 0000 ->1111 1111\n\n2. 其中0000 0000->0 、1111 1111至1000 0000 ->-1至-128 、 0000 0001至0111 1111->1至127\n\n整数在计算机内部以二进制存储，但“如何解释这串位”取决于类型和有符号/无符号规则。以 8 位为例：\n\n- 无符号 0 到 255：`00000000` 到 `11111111`。\n\n- 有符号补码 0 到 127：`00000000` 到 `01111111`。\n\n- 有符号补码 -1 到 -128：`11111111` 到 `10000000`。\n\n补码的核心不是背公式，而是理解数学关系：`-a` 的补码可以看成 `0 - a` 的二进制结果，即 `2^n - a` 在 n 位范围内的表示。例如 8 位中 `-1` 是 `11111111`，`-1 + 1` 得到 `100000000`，超出 8 位的进位被丢掉，剩下 `00000000`。所以补码让加法无需单独处理符号，这也是计算机选择它的原因。\n\n用 C 观察时，不要依赖打印出的十进制值来猜测二进制位。可以这样看：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    char c = -1;\n    unsigned char u = (unsigned char)c;\n    printf(\"%hhu\\n\", u);\n    return 0;\n}\n```\n\n这里 `%hhu` 把 `char` 当作无符号数输出，8 位全 1 会显示 255；而 `printf(\"%d\", c)` 根据实现可能得到 -1。`char` 是否带符号由编译器决定，显式使用 `signed char` / `unsigned char` 更清晰。\n\n需要掌握的检查点包括：最高位的作用、位数变化后符号是否扩展、无符号数和有符号数混算、加法溢出。范围边界是理解补码的关键，而不是只记 `-128` 和 `127` 两个数字。"},{"page":56,"body":"**本节完整补充**\n\n**整数的范围**\n\n* 对于一个字节（8 位），可以表达的是：00000000 ~ 11111111\n\n* 其中：00000000 —> 0\n\n* 11111111 ~ 10000000 —> -1 ~ -128\n\n* 00000001 ~ 01111111 —> 1 ~ 127\n\n```c\nchar c = 255;\nint i = 255;\nprintf(\"c = %d, i = %d\\n\", c, i);\n//c 是 11111111\n//i 是 00000000 00000000 00000000 11111111\n/*所以输出结果是 c = -1， i = 255*/\n```\n\n**unsigned 类型**\n\n在整形类型前加上 unsigned 使得它们成为无符号的整数，即不把他看成补码，看成纯二进制。\n\n内部的**二进制表达没变**，**变的是如何看待它们**，比如 11111111，对于 char，是-1，对于 unsigned char，是 255。\n\n如何输出？\n\n* 如果一个字面量常数想要表达自己是 unsigned，可以在后面加 u 或 U，比如 255U。\n\n* 用 l 或 L 表示 long(long)\n\n* unsigned 的初衷并非扩展数能表达的范围，而是为了做纯二进制运算，主要是为了移位\n\n整型数据类型\n\n- 英文类型名 · 中文类型名 · 数据长度 · 取值范围\n\n- int · [有符号]整型 · 32 位 · -2147483648\\sim2147483648(-2^{31}\\sim2^{31}-1)\n\n- short [int] · [有符号]短整型 · 16 位 · -32768\\sim32767(-2^{15}\\sim2^{15}-1)\n\n- long [int] · [有符号]长整型 · 32 位 · -2147483648\\sim2147483648(-2^{31}\\sim2^{31}-1)\n\n- unsigned [int] · 无符号整型 · 32 位 · 0\\sim4294967295(0\\sim2^{32}-1)\n\n- unsigned short [int] · 无符号短整型 · 16 位 · 0\\sim65535(0\\sim2^{16}-1)\n\n- unsigned long [int] · 无符号长整型 · 32 位 · 0\\sim4294967295(0\\sim2^{32}-1)\n\n注：方括号[ ]中的内容可以省略\n\n**整数越界**\n\n整数是以纯二进制方式进行计算的，所以：\n\n11111111 + 1 —> 100000000 —> 0\n\n01111111 + 1 —> 10000000 —> -128\n\n10000000 - 1 —> 01111111 —> 127\n\n整数越界\n\n**sizeof**\n\n1. 是一个运算符，给出某个类型或者变量在内存中所占据的字节数\n\n例如：sizeof(int)、size(i)\n\n1. 是静态运算符，它的结果在编译时刻就决定了\n\n2. 不要在sizeof的括号里做运算，这些运算不会进行的\n\n**unsigned**\n\n1. 在整形类型前加上unsigned使得它们变成无符号的整数\n\n2. 内部的二进制表达没变，变的是如何看待它们。如何输出\n\n3. 1111 1111对于char来说是-1，对于unsigned char来说是255\n\n4. 如果一个字面量常数想要表达自己是unsigned，可以在后面加上U或者u，255U\n\n5. 用L或者l表示long( long)\n\n6. unsigned初衷并非扩展数能表达的范围，而是为了做纯二进制运算，主要是为了移位\n\n1. 1111 1111+1 -> 1 0000 0000 ->0\n\n2. 0111 1111+1 -> 1 0000 0000 ->-128\n\n3. 1000 0000-1 -> 0111 1111 ->127"},{"page":57,"body":"**本节完整补充**\n\n**整数的输入输出**\n\n* 只有两种形式：int 或 long long，长度大于 int 的，选后者，反之选前者。\n\n```c\n%d：int\n%u：unsigned\n%ld：long long\n%lu：unsigned long long\n```\n\n```c\nchar c = -1;\nint i = -1;\nprintf(\"c = %u, i = %u\\n\", c, i);\n//两者输出都为 4294967295\n//二者 -1 的补码都是二进制全 1，按无符号数输出就是最大的正数。\n```\n\n**8 进制和 16 进制**\n\n* 一个以 0 开始的数字字面量是 8 进制，一个以 0x 开始的数字字面量是 16 进制\n\n* %o 用于 8 进制，%x 用于 16 进制，%X 可以输出以大写字母为格式的 16 进制。\n\n* 8 进制和 16 进制只是如何把数字表达为字符串，与内部如何表达数字无关，内部永远都是二进制存储。\n\n* 16 进制很适合表达二进制数据，因为 4 位二进制正好是一个 16 进制位；8 进制的一位数字正好表达 3 位二进制。\n\n* 因为早期计算机的字长是 12 的倍数，而非 8 的倍数\n\n表-整型数据格式控制说明\n\n数据类型\n\n输入输出形式\n\n十进制\n\n八进制\n\n十六进制\n\nint\n\n%d\n\n%o\n\n%x\n\nlong\n\n%ld\n\n%lo\n\n%lx\n\nunsigned\n\n%u\n\nunsigned long\n\n%lu\n\n表-整型数据基本格式控制说明的含义\n\n- 格式 · 含义\n\n- %d · 以十进制形式输入输出一个整数\n\n- %u · 以十进制形式输入输出一个无符号整数\n\n- %o · 以八进制形式输入输出一个整数\n\n- %x · 以十六进制形式输入输出一个整数\n\n只有两种形式：int或者long long\n\n1. %d：int\n\n2. %u：unsigned\n\n3. %ld：long long\n\n4. %lu：unsigned long long\n\n**8进制和16进制**\n\n1. 一个以0开始的数字字面量是8进制\n\n2. 一个以0x开始的数字字面量是16进制\n\n3. %o用于8进制，%x用于16进制\n\n4. 8进制跟16进制只是如何把数字表达为字符串，与内部如何表达数字无关\n\n5. 16进制很适合表达二进制数据，因为4位二进制正好是一个16进制位\n\n6. 8进制的一位数字正好表达3位二进制\n\n7. 因为早期计算机的字长是12的倍数，而非8\n\n整数的输入输出格式必须和变量类型匹配。最常见的对应关系是：`int` 用 `%d`，`unsigned int` 用 `%u`，`long` 用 `%ld`，`long long` 用 `%lld`，`unsigned long long` 用 `%llu`。\n\n下面这个例子会暴露类型匹配问题：\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    long long value = 9000000000LL;\n    printf(\"%lld\\n\", value);\n    return 0;\n}\n```\n\n如果写成 `%d`，格式化函数可能只读取部分字节，输出会不可预测；如果变量是 `long long` 却只用 `%ld`，在宽度不同的平台上也可能出错。\n\n八进制和十六进制是“文本表示”而非内部存储格式：`017` 是八进制字面量，`0x17` 是十六进制字面量；`%o` 输出八进制，`%x` 输出十六进制，`%X` 输出大写十六进制。16 进制一位正好对 4 个二进制位，8 进制一位对 3 个二进制位，所以它们常用来观察位模式。\n\n格式化字符串还可以带宽度：`%5d` 右对齐至少 5 个字符，`%-5d` 左对齐，`%05d` 用前导零补齐。写程序时先确认变量类型，再选择格式符，最后检查题目要求的空格、换行和宽度。"},{"page":58,"body":"**本节完整补充**\n\n**选择整数的类型**\n\n* 为什么整数要有那么多种？→ 为了准确表达内存，做底层程序的需要\n\n* 没有特殊需要，就选择 int，因为现在的 CPU 的字长普遍是 32 位或 64 位，一次内存读写就是一个 int，一次计算也是一个 int，选择更短的类型不会更快，甚至可能更慢\n\n* 现代的编译器一般会设计**内存对齐**，所以更短的类型实际在内存中有可能也占据一个 int 的大小(虽然 sizeof 告诉你更小)\n\n* unsigned 与否只是输出的不同，内部计算是一样的。\n\n**3.浮点数**\n\n**选择整数类型**\n\n1. 为什么整数要有那么多种？为了准确表达内存，做底层程序的需要\n\n2. 没有特殊需要就选择int\n\n3. 现在CPU的字长普遍是32位或者64位，一次内存读写就是一个int，一次计算也是一个int，选择更短的类型不会更快，甚至可能更慢\n\n4. 现代的编译器一般会设计内存对齐，所以更短的类型实际在内存中有可能也占据一个int的大小(虽然sizeof告诉你更小)\n\n5. unsigned与否只是输出的不同，内部计算是一样的\n\n整数类型的选择不是“哪个数据类型更高级”，而是根据范围、存储、性能和可移植性做权衡。大多数教学程序用 `int` 就够了；现代 CPU 一次读写、一次计算往往是 32 位或 64 位，选择 `short` 或 `char` 不一定更快，反而可能因类型转换和内存对齐产生额外开销。\n\n建议按题目约束判断：\n\n- 数值范围明确很小：`short` 或 `char` 可用于数组、硬件协议、位图。\n\n- 普通整数：默认 `int`。\n\n- 可能超过 20 亿：使用 `long long`，并匹配 `%lld`。\n\n- 需要精确宽度：使用 `int32_t`、`uint64_t` 等固定宽度类型，适合文件格式和跨平台数据。\n\n- 只表示非负数：可以使用 `unsigned`，但和 signed 混算时要注意隐式转换和比较规则。\n\n`sizeof` 只能告诉你一个类型占用多少字节，不代表它在内存中一定按这个大小紧凑排列。编译器可能为了对齐在结构体中插入填充，也可能在表达式求值前把 `char` / `short` 提升为 `int`。需要精确布局时，应结合编译器的对齐规则和结构体成员顺序。\n\n选择类型时问三个问题：最小值、最大值、输入的格式符是什么。如果题目给出 `n <= 10^9`，用 `int` 通常足够；如果给出 `n <= 10^18`，应直接使用 `long long`，避免在运算中途溢出。\n\n选择整数类型时先看数据的数学范围，再看输入输出格式，最后才看性能。普通 `int` 在常见的 32 位平台能表示约 `-2147483648` 到 `2147483647`，接近 20 亿；若题目允许 `10^9` 相加减，结果可能超过边界，应直接使用 `long long`。`long long` 在 C99 及之后标准中至少为 64 位，配合 `%lld` 和 `%llu` 使用。\n\n需要精确宽度时，可以包含 ` ` 并使用 `int8_t`、`uint16_t`、`int32_t`、`uint64_t`。它们适合文件格式、网络协议、位图和跨平台数据；`int`、`long` 的宽度取决于编译器，更适合普通业务代码。`char` 是否带符号由编译器决定，要明确表示字节时用 `signed char` 或 `unsigned char`。\n\n还有两个容易被忽略的规则：运算中较小的整数通常会被提升为 `int`，混合有符号和无符号时可能发生隐式转换，导致负数变成很大的正数；结构体成员为了对齐可能被填充，所以 `sizeof` 之和不一定等于结构体总大小，不能想当然地按成员字节数计算布局。\n\n做类型选择时，把“最小值、最大值、输入格式、输出格式、是否跨平台、是否需要精确字节布局”列成一张表。题目给出 `n <= 10^9` 时先考虑 `int`；给出 `n <= 10^18` 或可能使用 64 位中间结果时，直接使用 `long long`，不要等溢出后再改。"},{"page":59,"body":"**本节完整补充**\n\n**浮点类型**\n\n* 浮点类型有两种，float 和 double。\n\n表-浮点数数据类型\n\n- 类型 · 数据长度 · 表示范围 · 有效数字\n\n- float(单精度浮点型) · 32 位 · ±(1.20×10^{-38} \\sim 3.40×10^{38}),0,±inf,nan · 7\n\n- double(双精度浮点型) · 64 位 · ±(2.20×10^{-308} \\sim 1.79×10^{308}),0,±inf,nan · 15\n\n* 用这个类型表达有效数字之外的数是不准确的。\n\n* inf 是 infinity，表示无穷大的意思。\n\n* nan 是 not a number，表示不是一个有效的数字。\n\n* printf 输出 inf 表示超过范围的浮点数：±∞\n\n* printf 输出 nan 表示不存在的浮点数\n\n```c\nprintf(\"%f\\n\", 25.0 / 0.0);\nprintf(\"%f\\n\", -25.0 / 0.0);\nprintf(\"%f\\n\", -0.0 / 0.0);\nprintf(\"%d\\n\", 25 / 0);\n//结果分别是：inf、-inf、nan、编译失败\n//因为浮点数虽然有效数字不包含无穷大，但是专门设计了一种情况表示无穷大，而整型无法表示无穷大。\n```\n\n**浮点的输入输出**\n\n表-浮点数格式控制说明\n\n函数\n\n数据类型\n\n格式\n\n含义\n\nprintf\n\nfloat\n\n%f\n\n以小数形式输出浮点数（保留 6 位小数）\n\ndouble\n\n%e\n\n以指数形式输出浮点数（小数点前有且仅有一位非零的数字）\n\nscanf\n\n以小数形式或指数形式输入一个单精度浮点数\n\n%lf\n\n以小数形式或指数形式输入一个双精度浮点数\n\n%le\n\n* %e、%E 是按科学计数法的格式输出。\n\n* printf 的 %f 说明符的确既可以输出 float 型又可以输出 double 型。 根据\"默认参数提升\"规则（在 printf 这样的函数的可变参数列表中 ，不论作用域内有没有原型，都适用这一规则）float 型会被提升为 double 型。因此 printf()只会看到双精度数。\n\n* 对于 scanf，情况就完全不同了，它接受指针，这里没有类似的类型提升。（通过指针）向 float 存储和向 double 存储大不一样，因此，scanf 区别 %f 和 %lf。double 型数据的输入格式控制必须用 %lf 或 %le。\n\n* 在输出格式控制说明中可以加**宽度限定词**，指定浮点型数据的输入输出。例如输出格式控制说明`%m.nf`，指定输出浮点型数据时保留 n 位小数，且输出宽度是 m（包括符号位和小数点）。若数据的实际位数小于 m，左端补空格，若大于 m，按实际位数输出。\n\n**浮点数**\n\n- 类型 · 字长 · 范围 · 有效数字\n\n- float · 32 · 正负(1.20×100,正负inf，NaN) · 7\n\n- double · 64 · 正负(2.20×100,正负inf，NaN) · 15\n\n- 类型 · scanf · printf\n\n- float · %f · %f，%e\n\n- double · %lf · %f，%e\n\n**科学计数法**\n\n1. 可选的+或者-符号\n\n2. 可以用e或者E\n\n3. 整个词不能有空格\n\n4. 小数点也是可选的\n\n5. 符号可以是正负也可以省略(表示+)\n\n6. 例如：-5.67E+16\n\n**输出精度**\n\n在%和f之间加上.n可以指定输出小数点后几位，这样的输出是做四舍五入的\n\n1. printf(\"%.3f\\n\",-0.0049);\n\n2. printf(\"%.30f\\n\",-0.0049);\n\n3. printf(\"%.3f\\n\",-0.00 49);\n\n**超出范围的浮点数**\n\n1. printf输出inf表示超过范围的浮点数：+无穷\n\n2. printf输出nan表示不存在的浮点数\n\n**浮点运算的精度**\n\n1. 带小数点的字面量是double而非float\n\n2. float需要用f或者F后缀来表明身份\n\n**浮点数的内部表达**\n\n1. 浮点数在计算时是由专用的硬件部件实现的\n\n2. 计算double和float所用的部件是一样的\n\n**选择浮点类型**\n\n1. 如果没有特殊需要，只使用double\n\n2. 现代CPU能直接对double做硬件运算，性能不会比float差，在64位的机器上，数据存储的速度也不比float慢"},{"page":60,"body":"**本节完整补充**\n\n**科学计数法**\n\n* 之前，我们在 C 编程时对浮点型常量使用的都是**浮点表示法**，实数由正号、负号、阿拉伯数字 0~9 和小数点组成，必须有小数点，且小数点前后至少有一边要有数字。实数的浮点表示法又称实数的小数形式。\n\n* 下面介绍用于很大或者很小的浮点型变量的表示方法——**科学计数法**。\n\n```c\n-5.67E+16\n6.026E-27\n2e4\n```\n\n① 可选+、-号\n\n② E 前必须有数，但这个数是小数的话可以省略整数部分或者小数部分，是整数可以省略小数点。\n\n③ E 可以用 e 或 E\n\n④ E 后也必须有数，符号+、-皆可，而且这个数必须为整数\n\n⑤ 整个词中间不能有空格\n\n**输出精度**\n\n* 由于 float 和 double 的特性，不是所有小数都可以在计算机内部准确表达的，有时候实际存储的数与想表达的数是不一样的。\n\n* 在 % 和 f 之间加上 .n 可以指定输出小数点后几位，这样的输出是做**四舍五入**的\n\n```c\nprintf(\"%.3f\\n\", -0.0049);\n\nprintf(\"%.30f\\n\", -0.0049);\n\nprintf(\"%.3f\\n\", -0.00049);\n```\n\n**浮点运算精度**\n\n```c\nfloat a, b, c;\n\na = 1.345f;\nb = 1.123f;\nc = a + b;\nif (c == 2.468)\n    printf(\"相等\\n\");\nelse\n    printf(\"不相等，c = %.10f，或 %f\", c, c);\n```\n\n* 带小数点的字面量是 double 类型，float 要用 f 或者 F 后缀来表明身份。\n\n* f1 == f2 可能失败，需要用：fabs(f1-f2) < 1e-12，一般比有效数字的精度小即可，即 float 为 1e-8。\n\n* **数值精度**和**取值范围**是两个不同的概念，比如 1234567.89 在单精度浮点型数据的取值范围内，但它的有效数字超过了 8 位，如果将它赋值给单精度浮点型变量，该变量的值就是 1234567.80，最后一位是一个随机数，损失了有效数字，降低了精度。\n\n**浮点数的内部表达**\n\n图-double 的内部表达\n\n* 存储浮点型(实型)数据时，分为**符号位**、**阶码**和**尾数**三部分。\n\n* 浮点数在计算时是由专用的硬件部件实现的\n\n* 计算 double 和 float 所用的部件是一样的\n\n**选择浮点类型**\n\n* 如果没有特殊需要，只使用 double\n\n* 现代 CPU 能直接对 double 做硬件运算，性能不会比 float 差，在 64 位的机器上，数据存储的速度也不比 float 慢\n\n**4.类型转换**\n\n**浮点的输入输出**\n\n- 类型 · scanf · printf\n\n- float · %f · %f，%e\n\n- double · %lf · %f，%e\n\n1. 可选的+或者-符号\n\n2. 可以用e或者E\n\n3. 整个词不能有空格\n\n4. 小数点也是可选的\n\n5. 符号可以是正负也可以省略(表示+)\n\n6. 例如：-5.67E+16\n\n在%和f之间加上.n可以指定输出小数点后几位，这样的输出是做四舍五入的\n\n1. printf(\"%.3f\\n\",-0.0049);\n\n2. printf(\"%.30f\\n\",-0.0049);\n\n3. printf(\"%.3f\\n\",-0.00 49);\n\n**超出范围的浮点数**\n\n1. printf输出inf表示超过范围的浮点数：+无穷\n\n2. printf输出nan表示不存在的浮点数\n\n**浮点运算的精度**\n\n1. 带小数点的字面量是double而非float\n\n2. float需要用f或者F后缀来表明身份\n\n1. 浮点数在计算时是由专用的硬件部件实现的\n\n2. 计算double和float所用的部件是一样的\n\n1. 如果没有特殊需要，只使用double\n\n2. 现代CPU能直接对double做硬件运算，性能不会比float差，在64位的机器上，数据存储的速度也不比float慢"},{"page":61,"body":"**本节完整补充**\n\n**字符型数据**\n\n* **字符型常量**指的是单个字符，用一对**单引号**及其括起来的字符来表示字符字面量，每个字符都有一个唯一的次序值，即 **ASCII 码**。`scanf()`和`printf()`里用`%c`来输入和输出字符。\n\n* `''、''、'\\n'、'\\063'、'\\x41'`都是合法的字符常量，第二个两个单引号之间没任何东西，叫做**空字符常量**（也是一个字符）。第三个是合法的转义字符常量，`'/'`是合法的字符常量，但`'\\'`是非法的字符常量。`'\\ddd'`表示 1 到 3 位 8 进制整数所代表的字符(d 不能大于等于 8)；形式`'\\xhh'`表示 1 到 2 位 16 进制整数所代表的字符。C 语言字符集中的任何一个字符均可用转义字符来表示。上面的`'\\ddd'`和`'\\xhh'`正是为此而提出的，分别为八进制和十六进制的 ASCII 代码。\n\n* 有一些字符，比如回车符、退格符等控制码，它们不能在屏幕上显示，也无法从键盘上输入，只能用**转义字符**来表示。转义字符由反斜杠加上一个字符或者数字组成，，它表示将反斜杠后面的字符或者数字转换成别的意义。它在形式上由多个字符组成，但实际只代表一个字符。\n\n* 数字字符'0'，'1'，...，'9'、大写字母'A'，'B'，...，'Z'、小写字母'a'，'b'，...，'c'，**分别**都是按 ASCII 码升序连续排列，所以判断一个字符 c 是否为大写字母，可以使用`c>='A' && c <= 'Z'`来判断。\n\n表-常见 ASCII 码表\n\n- 字符 · ASCII 码\n\n- 空字符('\\0') · 0\n\n- 空格(' ') · 32\n\n- 0('0') · 48\n\n- A('A') · 65\n\n- a('a') · 97\n\n* 可以知道，数字、大写字母、小写字母的 ASCII 码三块，块内连续升序排列但块间不是连续的，而且小写字母的 ASCII 码比大写字母更大。\n\n```c\n    //ch 是空格或者回车\n    ch == ' ' || ch == '\\n'  //要两个 ch 分别判断，和文字描述有点区别，不能 ch == ' ' || '\\n'\n    //number 是偶数\n    number % 2 == 0\n    //year 是闰年，即 year 能被 4 整除但不能被 100 整除，或 year 能被 400 整除\n    (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)\n    //ch 不是小写字母，注意是“或”\n    ch < 'a' || ch > 'z'\n    //ch 是英文字母\n    (ch >= 'A' && ch  <= 'Z') || (ch >= 'a' && ch  <= 'z')\n    //x 为零\n    x == 0\n    !x\n    //x 和 y 不同时为零(可以列真值表验证)\n    !(x == 0 && y == 0)\n    x != 0 ||  y != 0\n    x || y\n```\n\n* **字符型变量**在定义时用类型名 char，例如`char op;`定义了一个字符型变量 op，它的值是字符型数据。`op='+'`将字符型常量`'+'`赋给字符型变量 op。char 是一种有符号的**整数**类型，取值范围为 -128~127。\n\n* `scanf()`和`printf()`也可以处理字符型数据的输入输出，在函数调用的格式控制串中相应的格式控制说明为 %c。\n\n```c\nscanf(\"%lf%c%lf\", &value1, &op, &value2);\n```\n\n* 上述代码就是读入两个操作数和一个运算符，操作数和运算符之间不能出现空格，因为空格本身也是个字符，会被作为输入字符。\n\n**字符型数据的输入与输出**\n\n* 调用字符输入函数 getchar() 可以从**标准输入**读入**一个**字符，设 ch 是字符变量，函数 getchar() 的一般调用格式为：\n\n```c\n//调用格式\nch = getchar();\n//下面的表达式可以实现输入和比较两种运算，注意，下式不可以省略括号写成 ch= getchar() != '\\n';\n(ch = getchar()) != '\\n';\n//函数原型\nint getchar(void);\n```\n\n* 功能是从标准输入读入一个字符，并赋值给变量 ch\n\n* 返回类型是`int`是为了返回`EOF`(-1)\n\n```c\nWindows → ctrl + Z\nUnix → ctrl + D\n```\n\n* 由于 getchar() 每次只能读入一个字符，要输入多个字符一般采用**循环调用**的方式\n\n* 输入多个字符时，这些字符之间不能有间隔，因为间隔符（如空格）本身也是字符，会被作为输入字符\n\n* 调用字符输出函数 putchar() 可以输出一个字符，函数 putchar() 的一般调用格式为：\n\n```c\n//调用格式\nputchar(输出参数);\n//函数原型\nint putchar(int c);//注意输出参数是 int 类型而不是 char 类型，但一次也只能接收一个字符。\n```\n\n* 其功能是向标准输出写一个字符型变量或字符型常量\n\n* 返回写了几个字符(正常情况返回 1)，EOF(这个宏的值为 -1)表示写失败\n\n* 字符常量在程序中有单引号，但用户输入输出时，字符两侧没有单引号\n\n* 下列程序顺序输入`'Q'`后，输出`'`\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    char ch;\n    ch = getchar();\n    putchar(ch);\n\n    return 0;\n}\n```\n\n* 计算机内部，数字`1`和字符`'1'`是不相等的。\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    char c;\n    char d;\n    c = 1;\n    d = '1';\n    if (c == d)\n        printf(\"Equal\\n\");\n    else\n        printf(\"Not Equal\\n\");\n    printf(\"c = %d\\n\", c);\n    printf(\"d = %d\\n\", d);\n\n    return 0;\n}\n/*\nNot Equal\nc = 1\nd = 49\n*/\n```\n\n* 如何输入`'1'`这个**字符**给 char c？(同一个变量以`%c`和`%d`形式输入，结果是不同的！)\n\n```c\nscanf(\"%c\", &c);->1\n\nscanf(\"%d\", &i);c=i;—>49\n```\n\n* `'1'`的 ASCI 编码是 49，所以当 c==49 时，它代表`'1'`\n\n```c\nprintf(\"%i %c\\n\", c, c);\n```\n\n* 一个 49 **各自表述**！\n\n* **混合输入**：下面两行代码有何不同？\n\n```c\n#include <stdio.h>\n\nint main()\n{\n    int i;\n    char c;\n    scanf(\"%d %c\", &i, &c);  //line1,两句任选一句执行\n    scanf(\"%d%c\", &i, &c);   //line2,两句任选一句执行\n    printf(\"i = %d, c = %d, c = '%c'\\n\", i, c, c);\n    return 0;\n}\n//line1:三组输入输出\n12 1\ni = 12, c = 49, c = '1'\n12a\ni = 12, c = 97, c = 'a'\n12  1\ni = 12, c = 49, c = '1'\n//line2:三组输入输出\n12 1\ni = 12, c = 32, c = ' '\n12a\ni = 12, c = 97, c = 'a'\n12  1\ni = 12, c = 32, c = ' '\n```\n\n* 由实践可以知道，有没有空格是不一样的。如果输入控制符`%d`后面**有空格**，读取数据时会**将后面的空格全部读取掉**，才会读取下一个数据。如果输入控制符`%d`后面**没空格**，说明读取数据只读到整数结束为止，后面的数据给之后的字符类型读取。\n\n**字符计算**\n\n* 一个字符加一个数字得到 ASCII 码表中那个数之后的字符\n\n* 两个字符的减，得到它们在表中的距离\n\n```c\nchar c = 'A';\nc++;\nprintf(\"%c\\n\", c);\n```\n\n```c\nint i = 'Z' - 'A';\nprintf(\"%d\\n\", i);\n```\n\n**大小写转换**\n\n* 字母在 ASCII 表中是顺序排列的\n\n* 大写字母和小写字母是分开排列的，并不在一起\n\n* `'a' - 'A'`可以得到两段之间的距离，于是\n\n* `ch + 'a' - 'A'`可以把一个大写字母 ch 变成小写字母\n\n* 而`ch + 'A' - 'a'`可以把一个小写字母 ch 变成大写字母\n\n**字符**\n\n**字符类型**\n\nchar是一种整数，也是一种特殊的类型：字符。这是因为：\n\n1. 用单引号表示的字符字面量：'a'，'1'\n\n2. \"也是一个字符\n\n3. printf和scanf里用%c来输入输出字符\n\n**字符的输入输出**\n\n1. 如何输入'1'这个字符给char c?\n\nscanf(\"%c\"，&c);->1\n\nscanf(\"%d\"，&i);c=i ;->49\n\n1. '1'的ASCII编码是49，所以当c==49时，它代表'1'\n\n2. 这是一个49的各自表述\n\n**混合输入**\n\n有何不同\n\nscanf(\"%d %c\",&i,&c);\n\nscanf(\"%d%c\",&i,&c);\n\n1. 一个字符加一个数字得到ASCII码表中那个数之后的字符\n\n2. 两个字符的减，得到它们在表中的距离\n\n**大小写转化**\n\n1. 字母在ASCII表中是顺序排列的\n\n2. 大写字母和小写字母是分开排列的，并不在一起\n\n3. ‘a’-‘A’可以得到两段之间的距离，于是a+‘a’-‘A’可以把一个大写字母变成小写字母，反之把这个运算中的‘a’与‘A’互相调换即可把一个小写字母变成大写字母\n\n**逃逸字符**\n\n用来表达无法印出来的控制字符或特殊字，在那些具有特殊符号的前面加上\\即可将其当作普通字符使用\n\n- 字符 · 意义\n\n- \\b · 回退一格\n\n- \\t · 到下一个表格位\n\n- \\n · 换行\n\n- \\r · 回车\n\n- \\\" · 双引号\n\n- \\' · 单引号\n\n- \\\\ · 反斜杠本身\n\n**制表位**\n\n1. 每行的固定位置\n\n2. 一个\\t使得输出从下一个制表位开始\n\n3. 用\\t能使上下两行对齐"},{"page":62,"body":"**本节完整补充**\n\n**逃逸字符**\n\n* 用来表达无法印出来的控制字符或特殊字符，它由一个反斜杠``开头，后面跟上另一个字符，这两个字符合起来，组成了一个字符。\n\n```c\nprintf(\"请分别输入身高的英尺和英寸，\"\n\"如输入\\\"57\\\"表示 5 英尺 7 英寸：\");\n```\n\n表-逃逸字符\n\n- 字符 · 意义 · 字符 · 意义\n\n- `\\b` · 回退一格 · `\\\"` · 双引号\n\n- `\\t` · 到下一个表格位 · `\\'` · 单引号\n\n- `\\n` · 换行 · `` · 反斜杠本身\n\n- `\\r` · 回车\n\n注意：\n\n1.回退一格意义并**不是删除**，所以不能用于输出诸如末尾不带空格的数据这些格式控制。\n\n* 实际上，不同的 shell 会对不同的控制字符做出不同的处理，所以呈现给用户的输出结果是不一样的。\n\n* Shell 对用户的标准输入执行**行编辑**，当用户按下回车之后，内容会被送入缓冲区中，`getchar()`、`scanf()`就是从缓冲区中读内容并执行后续操作的。\n\n* 例如 Sublime text 是以`BS`字符呈现给用户来表示`\\b`，而在 Macintosh 中，是把光标移动到后退一格来表示`\\b`，如果不继续输出，在用户看来什么都没发生，如果输出新的字符，则新的字符会覆盖掉前一个字符。`\\b`通常在 shell 中被解释为“回退但不删除”，当然也不否认会存在一个终端 shell 将`\\b`解释为删除。\n\n逃逸字符\n\n2.制表位\n\n* 每行的**固定位置**（而不是固定的字符数量，所以可以用来对齐）\n\n* 一个`\\t`使得输出从下一个制表位开始\n\n* 用`\\t`才能使得上下两行对齐\n\n3.`\\n`和`\\r`意味着换行和回车\n\n* 源于打字机的动作，正好也和计算机编程的动作对应。\n\n* 将打字部分返回最初位置叫作**回车**，控制滚筒滚动纸页到下一行叫作**换行**。\n\n* 之前之所以编程时都只用`\\n`是因为大部分 shell 都会把`\\n`翻译成**回车和换行两个动作的结合**。\n\n用来表达无法印出来的控制字符或特殊字，在那些具有特殊符号的前面加上\\即可将其当作普通字符使用\n\n- 字符 · 意义\n\n- \\b · 回退一格\n\n- \\t · 到下一个表格位\n\n- \\n · 换行\n\n- \\r · 回车\n\n- \\\" · 双引号\n\n- \\' · 单引号\n\n- \\\\ · 反斜杠本身\n\n**制表位**\n\n1. 每行的固定位置\n\n2. 一个\\t使得输出从下一个制表位开始\n\n3. 用\\t能使上下两行对齐\n\n逃逸字符是“用两个字符表示一个特殊字符”的语法。反斜杠 `` 是转义开始，后面的字符决定它的含义。例如 `\\n` 表示换行，`\\t` 表示制表位，`\\r` 表示回车，`` 表示一个反斜杠，`\\\"` 表示双引号，`\\'` 表示单引号。\n\n```c\n#include <stdio.h>\n\nint main(void)\n{\n    printf(\"第一行\\n\\t缩进内容\\n\");\n    printf(\"她说：\\\"你好\\\"\\\\\\n\");\n    return 0;\n}\n```\n\n三个常见误区：\n\n1. `\\b` 是回退一格，不是删除一个字符，终端对它的处理可能不同。\n\n2. `\\t` 到达下一个制表位，不是固定数量的空格，因此适合对齐表格。\n\n3. `\\r` 只把光标回到行首，`\\n` 表示换行；许多终端会把 `\\n` 解释成回车加换行，所以日常输出常用 `\\n`。\n\n调用 `scanf` 时，格式串中的空格和换行大多会被当作空白分隔，实际读取由格式符决定。`getchar` 和 `scanf` 从输入缓冲区读取，用户按下回车后，缓冲区才被送入程序。\n\n写字符串时，反斜杠会改变字面量的含义。如果要表示 Windows 路径，通常需要写成 `\"C:\\\\temp\\\\file.txt\"`，或使用正斜杠和相应的库函数。理解转义后，再遇到编译器警告“unknown escape sequence”时，就知道通常不是编译器坏了，而是 `` 后面跟了一个没有定义的字符。"},{"page":63,"body":"**本节完整补充**\n\n**自动类型转换**\n\n**1.非赋值运算的类型转换**\n\n* 当运算符的两边出现不一致的类型时，会自动转换成表达的数的范围更大的类型\n\n* char —> short —> int —> long —> long long\n\n* int —> float —> double\n\n* 对于 printf，任何小于 int 的类型会被转换成 int；float 会被转换成 double\n\n* 但是 scanf 不会，要输入 short，需要 %hd\n\n* 自动转换由 C 语言**编译系统自动完成**\n\n* 系统会先进行水平方向的转化，如果这些参加运算的数据的类型仍然不相同，再将这些数据自动转换成其中最高级别的类型。\n\n图-数据类型的自动转换规则\n\n* 举个例子，设变量 a 的类型为 char，变量 b 的类型为 int，变量 c 的类型是 double，求解表达式 a + b - c。运算次序是先计算 a+b，将 a 转换为 int 后求和，结果是 int 型；再将 a+b 的和转换为 double 型，再与 d 相减，结果是 double 型。\n\n**2.赋值运算的类型转换**\n\n* 赋值运算时，将赋值号**右侧表达式的类型**自动转换成赋值号**左侧变量的类型**。\n\n* 比如 x 的类型是 double，计算表达式 x = 1，会先将 int 型常量 1 转换为 double 型常量 1.0，然后赋值给 x，结果为 double 型。所以如果**右侧表达式的类型比赋值号左侧变量的类型级别高，运算精度会降低**。\n\n```c\ndouble x;\nx = 1;\nint y;\ny = 2.5\nprintf(\"x = %f, y = %d\", x, y);\n```\n\n* 因此我们可以知道，赋值号两侧数据的类型最好相同，至少右侧数据的类型比左侧数据的类型级别低，或者在左侧取值范围内，否则会导致精度降低，甚至出现意料之外的结果。\n\n**强制类型转换**\n\n* 强制转换通过特定的运算完成\n\n* 要把一个量强制转换成另一个类型（通常是较小的类型），需要：`(类型名)表达式`\n\n```c\n(int)20.25\n(short)32\n(short)32768\n```\n\n* 注意这时候的安全性，小的变量不总能表达大的量，比如上面的`(short)32768`\n\n```c\nprintf(\"%d\\n\", (short)32768);\nprintf(\"%d\\n\", (char)32768);\n```\n\n* 第一个结果是-32768，第二个结果是 0，因为 32768 在内存中是 1000 0000 0000 0000，对于 short 来说能表达的最大范围是-32768~32767，所以 32768 发生上溢了，直接变成-32768(可参考图-整数越界)，恰好是 short 能表示的最小的负数。而 char 型只有 8 位，取低 8 位即为 0。\n\n```c\nint i = 32768;\nshort s = (short)i;\nprintf(\"%d\\n\", i);\n//输出结果为 32768\n```\n\n* 强制类型转换只是从那个变量临时计算出了一个新的类型的值，它并不改变数据的定义，无论是**值**还是**类型**都不改变。\n\n```c\ndouble a = 2.5;\ndouble b = 0.5;\nint i = (int)a / b;\nprintf(\"%d\\n\", i);\n//输出结果为 4\ndouble a = 2.5;\ndouble b = 0.5;\nint i = (int)(a / b);\nprintf(\"%d\\n\", i);\n//输出结果为 5\n```\n\n* 强制类型转换的**优先级高于四则运算**，所以要注意在合适的地方添加括号，以满足计算的需要。\n\n**类型转换**\n\n1. 当运算符的两边出现不一致的类型时，会自动转换成较大的类型\n\n2. 大的意思是能表达的数的范围更大\n\n3. char->short->int->long->long long\n\n4. int->float->double\n\n5. 对于printf，任何小于int的类型会被转化成int;float会被转换成double\n\n6. 但是scanf不会，要输入short，需要%hd\n\n1. 要把一个量强制转换成另一个类型(通常是较小的类型)，需要：(类型)值\n\n2. 比如(int)10.2 (short)32\n\n3. 需要注意这个时候的安全性，小的变量不总能表达大的量\n\n4. 这个只是从那个变量计算出了一个新的类型的值，它并不改变那个变量，无论值还是类型都不改变\n\n5. 强制类型转换的优先级高于四则运算"},{"page":64,"body":"**本节完整补充**\n\n**bool 类型**\n\n* bool 类型（布尔类型）：最早 C 语言是没有 bool 类型的，C89 开始有这类逻辑运算类型的苗头，C99 正式确立了这种新的类型。它不是 C 语言真正的原生类型，包含 #include 头文件之后，就可以使用 bool、true 和 false。\n\n* 注意，bool 其实不是一个新类型，它也**属于整数类型**。\n\n* C 语言中最简单的数据类型包括：**整型**、**实型**、**字符型**。\n\n* 此外，C 语言还提供了一些更复杂的数据类型，称为**构造类型**或**导出类型**，它由基本类型按一定规则组合而成。\n\n**逻辑类型**\n\n**bool**\n\n1. 在宏定义中加上#include\n\n2. 之后就可以使用bool和true跟false\n\n**bool的运算**\n\n1. bool实际上还是以int的手段实现的，所以可以当作int来计算\n\n2. 也只能当作int来输入输出\n\nC 的布尔类型不是独立的高层类型，`bool` 通常仍由整数实现，`true` 和 `false` 分别对应 1 和 0。` ` 提供这些名字，`_Bool` 是 C99 引入的基础类型，二者都可以保存逻辑值。\n\n```c\n#include <stdio.h>\n#include <stdbool.h>\n\nint main(void)\n{\n    bool ok = true;\n    int n = 7;\n    bool isOdd = n % 2 != 0;\n\n    printf(\"%d %d\\n\", ok, isOdd);\n    return 0;\n}\n```\n\n逻辑值可以直接作为条件：`if (ok)` 比 `if (ok == true)` 更符合 C 的习惯；`if (!ok)` 表示条件不成立。关系运算的结果是 0 或 1，因此 `bool` 与整数参与运算时会发生整数转换。\n\n初学时不要把“逻辑真”和“变量存在”混为一谈。`0` 是假，`NULL` 表示空指针，`'\\0'` 是字符串结束符，`0.0` 是浮点零；它们可能都在条件中表现为假，但用途不同。命名布尔变量时可以用 `isValid`、`hasNext`、`found` 等动词开头，让读者一眼知道它代表状态。\n\n使用 `bool` 时注意头文件拼写是 `stdbool.h`，而不是 `stbool.h`；`printf` 输出布尔值通常用 `%d`，也不要期待 C 自动把 `true` 打印成英文单词。\n\n布尔类型的实用价值是让代码意图可读：`bool isEven`、`bool hasNext`、`bool found` 这些名称比 `0/1` 更容易说明变量代表什么。C99 引入 `_Bool` 作为基础类型，` ` 又把 `bool`、`true`、`false` 定义为可用的名字；`true` 通常展开为 1，`false` 通常展开为 0。\n\n常见写法如下：\n\n```c\n#include <stdbool.h>\n\nbool isValid(int n)\n{\n    return n >= 0 && n <= 100;\n}\n\nif (isValid(x)) {\n    printf(\"valid\\n\");\n} else {\n    printf(\"invalid\\n\");\n}\n```\n\n这里不需要写 `if (isValid(x) == true)`，因为条件表达式本身就接受真值；也不需要用 `isValid(x) ? 1 : 0` 包装函数的返回值。`!done` 比 `done == false` 更简洁明确。\n\n- 布尔值虽然与整数兼容，但不要把它当成普通整数随意运算。`printf` 通常用 `%d` 输出 `true/false` 对应的 1/0，不会自动写成英文单词；若需要输出“是/否”，应显式写条件字符串。逻辑运算 `&&`、` · `、`!` 会短路：`a != 0 && b / a > 1` 中，只有 `a != 0` 成立时才会计算右边，因此避免除零的效果比单独写几个 `if` 更自然。"},{"page":65,"body":"**本节完整补充**\n\n**逻辑运算**\n\n* **逻辑运算**是对逻辑量进行的运算，结果只有 0 或 1\n\n* **逻辑量**是关系运算或者逻辑运算的结果\n\n* 用逻辑运算符将关系表达式或逻辑量连接起来的式子，称为**逻辑表达式**。\n\n表-逻辑运算符\n\n- 运算符 · 描述 · 示例 · 结果\n\n- ! · 逻辑非 · !a · 如果 a 是 true，结果就是 false，反之则反\n\n- && · 逻辑与 · a && b · 如果 a 和 b 都是 true，结果才是 true，其中有一个为 false，结果就为 false\n\n- \\ · \\ · 逻辑或 · a \\ · \\ · b · 如果 a 和 b 有一个为 true，结果就是 true，两个都为 false，结果才为 false\n\n* 第三次强调运算符优先级：括号 > 单目 > 算术 > 关系 > 逻辑 > 赋值 > 逗号\n\n* 有一个特殊的地方，逻辑非是单目运算符，还有另一套适用规则\n\n* 优先级：单目运算符 > 双目运算符\n\n* 因此对于表达式`!age < 20`，由于`!age`只能为 0 或 1，所以`!age < 20`表达式永远为 1。\n\n注记：\n\n反之亦然：(p → q) ↔ (q → p);\n\n反之则反：(p → q) ↔ (﹁p → ﹁q);\n\n同样的，逻辑运算符内部的优先级也不一样，将已学过的所有运算符整理为下表\n\n表-逻辑运算符优先级与结合性\n\n- 优先级 · 运算符 · 结合性\n\n- 1 · () · 从左到右\n\n- 2 · ! + - ++ -- · ==从右到左(单目的 + 和 -)==\n\n- 3 · * / % · 从左到右\n\n- 4 · + - · 从左到右\n\n- 5 · >= · 从左到右\n\n- 6 · == != · 从左到右\n\n- 7 · && · 从左到右\n\n- 8 · \\ · \\ · 从左到右\n\n- 9 · = += -= *= /= · ==从右到左==\n\n- * `a · 3+10&&2`等价于`a · ((3+10)&&2)`\n\n* 如果要表达数学中的区间，如 x∈(2,5)，应该如何写 C 的表达式呢？\n\n* 通过第三章及本章，我们可以知道 C 语言描述关系和数学语言有一点不同，`2 2 && x < 5`\n\n**有些运算**\n\n1. 逻辑运算是对逻辑量进行的运算，结果只有0或者1\n\n2. 逻辑量是关系运算或逻辑运算的结果\n\n- ！ · 逻辑非 · !a · 如果a是true结果就是false如果a是fasle结果就是true\n\n- && · 逻辑与 · a&&b · 如果a和b都是是true结果就是true否则就是false\n\n- \\ · \\ · 逻辑或 · a\\ · \\ · b · 如果a和b有一个是true结果就是true两个都是false，结果才是false\n\n**try(尝试)**\n\n如何判断一个字符c是否是大写字母？\n\nc >= ‘A’&&c<=‘Z’\n\n**逻辑优先级**\n\n!>&&>||\n\n**总体优先级排名**\n\n- 2 · ！+- ++ -- · 从右到左(单目的+和-)\n\n- 3 · / % · 从左到右\n\n- 9 · = += -= *= /= %= · 从右到左\n\n**短路**\n\n1. 逻辑运算是自左向右进行的，如果左边的结果已经能够决定结果了，就不会做右边的计算了\n\n2. 对于&&，左边是false时就不会做右边的运算了\n\n- 3. 对于 · ，左边是true时就不会做右边的运算了\n\n4. 不要把赋值，包括复合赋值组合进表达式"},{"page":66,"body":"**本节完整补充**\n\n**条件运算和逗号运算**\n\n* 条件运算符最初起源于 BCPL 程序设计语言，而 B 语言就是 C 语言的前身，所以条件运算符也属于历史遗留问题\n\n```c\n//条件运算符格式\n条件判断 ? 条件满足 : 条件不满足\n```\n\n* 条件运算符的优先级高于赋值运算符，但是低于其他的所有运算符。\n\n* 条件运算符是**自右向左**结合的，所以如果遇到复杂的嵌套条件运算，需要先把分支算清楚再回溯到最外层。\n\n* 条件运算符看似将 if-else 的选择结构简化为了一句语句，但如果表达式存在赋值语句或者存在嵌套条件运算，会大大增加阅读和维护代码的难度，因此不建议使用嵌套的条件表达式。\n\n**逗号运算**\n\n* 逗号在 C 语言中属于一种运算符，其优先级是所有运算符中最低的，甚至比赋值运算还低。\n\n* 带括号的逗号表达式，结果是逗号**后面那个表达式**\n\n```c\n //i 的结果为 7\ni = 2 + 5, 5 + 6;\n//i 的结果为 11\ni = (2 + 5, 5 + 6);\n```\n\n* 因此，C 语言中以下 a、b 变量初始化的方式是错误的，它会导致 a 的初值不确定，b 被赋初值 0；\n\n```c\nint a, b = 0;\n```\n\n* 目前，逗号表达式主要用于 for 语句，如果在 for 内三块区域需要进行多个计算，就可以在区域内使用逗号表达式实现\n\n```c\nfor (i = 0, j = 10; i < j; i++, j--)\n```\n\n**条件运算符**\n\n1. count = (count>20)？count-10：count+10;\n\n2. 当count>20是真的时候执行前者，是假执行后者\n\n3. 优先级:条件运算符的优先级高于赋值运算符，但是低于其他运算符\n\n4. 不要嵌套条件表达式(很容易出问题)\n\n**逗号运算符**\n\n1. 逗号用来连接两个表达式，并以其右边的表达式的值作为它的结果。逗号的优先级是所有运算符中最低的，所以它两边的表达式会先计算;逗号的组合关系是自左像右，所以左边的表达式会先计算，而右边的表达式的值就留下来作为逗号运算的结果\n\n2. 比如在for中使用\n\n3. for(i = 1,j=8;i<j;i++,j--)\n\n条件运算符是唯一的三目运算符，适合把简单二选一写成一个表达式。\n\n```c\nint a = 7, b = 3;\nint max = a > b ? a : b;\n```\n\n它等价于一个小 if-else，但不适合嵌套多层；复杂分支仍应写 if。条件运算的优先级较低，混合表达式时建议给三目整体加括号。\n\n逗号运算符从左到右计算两个表达式，整体值是右边表达式的值。常见位置是 for 的多变量更新，例如同时推进前后下标。不要在普通参数列表中依赖逗号运算符，因为函数参数里的逗号首先用于分隔参数。写代码时以清晰为先，能不用复杂的逗号表达式就不用。"}];
